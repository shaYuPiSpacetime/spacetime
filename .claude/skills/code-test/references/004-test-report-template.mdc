---
description: 变更代码测试 - 测试报告模板（testreport.md）
alwaysApply: false
---

# 测试报告模板

> 测试报告是测试执行的唯一记录，独立于 testcase.md（测试设计）。
> 输出位置：`docs/测试文档/{模块名称}-testreport.md`

---

## 判定规则

| 条件 | 结论 |
|------|------|
| 全部通过（跳过 ≤ 2 条且均为 P2/P3） | ✅ 通过 |
| 有失败但全部为 P2/P3，或跳过 > 2 条 | 🟡 有条件通过（必须填遗留项） |
| 任何 P0 失败，或 P1 失败 ≥ 2 条 | 🔴 不通过 |

## 结果收集方式

| 层级 | 结果来源 | 提取方式 |
|------|---------|---------|
| L1 | cURL 脚本终端输出 | 解析脚本尾部汇总行 `总计 N / 通过 M / 失败 K`，或逐条 ✅/❌ 标记 |
| L2/L3 | `mvn test` 控制台 | 解析 `Tests run: X, Failures: Y, Errors: Z, Skipped: W` |
| L4 | Playwright list/JSON 输出 | 解析 `N passed, M failed, K skipped` |
| 手动 | 用户手动填写 | testcase.md §7 表格中的「实际结果」和「状态」列 |

---

## testreport.md 文档模板

````markdown
# {模块名称} - 测试报告

> **关联文档**：
> - 测试用例：`docs/测试文档/{模块名称}-testcase.md`

---

## 1. 测试概况

| 项目 | 信息 |
|------|------|
| 功能名称 | {功能名称} |
| 测试环境 | {BASE_URL，如 https://test.lx-rhino.com} |
| 执行日期 | {YYYY-MM-DD} |
| 执行人 | {姓名} |
| 后端版本 | {分支名 / commit hash} |
| 前端版本 | {分支名 / commit hash} |
| 测试策略 | {L1+L2+L3} + {手动+L4} |
| 测试模式 | 完整模式 / 轻量模式 / 增量模式 |

## 2. 测试结果汇总

| 层级 | 总数 | 通过 ✅ | 失败 ❌ | 跳过 ⏭️ | 通过率 |
|------|------|--------|--------|---------|--------|
| L1 接口测试 | {N} | {N} | {N} | {N} | {%} |
| L2 Controller | {N} | {N} | {N} | {N} | {%} |
| L3 Service | {N} | {N} | {N} | {N} | {%} |
| L4 E2E | {N} | {N} | {N} | {N} | {%} |
| 手动测试 | {N} | {N} | {N} | {N} | {%} |
| **合计** | **{N}** | **{N}** | **{N}** | **{N}** | **{%}** |

## 3. 测试结论

**判定结果**：✅ 通过 / 🟡 有条件通过 / 🔴 不通过

**判定依据**：
- P0 用例：{通过数}/{总数} 通过
- P1 用例：{通过数}/{总数} 通过
- 跳过用例：{数量} 条（{原因概述}）

## 4. 失败用例明细

> 无失败用例则写"无"。

| 用例ID | 层级 | 优先级 | 场景描述 | 失败现象 | 原因分析 | 后续处理 |
|--------|------|--------|---------|---------|---------|---------|
| {ID} | {L1/L2/L3/L4} | {P0/P1/P2/P3} | {场景} | {实际结果} | {原因} | {处理方式} |

## 5. 跳过用例明细

> 无跳过用例则写"无"。

| 用例ID | 层级 | 优先级 | 场景描述 | 跳过原因 | 是否需要补测 |
|--------|------|--------|---------|---------|------------|
| {ID} | {L1/L4} | {P0/P1/P2/P3} | {场景} | {原因} | {是/否} |

## 6. 各层级执行详情

### 6.1 L1 接口测试

```
执行命令: API_URL=xxx TOKEN=xxx bash docs/测试文档/{模块名称}-test-l1.sh
执行时间: {HH:MM}
```

| 用例ID | 优先级 | 场景 | HTTP 状态 | 结果 | 备注 |
|--------|-------|------|----------|------|------|
| F1-P0-01 | P0 | {场景} | {200/400/500} | ✅/❌ | |

### 6.2 L2 Controller 测试（按需）

```
执行命令: cd backend && mvn test -Dtest=XxxControllerTest
执行时间: {HH:MM}
```

| 用例ID | 测试方法 | 结果 | 备注 |
|--------|---------|------|------|
| L2-01 | {method} | ✅/❌ | |

### 6.3 L3 Service 测试（按需）

```
执行命令: cd backend && mvn test -Dtest=XxxServiceTest
执行时间: {HH:MM}
```

| 用例ID | 测试方法 | 结果 | 备注 |
|--------|---------|------|------|
| L3-01 | {method} | ✅/❌ | |

### 6.4 L4 E2E 浏览器测试（按需）

```
执行命令: cd frontend && BASE_URL=xxx TOKEN=xxx npx playwright test
执行时间: {HH:MM}
Playwright 报告: frontend/playwright-report/index.html
```

| 用例ID | 优先级 | 场景 | 耗时 | 结果 | 截图 |
|--------|-------|------|------|------|------|
| L4-01 | P0 | {场景} | {N}s | ✅/❌/⏭️ | {失败时附截图路径} |

### 6.5 前端手动测试

| 用例ID | 优先级 | 操作步骤 | 期望结果 | 实际结果 | 状态 |
|--------|-------|---------|---------|---------|------|
| M-01 | P0 | {步骤} | {期望} | {实际} | ✅/❌ |

## 7. 遗留问题（🟡 有条件通过时必填）

> 判定为 ✅ 通过时可省略本章节。

| 编号 | 问题描述 | 影响范围 | 优先级 | 预计处理时间 | 负责人 |
|------|---------|---------|--------|------------|--------|
| 1 | {问题} | {影响} | {高/中/低} | {日期} | {人} |

## 8. 测试建议

> 对后续迭代的测试改进建议（可选）。

- {建议1}
- {建议2}
````
