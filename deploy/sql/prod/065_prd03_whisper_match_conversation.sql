-- PRD-03 历史迁移编号兼容占位。
--
-- 早期草案曾在本文件创建会话、成员、消息和悄悄话 4 张表，但字段包含已经废弃的
-- 正文密文、本地普通私信未读数和重复 TIM 映射。生产脚本按编号执行时，旧表会抢先
-- 创建，导致后续 CREATE TABLE IF NOT EXISTS 无法修正结构。
--
-- 最终 14 张表及全部中文字段/枚举注释统一由以下迁移负责：
--   070_prd03_message_center_closure.sql
--
-- 保留本文件是为了兼容已经登记过 065 编号的发布流水，不创建、修改或删除任何表。
