# 腾讯表格待处理 UI 闭环技术方案

## 范围

本次仅闭环腾讯表格 `BB08J2` 中状态为“待处理”的第 26～31 行，不扩展到“处理中”条目。

| 行号 | 问题 | 实施基线 |
|---|---|---|
| 26 | 千寻币余额背景需更换 | 保留真实余额、明细控件，以 CSS 低透明度几何字形还原蓝湖水印，禁止整页截图 |
| 27 | 充值须知弹窗偏下、字号偏小 | 使用 2026-08-05 刷新稿：弹窗 `620×570rpx`、正文 `26/44rpx`、顶部 `386rpx` |
| 28 | 暂无记录图片调整 | 使用真实 View 绘制文档/搜索空态与五个装饰标记，文案统一为“暂无记录” |
| 29 | 千寻互动图片调整 | 使用蓝湖“千寻-切图”的 `编组 6` 无文字 2x 切图，文字与按钮保持真实组件 |
| 30 | 我的页头像、间距、背景、图标、字重、Tab 位置 | 保持 `06-我的` 已对齐几何，扩大头像资料区真实点击面；复核卡片、图标和底部 Tab 门禁 |
| 31 | 点击跳转“心动” | 三个统计项均调用 `Taro.switchTab` 跳转 `/pages/community/index` |

## 组件与资源映射

- 千寻币余额卡：`BalanceCard` + `BalanceWatermarks`，所有业务文案和点击区均为真实组件。
- 千寻币空态：`EmptyState` + `EmptyPlusMark` + `EmptyRingMark`，不引入整页截图。
- 千寻互动引导卡：`qianxunPostGuideBg` 为蓝湖官方独立切图，原始尺寸 `1300×376`，运行尺寸 `650×188rpx`，不缩放、不压缩、不转换上传 OSS。
- 我的页：`HeaderBlock` 整块可点击；`StatsCard` 每个统计项独立可点击；底部 Tab 继续使用现有真实组件和包内 Tab 图标。

## 验证

1. 专项静态门禁先红后绿，锁定尺寸、资源、真实组件和导航契约。
2. 执行千寻币、千寻互动、资料页、底部 Tab 现有门禁。
3. 执行 TypeScript、小程序生产构建及微信开发者工具运行态截图/跳转验收。
4. 生成独立测试报告和截图证据；无数据库结构或数据变更，因此不执行 SQL。
