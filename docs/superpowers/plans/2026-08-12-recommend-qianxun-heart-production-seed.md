# 推荐、千寻、心动生产感数据与推荐头部图标实施计划

> 日期：2026-08-12
> 范围：小程序推荐/理想型头部图标；测试环境推荐、千寻、心动数据
> 原则：蓝湖尺寸为唯一视觉基线；测试先行；数据脚本幂等且只操作带固定种子标识的数据。

## 目标

1. 推荐页“历史记录/偏好设置”和理想型页“历史信息”可见图标严格恢复为蓝湖 `36×36rpx`，保留现有 `70/72rpx` 点击热区和胶囊避让。
2. 围绕手机号 `17366629764` 在当前测试数据库生成接近生产内容密度的数据：推荐候选与回看、千寻图文动态/话题/评论/回复/点赞、心动双方喜欢/访客/匹配。
3. 所有种子数据可重复执行，不修改无种子标识的既有业务数据，并附执行后数量核验。

## Task 1：锁定推荐头部图标蓝湖门禁

**文件**

- 修改：`miniapp/scripts/test-prd08-recommend-ideal-closure.cjs`
- 修改：`miniapp/src/pages/recommend/index.tsx`

**步骤**

1. 增加静态测试，断言三枚可见图标均为 `36rpx`，同时断言点击热区仍为 `70/72rpx`。
2. 运行 `node --test scripts/test-prd08-recommend-ideal-closure.cjs`，确认旧代码因 `48/52rpx` 失败。
3. 仅调整 `Image` 可见尺寸，不改变导航事件、热区和胶囊定位。
4. 重跑测试并执行小程序构建。

## Task 2：建立生产感种子数据门禁

**文件**

- 新增：`scripts/test-rich-module-seed.cjs`
- 新增：`deploy/sql/ops/2026-08-12-seed-recommend-qianxun-heart-rich-data.sql`

**步骤**

1. 先读取当前数据库真实表结构、枚举值和目标账号状态。
2. 写失败测试，约束脚本必须：
   - 以目标手机号定位用户，禁止写死数据库主键；
   - 使用固定业务号前缀并支持重复执行；
   - 至少准备 12 名完整候选、12 条图文动态、18 条评论/回复，以及心动/访客/匹配数据；
   - 使用多组现有 OSS 公网图片；
   - 不执行全表删除或更新。
3. 编写事务型 SQL，候选资料包含昵称、头像、年龄、地区、职业、教育、简介、相册和三项通过认证；千寻内容覆盖不同话题、图片数量和时间；心动覆盖双方喜欢、访客与匹配状态。
4. 对测试数据库执行一次，再执行第二次验证幂等。

## Task 3：闭环验证

1. 查询推荐候选资格、千寻内容密度、心动/访客/匹配数量，并保存核验结果。
2. 运行新增门禁、PRD-08 门禁与小程序构建。
3. 生成验收报告，说明蓝湖尺寸、数据覆盖、执行结果及未覆盖项；不在未取得运行截图时宣称截图级 1:1。
