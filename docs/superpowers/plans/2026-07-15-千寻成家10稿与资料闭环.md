# 千寻成家 10 稿与资料闭环实施计划

> 目标：修复开发固定登录、资料相册六宫格、标签块展示，按蓝湖最新 10 个状态还原千寻成家，并把指定账号三重认证和真实数据分流完整打通。

## 任务 1：固定 Token 单一开关

- 先修改 `miniapp/scripts/validate-dev-fixed-login.mjs`，要求运行态与启动页共同读取 `MINIAPP_DEV_FIXED_LOGIN`，默认开发监听开启，显式登录调试脚本关闭。
- 运行门禁确认先失败。
- 修改 `miniapp/src/constants/config.ts`、`miniapp/package.json`、`miniapp/src/app.tsx`，消除“启动页开关”和“运行态开关”分裂。
- 运行固定登录、发布启动隔离门禁。

## 任务 2：更多照片六槽上传与回显

- 先补相册六槽、异常顺序回填、真实错误提示和运行时上传数量配置测试。
- 增加幂等数据库迁移，把动态配置中的相册上限调整为六张，不写死页面限制。
- 修复前端相册槽位合并和上传错误处理；后端继续从材料 JSON 返回真实 `sortOrder`。
- 执行本地迁移，用第 4 槽新增/查询/删除验证第二行闭环。

## 任务 3：标签统一组件

- 先补编辑页、标签选择页、主页预览三处的块换行、单块不拆行、稳定颜色与真实 code 回显测试。
- 新增共享标签色板/标签块组件，三处统一使用 `{code,label}` 数据。
- 过滤接口返回的重复 `ALL` 分组，保证颜色不随筛选或顺序变化。

## 任务 4：千寻成家最新 10 稿

- 以蓝湖 375×812 最新十稿为基线，建立页面 token、组件和状态映射。
- 对齐真实 `/miniapp/community/posts` 契约，增加 `FOLLOWING/CITY/HOT` 场景查询，作者读取 `app_user`。
- 重建推荐页：关注空态、关注无更新、同城信息流、热门话题、动态卡、更多操作、举报、未认证弹层。
- 重建发布动态页，正文、图片、话题和发布按钮使用真实组件与接口。
- 每个状态分别构建、截图、记录差异并修正。

## 任务 5：指定账号认证与分流

- 新增只操作手机号 `17366629764` 三类最新认证记录的幂等 SQL，同时写审核历史并校验材料完整性。
- 执行 SQL 后验证 `verifyLevel=3`、`coreAccessStatus=CORE_ALLOWED`。
- 千寻默认进入成家/关注；关注接口有数据展示列表，无数据展示“关注暂无数据”；用户点击同城后再按同城数据展示列表或空态。

## 任务 6：总体验收

- 运行相关 Node 门禁、小程序构建、后端单元测试和本地接口回归。
- 生成 `docs/验收报告/2026-07-15-千寻成家10稿与资料闭环-acceptance.md`，附页面截图、差异清单、还原度和未决依赖。
