# PRD-05 推荐与社区全链路测试报告

> **2026-08-04 生产复验更新：🔴 生产验收不通过。** 本地候选版本回归通过，但线上仍部署旧版前端包，且用户提供的 Demo 地址最终返回 403。本文第 7 节为当前发布结论，优先于 2026-08-03 的本地闭环结论。

> 执行日期：2026-08-03
>
> 测试用例：`docs/测试文档/社区互动-PRD05-testcase.md`
>
> 技术方案：`docs/技术方案/2026-08-03-PRD-05-推荐与社区全链路闭环-tcdesign.md`
>
> UI 验收：`docs/测试文档/社区互动-PRD05-UI验收报告.md`

## 1. 结论

PRD-05 本轮闭环实现的后端、管理后台、小程序、开发库迁移和真实接口冒烟测试均通过。现有帖子、评论、举报数据在迁移前后数量一致，迁移脚本连续执行两次未产生重复话题、字典或菜单。PRD-03 消息域源码未被修改，社区仅保留聊天举报解析契约和事件 Outbox。

## 2. 自动化执行结果

| 层级 | 执行项 | 结果 |
| --- | --- | --- |
| L1 | 开发环境真实接口冒烟 | 42/42 通过，0 失败，0 跳过 |
| L2/L3 | Java 21 Maven 全量测试 | 455/455 通过，0 失败，0 错误，0 跳过 |
| L3 | 微信文本/图片内容安全契约 Mock | 通过、拒绝、超时、异常分支均通过 |
| L4 | 管理后台社区契约 Playwright | 12/12 通过 |
| L4 | 管理后台六页真实数据视觉验收 | 六页双视口及详情抽屉通过 |
| 构建 | 管理后台 `npm run build` | 通过，1719 个模块完成转换 |
| 构建 | 小程序 `npm run build:weapp` | 通过，76 个页面注册校验通过 |
| 静态门禁 | 小程序主流程、登录产物、消息边界 | 通过 |

### 2.1 后端

- Java：21。
- Maven：`Tests run: 455, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- 覆盖社区状态机、字典历史显示、四分区配置、权限、并发版本、草稿、互动统计、处罚限制、内容安全降级、媒体审核回调及 Outbox。

### 2.2 管理后台

- 生产构建通过；产物正常生成。
- 社区契约 Playwright 12/12 通过，覆盖六个正式路由、旧路由重定向、动态文案、空态、详情抽屉及无权限态不发起业务请求。
- 真实后台与开发库联调截图覆盖 1440×900、1280×800；举报、配置、内容详情在修复后分别做了定向复验。

### 2.3 小程序

- `build:weapp` 编译成功。
- 页面注册校验：76 个页面均为单 Page 注册，App 注册为 1。
- 登录固定 Token 产物门禁、主流程 UI 覆盖门禁通过。
- 包体结果：主包 1.29 MiB，千寻分包 97.6 KiB，总包 1.95 MiB。

### 2.4 真实接口 L1

开发环境真实接口通过 42/42，覆盖：

- 管理端登录、meta、六页统计/列表、详情抽屉、举报上下文、配置版本与变更记录。
- 小程序 meta、配置、话题、关注/同城/热门、诚意贴、本人动态、互动历史、浏览历史、关注粉丝、隐藏作者和个人汇总。
- 服务端草稿保存、恢复、内容回显、删除闭环。
- 管理端和小程序未登录访问拒绝。

固定 Token 对应账号当前不具备三项认证，测试未修改该账号认证状态，也未强行执行正向发布；真实环境已验证准入拒绝，发布状态机正向分支由单元测试与契约 Mock 覆盖。

## 3. 数据库迁移验收

- 迁移文件：`deploy/sql/prod/058_prd05_community_full_closure.sql`。
- 执行范围：开发库；未操作生产库。
- 执行前备份：`/tmp/spacetime-prd05-backup-20260803-2009/prd05-before.sql`。
- 备份大小：602K。
- SHA-256：`e45384f0def37da8941aa17cc46d8ff9aafad410f750bd2a9463560fa4690dd1`。
- 脚本连续执行两次，第二次执行成功且无重复数据。

| 核对项 | 结果 |
| --- | --- |
| 原帖子 | 6，迁移前后不变 |
| 原评论 | 5，迁移前后不变 |
| 原举报 | 3，迁移前后不变 |
| 去重后话题 | 3 |
| 新增社区能力表 | 12 张 |
| 动态文案配置 | 239 条 |
| 社区字典重复 code | 0 |
| 互动准入模式选项 | 2 个 |
| 正式可见业务路由 | 6 个 |
| 角色 1 社区权限节点 | 17 个 |
| 已逻辑删除草稿残留 | 0 |
| 举报 `target_id` | `varchar(64)` |

## 4. PRD-03 边界

- 未修改小程序消息页面、消息 Service、消息 Store、消息类型定义。
- 未修改后端消息或 IM 模块源码和消息表。
- 聊天举报只新增公共社区域解析契约；解析器未接入时失败关闭，不接受客户端正文或用户 ID 作为可信上下文。
- 点赞、评论、关注、审核与举报事件写社区 Outbox，消息投递失败不回滚社区主事务。

## 5. 未执行的外部实网项

未调用真实微信内容安全生产服务，因为当前没有本次测试授权的微信实网回调、媒体和生产凭据。该项不是代码失败；通过契约 Mock 已覆盖文本与图片审核的通过、拒绝、超时和异常路径。上线前仍需使用已授权的小程序环境完成一次微信实网联调，并轮换曾暴露的开发凭据。

## 6. 最终判定

本次 PRD-05 计划范围内的开发库闭环验收通过。当前无自动化失败项；剩余事项仅为上线环境的微信内容安全实网联调及 PRD-03 后续消费社区 Outbox，不属于本次代码阻塞。

## 7. 2026-08-04 生产环境复验

### 7.1 当前结论

**判定结果：🔴 不通过。**

本地待发布版本的后端、管理后台六页契约和生产构建均通过；使用已登录管理员会话复验后确认，生产菜单已经下发 PRD-05 六个新地址，但生产站点仍运行不包含其中五个路由的旧前端产物。因此“内容管理点不动”的直接原因是：点击 `/community/content` 后命中前端兜底路由并立即跳回 `/dashboard`，属于生产菜单与前端发布物版本不一致，不是菜单按钮事件未绑定。

### 7.2 结果汇总

| 层级 | 总数 | 通过 | 失败 | 跳过 | 结果 |
|---|---:|---:|---:|---:|---|
| PRD-05 后端定向 L2/L3 | 42 | 42 | 0 | 0 | ✅ |
| 后端全量回归 | 455 | 455 | 0 | 0 | ✅ |
| 管理后台六页 L4 契约 | 12 | 12 | 0 | 0 | ✅ |
| 管理后台生产构建 | 1 | 1 | 0 | 0 | ✅ |
| 生产发布静态核对 | 4 | 2 | 2 | 0 | ❌ |
| 生产六页导航 | 6 | 1 | 5 | 0 | ❌ 仅举报管理可打开 |
| 生产旧版只读冒烟 | 7 | 6 | 1 | 0 | ❌ 评论无审核操作 |
| 生产治理与配置写入 | 1 | 0 | 0 | 1 | ⏭️ 新版页面缺失且无可回滚测试数据 |

### 7.3 生产失败证据

| 用例 | 实际结果 | 判定 |
|---|---|---|
| ONLINE-P0-001 管理后台入口 | `/dashboard` HTTP 200，未登录进入 `/login` | ✅ |
| ONLINE-P0-002 新版发布物 | 线上 `/assets/index-xlTlEz6h.js` 不包含 `community/content`、`community/moments`、`community/comment-audit`、`community/topics`、`/admin/community/meta` 或“审核规则配置”；仍包含旧 `/community/posts` | ❌ P0 |
| ONLINE-P0-003 内容管理菜单可导航 | 已登录账号 `peter` 展开菜单后可见 `/community/content`，点击后立即回到 `/dashboard`，页面仍为“首页概览” | ❌ P0 |
| ONLINE-P1-001 Demo 地址 | 首次 301 到尾斜杠目录，最终由 `nginx/1.27.5` 返回 HTTP 403 | ❌ P1 |
| ONLINE-P1-002 六页只读冒烟 | `/community/reports` 正常打开；`/community/content`、`/community/moments`、`/community/comment-audit`、`/community/topics`、`/community/config` 均回到 `/dashboard` | ❌ P0 |
| ONLINE-P1-003 治理与配置写闭环 | 新版五页无法进入；未对旧版生产数据提交审核、举报处理或配置保存 | ⏭️ 跳过 |
| ONLINE-P3-001 未登录拦截 | `/api/admin/community/meta` 与旧列表接口均返回 HTTP 401 | ✅ |

线上入口 HTML 的 `Last-Modified` 为 `2026-08-03 04:02:28 GMT`。线上 JS SHA-256 为 `e95e555ee1aa32588112409826b190a4b61aacdf56cf97e786f22d059540fc89`；本次本地构建生成 `index-CvJ3OtLs.js`，SHA-256 为 `7cdd1e0ac71747aaf2fa0f3ab1dc49add0da0d6e50ebc631dc6a005b18f215f1`，两者不是同一发布物。

### 7.4 已登录旧版页面冒烟

在不修改生产数据的前提下，已完成当前仍可访问旧页面的只读与表单展示验证：

- `/community/posts`：加载 6 条记录；关键词查询“【验收】待审核社区动态”后剩 1 条，重置恢复 6 条；审核弹窗、结果选项和说明输入框可打开后取消。
- `/community/comments`：加载 5 条记录；查询“【验收】待审核评论”后剩 1 条，重置恢复 5 条；操作列没有审核按钮，无法形成评论审核闭环。
- `/community/reports`：加载 3 条记录；查询、重置及待处理举报弹窗可打开后取消。
- `/community/configs`：页面可打开，但显示“暂无首页Tab配置”和“暂无配置数据”，未执行保存。
- 浏览器控制台采集到一条 Axios HTTP 403 错误；生产页面未提供足够上下文定位具体接口，需在新版部署后结合服务端访问日志复核。

### 7.5 本地候选版本验证

- 仅应用本次暂存补丁的干净工作树执行 `JAVA_HOME=$(/usr/libexec/java_home -v 21) mvn test`：`455/455` 通过，`BUILD SUCCESS`。
- PRD-05 后端定向测试：`42/42` 通过，覆盖管理端审核、举报治理、配置边界以及小程序社区状态机。
- `npx playwright test --config=e2e-tests/playwright.contract.config.ts`：`12/12` 通过，六个正式路由、旧路由重定向、空态、详情抽屉及无权限态通过。
- `npm run build`：Vite 构建成功，1719 个模块完成转换；构建包含六个正式社区路由和 `/admin/community/meta`。
- 仅应用本次暂存补丁的小程序工作树执行 PRD-05 静态门禁与 `npm run build:weapp`：通过；76 个页面注册、固定登录产物和包体门禁均通过。完整预构建依赖仓库忽略的本机 `.lanhu-ref` 蓝湖参考素材，临时工作树以只读链接挂载同一素材后完成验证。

### 7.6 阻塞与发布后补测

1. 已登录会话可正常读取菜单和旧版社区数据；当前阻塞不再是登录，而是生产前端发布物缺少五个正式路由。
2. 生产菜单已出现 058 规划的六个地址，说明菜单数据已更新或存在等价配置；仍需在部署时确认 058 全量表结构、权限和动态配置均已应用，不能只凭菜单判断迁移完整。
3. 本轮没有可清理且可回滚的生产话题/配置测试数据，且新版治理页面尚不可访问，因此未提交任何生产写操作。
4. 发布时必须同时部署当前前端构建、后端版本并执行 058 幂等迁移；只更新其中一个会继续出现菜单、路由或接口不一致。
5. 发布完成后按 `ONLINE-P0-003`、`ONLINE-P1-002`、`ONLINE-P1-003` 补测并更新本报告，生产验收通过前不得沿用 2026-08-03 的本地“通过”结论。
