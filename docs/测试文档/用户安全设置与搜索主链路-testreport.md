# 用户安全设置与搜索主链路 - 测试报告

> **关联文档**：
> - 测试用例：`docs/测试文档/用户安全设置与搜索主链路-testcase.md`

---

## 1. 测试概况

| 项目 | 信息 |
|------|------|
| 功能名称 | 用户安全设置与搜索主链路 |
| 测试环境 | 后端 `http://localhost:8080`；前端 `http://localhost:5173` |
| 执行日期 | 2026-05-29 |
| 执行人 | Codex |
| 后端版本 | `master` / `f488978` |
| 前端版本 | `master` / `f488978` |
| 测试策略 | L1 + L2 + L3 + 手动 + L4 |
| 测试模式 | 完整模式 |

## 2. 测试环境准备

| 动作 | 结果 |
|------|------|
| 重启后端 | ✅ `http://localhost:8080` 正常监听 |
| 重启前端 | ✅ `http://localhost:5173` 正常监听 |
| 落 PRD-06 表结构 | ✅ 使用 JDBC 执行 `backend/docs/sql/schema-user-security.sql` |
| 补充后台权限码 | ✅ 给 `peter` 账号角色补充 `user:security:view`、`user:feedback:list`、`user:feedback:handle`、`user:cancel:list`、`user:cancel:handle` |
| 准备小程序 Token | ✅ Redis 写入 `miniapp:token:e2e-miniapp-token`，userId=1 |
| 准备链路数据 | ✅ 通过接口创建隐私/通知设置、屏蔽关系、关键词、反馈、注销申请、搜索日志 |

## 3. 测试结果汇总

| 层级 | 总数 | 通过 ✅ | 失败 ❌ | 跳过 ⏭️ | 通过率 |
|------|------|--------|--------|---------|--------|
| L1 接口测试 | 32 | 32 | 0 | 0 | 100% |
| L2/L3 后端测试套件 | 103 | 102 | 0 | 1 | 99.03% |
| 前端构建 | 1 | 1 | 0 | 0 | 100% |
| L4 E2E | 8 | 8 | 0 | 0 | 100% |
| 手动/小程序联调 | 12 | 0 | 0 | 12 | 0% |
| **合计** | **156** | **143** | **0** | **13** | **91.67%** |

## 4. 测试结论

**判定结果**：✅ 通过

**判定依据**：
- PRD-06 后台与小程序核心接口链路已完整执行：32 条 L1 全部通过。
- PRD-06 后台页面 L4 已完整执行：8 条 E2E 全部通过，覆盖反馈箱、注销申请、用户安全详情、未登录跳转。
- 后端测试套件通过：103 条测试，0 失败，0 错误，1 条既有种子数据测试跳过。
- 前端 `npm run build` 通过。
- 小程序真实前端不在本仓库，M-01 ~ M-12 保留为后续端侧联调项，不阻断当前仓库交付。

## 5. 失败用例明细

无。

## 6. 跳过用例明细

| 用例ID | 层级 | 优先级 | 场景描述 | 跳过原因 | 是否需要补测 |
|--------|------|--------|---------|---------|------------|
| M-01 ~ M-12 | 手动 | P0/P1/P2 | 小程序端交互与跨 PRD 联调 | 小程序前端不在本仓库，当前仅验证后端接口契约和后台前端 | 是 |

## 7. 各层级执行详情

### 7.1 L1 接口测试

```
执行命令: bash docs/测试文档/用户安全设置与搜索主链路-test-l1.sh
执行时间: 15:05
后台账号: peter/000000
小程序 Token: e2e-miniapp-token
结果: SUMMARY pass=32 fail=0
```

| 用例ID | 优先级 | 场景 | HTTP 状态 | 结果 | 备注 |
|--------|-------|------|----------|------|------|
| F1-P0-01 | P0 | 我的页聚合 | 200 | ✅ | 返回用户摘要、认证占位、入口配置 |
| F1-P0-02 | P0 | 认证中心聚合 | 200 | ✅ | 返回三项认证状态 |
| F1-P0-03 | P0 | 设置页聚合 | 200 | ✅ | 返回手机号/微信绑定状态与设置入口 |
| F2-P0-02 | P0 | 保存隐私设置 | 200 | ✅ | `showDistance=false` 生效 |
| F2-P0-01 | P0 | 查询隐私设置 | 200 | ✅ | 回显保存后的隐私设置 |
| F2-P0-04 | P0 | 保存通知设置 | 200 | ✅ | `chat=false` 生效 |
| F2-P0-03 | P0 | 查询通知设置 | 200 | ✅ | 回显保存后的通知设置 |
| F3-P0-01 | P0 | 加入黑名单 | 200 | ✅ | 返回屏蔽关系 ID |
| F3-P0-02 | P0 | 查询黑名单列表 | 200 | ✅ | 返回目标用户 `种子邀请人` |
| F3-P0-04 | P0 | 加入不看 TA 动态 | 200 | ✅ | 返回屏蔽关系 ID |
| F3-P0-05 | P0 | 查询不看 TA 动态列表 | 200 | ✅ | 返回目标用户 `普通已注册` |
| F4-P0-01 | P0 | 新增个人关键词 | 200 | ✅ | 返回关键词 ID |
| F4-P0-02 | P0 | 查询个人关键词 | 200 | ✅ | 返回 `E2E关键词` |
| F5-P0-01 | P0 | 提交反馈 | 200 | ✅ | 返回反馈 ID |
| F5-P0-02 | P0 | 后台反馈列表 | 200 | ✅ | 返回反馈记录 |
| F5-P0-03 | P0 | 后台反馈详情 | 200 | ✅ | 返回内容、联系方式、状态 |
| F5-P0-04 | P0 | 后台处理反馈 | 200 | ✅ | 状态更新成功 |
| F6-P0-01 | P0 | 查询注销状态 | 200 | ✅ | 初始 `NONE` |
| F6-P0-02 | P0 | 提交注销申请 | 200 | ✅ | 创建后悔期申请 |
| F6-P0-03 | P0 | 撤销注销申请 | 200 | ✅ | 状态转 `REVOKED` |
| F6-P0-05 | P0 | 后台注销列表 | 200 | ✅ | 返回注销申请 |
| F6-P0-06 | P0 | 后台注销详情 | 200 | ✅ | 返回后悔期与撤销时间 |
| F6-P0-07 | P0 | 后台注销备注 | 200 | ✅ | 备注保存成功 |
| F7-P0-01 | P0 | 搜索全部类型 | 200 | ✅ | 返回用户结果 `peter` |
| F7-P1-03 | P1 | 动态搜索占位 | 200 | ✅ | 返回空列表与空态文案 |
| F7-P3-01 | P3 | 未登录搜索拦截 | 401 | ✅ | 返回 `未登录` |
| F8-P0-01 | P0 | 后台用户安全摘要 | 200 | ✅ | 返回屏蔽、关键词、反馈、搜索、注销摘要 |
| F8-P0-02 | P0 | 后台用户隐私 | 200 | ✅ | 返回隐私设置 |
| F8-P0-03 | P0 | 后台用户通知 | 200 | ✅ | 返回通知设置 |
| F8-P0-04 | P0 | 后台用户黑名单 | 200 | ✅ | 返回黑名单记录 |
| F8-P0-05 | P0 | 后台用户动态屏蔽 | 200 | ✅ | 返回动态屏蔽记录 |
| F8-P0-06 | P0 | 后台用户关键词 | 200 | ✅ | 返回关键词记录 |

### 7.2 L2/L3 后端测试套件

```
执行命令: cd backend && JAVA_HOME=/Users/peter/Library/Java/JavaVirtualMachines/openjdk-22/Contents/Home mvn test
执行时间: 14:58
```

| 范围 | 结果 | 备注 |
|------|------|------|
| Controller + Service 现有套件 | ✅ | Tests run: 103, Failures: 0, Errors: 0, Skipped: 1 |

### 7.3 前端构建

```
执行命令: cd frontend && npm run build
执行时间: 14:58
```

| 范围 | 结果 | 备注 |
|------|------|------|
| TypeScript + Vite build | ✅ | 构建成功 |

### 7.4 L4 E2E 浏览器测试

```
执行命令: cd frontend/e2e-tests && BASE_URL=http://localhost:5173 API_URL=http://localhost:8080 npx playwright test tests/user-security.spec.ts --project=chromium --reporter=list
执行时间: 14:57
登录账号: peter/000000
结果: 8 passed
```

| 用例ID | 优先级 | 场景 | 耗时 | 结果 | 备注 |
|--------|-------|------|------|------|------|
| L4-US-01 | P0 | 反馈箱页面可加载 | 1.4s | ✅ | 表格、筛选、查询按钮可见 |
| L4-US-02 | P0 | 反馈箱筛选与重置交互 | 1.2s | ✅ | 用户 ID/类型筛选可输入并重置 |
| L4-US-07 | P0 | 反馈详情处理流程 | 2.1s | ✅ | 详情弹窗打开、处理备注保存 |
| L4-US-03 | P0 | 注销申请页面可加载 | 1.2s | ✅ | 表格、筛选、查询按钮可见 |
| L4-US-04 | P0 | 注销申请筛选与重置交互 | 1.2s | ✅ | 用户 ID 筛选可输入并重置 |
| L4-US-08 | P0 | 注销详情备注流程 | 1.5s | ✅ | 详情弹窗打开、后台备注保存 |
| L4-US-05 | P0 | 用户管理安全详情入口可打开 | 1.5s | ✅ | 安全摘要、黑名单、动态屏蔽、关键词可见 |
| L4-US-06 | P3 | 未登录访问反馈箱应跳转登录页 | 1.1s | ✅ | 显示登录页 |

### 7.5 前端手动测试

| 用例ID | 优先级 | 操作步骤 | 期望结果 | 实际结果 | 状态 |
|--------|-------|---------|---------|---------|------|
| M-01 ~ M-12 | P0/P1/P2 | 小程序端与跨模块联调 | 与 testcase 一致 | 小程序前端不在本仓库，未执行 | ⏭️ |

## 8. 遗留问题

| 编号 | 问题描述 | 影响范围 | 优先级 | 预计处理时间 | 负责人 |
|------|---------|---------|--------|------------|--------|
| 1 | 小程序真实前端不在本仓库，M-01 ~ M-12 需端侧联调补测 | 小程序真实交互体验 | 中 | 小程序端接入接口后 | C |
| 2 | PRD-06 专项 MockMvc/JUnit 测试类尚未从 testcase 派生，当前后端自动化通过接口链路和现有套件覆盖 | 自动化回归粒度 | 中 | 后续测试自动化增强 | C |

## 9. 测试建议

- 将本次用于补齐环境的 PRD-06 权限码纳入正式菜单/权限种子 SQL，避免新环境登录后无权限。
- `docs/测试文档/用户安全设置与搜索主链路-test-l1.sh` 已固化，可作为后续一键回归脚本。
- 后续小程序端接入后，补测 M-01 ~ M-12 的真实页面交互与 PRD-03/04/05/08 联动效果。
