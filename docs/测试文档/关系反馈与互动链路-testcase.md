# 关系反馈与互动链路测试用例

> 创建日期：2026-07-21
> 流程阶段：阶段 4 测试用例
> 测试模式：完整模式
> 当前实现范围：公共关系数据模型、管理后台后端、管理后台前端
> 延期范围：依据 `C02-12`，移动端接口和移动端页面不实现、不联调、不执行、不计入本轮验收
> 技术方案：`docs/技术方案/2026-07-16-关系反馈与互动链路-tcdesign.md`
> 冻结记录：`docs/需求文档/需求冻结记录/关系反馈与互动链路-prd-freeze.md`

## 1. 测试范围与策略

### 1.1 范围

1. 7 张关系事实表、`app_user` 与 `app_user_unlock_record` 增量字段、权限种子和历史状态迁移。
2. 喜欢、访客、匹配、匹配来源、匹配弹窗、失效、匿名化和解锁关联的公共领域规则。
3. App 用户列表关系准入/VIP 筛选、独立统计接口、关系摘要和四类明细接口。
4. 管理后台卡片入口、摘要、Tab 懒加载、10/20/50 分页、权限裁剪、匿名态和异常态。
5. 中文 DDL 注释、Java 枚举说明和前端中文映射的一致性。

### 1.2 测试策略决策

| 维度 | 评估 | 得分 |
|------|------|------|
| A 接口数量 | 修改列表并新增 5 个关系查询接口 | 2 |
| B 状态流转 | 喜欢、访客、匹配、来源、弹窗、解锁多状态联动 | 2 |
| C 规则复杂度 | 30 分钟归并、7 天精确统计、多来源单一匹配 | 2 |
| D 数据关联 | 关系表、用户、资产、审计等 4 张以上表 | 2 |
| E 既有代码影响 | 修改 App 用户核心列表与统一状态变化点 | 2 |
| F 安全变化 | 新权限、资产二次权限和失败关闭审计 | 1 |
| **后端合计** | **11 分** | **L1 + L2 + L3** |

| 前端条件 | 是否命中 | 说明 |
|----------|----------|------|
| G 多角色权限差异 | 是 | 关系权限、资产权限影响入口、筛选和字段 |
| H 复杂交互 | 是 | 弹窗、Tab 懒加载、筛选与分页联动 |
| I 多区域联动 | 是 | 用户卡片、模块补充弹窗和用户详情跳转 |
| J 核心业务页面 | 是 | App 用户管理为高频后台页面 |

**最终策略：L1 + L2 + L3 + L4 + 手工验收。**

## 2. 测试数据准备

| 数据编号 | 数据要求 | 用途 | 准备方式 | 幂等/清理 |
|----------|----------|------|----------|-----------|
| `TD-02-01` | 2 个账号正常且核心准入开放的 App 用户 | 喜欢、访客、匹配正常链路 | 测试 fixture 查询或构造 | 使用唯一业务号，可重复清理 |
| `TD-02-02` | 1 个准入未开放用户、1 个冻结用户 | 准入投影与筛选 | 测试 fixture | 事务回滚 |
| `TD-02-03` | 1 个注销用户，分别覆盖有/无 anonymousNo | 匿名化失败安全 | 测试 fixture | 事务回滚 |
| `TD-02-04` | 有关系查看权限但无资产权限的管理员 | 字段裁剪与筛选防推断 | 现有 RBAC fixture | 只读 |
| `TD-02-05` | 同时具备关系与资产权限的管理员 | 完整摘要/解锁记录 | 现有 RBAC fixture | 只读 |
| `TD-02-06` | 只具备 App 用户列表权限的管理员 | 越权验证 | 现有 RBAC fixture | 只读 |
| `TD-02-07` | active/expired/refunded 及历史 ENABLED/DISABLED 解锁记录 | 迁移和资产展示 | 隔离测试库 fixture | 迁移测试库重建 |
| `TD-02-08` | 7 天边界内外的访客事件 | UV/PV 精确统计 | 固定时钟的 L3 fixture | 事务回滚 |

任何 L1/L4 用例不得硬编码真实 Token、账号或生产用户 ID；环境缺少角色 Token 或可用数据时标记跳过，并在测试报告写明补测条件。

## 3. 需求到测试追溯

| 需求 ID | 技术方案 | 测试用例 ID | 层级 |
|---------|----------|-------------|------|
| `FRZ-02-01` | 数据模型、永久留存、状态约束 | `DB-P0-01~04`、`L3-P0-01~04`、`L3-P1-11` | DB/L3 |
| `FRZ-02-02` / `C02-03` | 单一有效匹配、多来源、新生命周期 | `DB-P1-05`、`L3-P0-05~08` | DB/L3 |
| `FRZ-02-03` | 摘要和四类明细 | `L1-P0-03~07`、`L2-P0-02~06`、`L4-P0-03~07` | L1/L2/L4 |
| `FRZ-02-04` / `C02-10` | 权限、脱敏、审计、匿名化 | `L1-P0-12~16`、`L3-P0-12~14`、`L4-P0-08~09` | L1/L3/L4 |
| `FRZ-02-05` | 列表筛选、模块补充入口 | `L1-P0-01~02`、`L4-P0-01~03` | L1/L4 |
| `FRZ-02-06` / `C02-15` | 10/20/50 分页、倒序、重置页码 | `L1-P1-08~10`、`L4-P0-06` | L1/L4 |
| `FRZ-02-07` / `C02-12` | 移动端延期 | `DFD-02-01~03` | 文档核查 |
| `FRZ-02-08` | PRD-03/04/05 边界 | `L3-P1-15`、`M-P1-05` | L3/手工 |
| `C02-01` | 一期无隐藏访问 | `DB-P0-04`、`L1-P1-11`、`L4-P1-11` | DB/L1/L4 |
| `C02-04` / `C02-09` | 两步解锁、永久清晰、失效不退款 | `DB-P1-07`、`L3-P0-09~11`、`L1-P0-07` | DB/L3/L1 |
| `C02-05` | 失效对象前台移除、后台保留 | `L3-P0-11`、`L1-P1-06` | L3/L1 |
| `C02-06` | 会话入口边界 | `L3-P1-15` | L3 |
| `C02-07` | 双方独立弹窗状态 | `DB-P1-05`、`L3-P0-07~08` | DB/L3 |
| `C02-13` | 30 分钟归并、实际 PV、去重 UV | `L3-P0-01~04`、`L1-P1-03` | L3/L1 |
| `C02-14` | 普通用户 10 条/会员 20 条 | `DFD-02-03` | 延期 |
| `TC-CFM-02-01~09` | 技术确认项 | `DB-P0-01~04`、`L1-P0-01~16`、`L3-P0-01~15`、`L4-P0-01~10` | 全层级 |

## 4. 数据库与迁移测试

| 用例 ID | 场景 | 验证步骤 | 预期结果 | 优先级 |
|---------|------|----------|----------|--------|
| `DB-P0-01` | 新增关系表与字段 | 在空白兼容 schema 执行生产迁移，查询 information_schema | 创建 7 张关系表；两张既有表字段齐全；引擎 InnoDB、字符集 utf8mb4 | P0 |
| `DB-P0-02` | 所有字段有中文注释 | 查询 TABLE_COMMENT/COLUMN_COMMENT 并做静态校验 | 每张新表有中文表注释；全部列含非空中文注释；通用字段也不例外 | P0 |
| `DB-P0-03` | 枚举中文释义完整 | 对状态、来源、动作、原因、业务类型字段读取 COLUMN_COMMENT | 注释逐项包含全部 code 与中文含义，默认值包含在枚举清单中；与 Java 枚举一致 | P0 |
| `DB-P0-04` | 基线与禁止项 | 检查审计字段、外键、隐藏访问字段和删除能力 | 每表包含 BaseEntity 字段；无数据库外键；无 hiddenVisit/windowStartedAt；无关系物理删除脚本 | P0 |
| `DB-P1-05` | 唯一约束 | 并发插入 active like、active match、重复 event/source/popup | 每个约束只保留一条合法记录，重复幂等键被唯一约束拦截 | P1 |
| `DB-P1-06` | 索引覆盖 | 查询 information_schema 并对摘要/分页 SQL 执行 EXPLAIN | 设计索引均存在；主查询不全表扫描，不出现逐行关联 SQL | P1 |
| `DB-P1-07` | 解锁历史迁移 | 准备 ENABLED/DISABLED 和无法映射关系的历史数据后执行迁移 | 状态转 active/expired；生成 ULK-LEGACY 编号；无法映射者不获新关系权益 | P1 |
| `DB-P1-08` | 注销用户匿名号回填 | 准备已注销且无匿名号用户后迁移 | 生成唯一稳定 ANON 编号，输出日志不含手机号/openid | P1 |
| `DB-P1-09` | 迁移可重复执行 | 连续执行生产迁移两次 | 第二次成功；不重复建列、权限或索引，不修改已生成业务事实 | P1 |

## 5. 前后台闭环测试

| 用例 ID | 闭环 ID | 步骤 | 预期结果 | 层级 | 优先级 |
|---------|---------|------|----------|------|--------|
| `CL-P0-01` | `CL-02-01` 喜欢 | 构造 active/cancelled/invalid 喜欢，再查询用户摘要和喜欢 Tab | 摘要只计 active；明细保留历史、方向、来源和真实失效原因 | L3+L1 | P0 |
| `CL-P0-02` | `CL-02-02` 访客 | 写同用户对 30 分钟内外事件，再查摘要和访客 Tab | 事件实际 PV 正确；展示记录按 30 分钟归并；7 天 UV 去重 | L3+L1 | P0 |
| `CL-P0-03` | `CL-02-03/04` 匹配 | 追加多个来源、撤销单一/最后来源并查询匹配 Tab | 同时仅一个有效匹配；来源可并存；最后来源撤销后失效 | L3+L1 | P0 |
| `CL-P0-04` | `CL-02-05` 解锁 | 构造有效、目标失效、已退款解锁记录并查询解锁 Tab | 财务状态不因目标失效自动改变；目标可用性和失效原因独立展示 | L3+L1 | P0 |
| `CL-P0-05` | `CL-02-06/07` 失效匿名 | 触发冻结/注销/拉黑/认证回退后查询 | 关系永久保留且失效；注销对象只返回 ANON，不泄露 PII | L3+L1 | P0 |
| `CL-P0-06` | `CL-02-08` 客服排查 | 打开卡片模块补充，依次浏览摘要和四 Tab | 数据来自真实 API；每次成功查询有审计；无假数据兜底 | L1+L4 | P0 |

## 6. L1 接口测试

| 用例 ID | 接口 | 场景/输入 | 预期结果 | 优先级 |
|---------|------|-----------|----------|--------|
| `L1-P0-01` | `GET /admin/users/app/list` | `relationshipAccess=OPEN/CLOSED/ABNORMAL,size=9` | 仅返回对应投影用户，每页最多 9 条，统计不混入列表计算 | P0 |
| `L1-P0-02` | 同上 | 有资产权限分别筛选 `vipStatus=active/inactive/expired` | 筛选命中正确；无资产行归 inactive；已过期按到期时间判 expired | P0 |
| `L1-P0-03` | `GET .../{userId}/relations/summary` | 存在用户及混合关系数据 | 返回精确 VO、active 计数、7 天 UV/PV、最后匹配时间并落审计 | P0 |
| `L1-P0-04` | `GET .../relations/likes` | page=1,size=10，方向/状态/来源筛选 | 按 likedTime,id 倒序；方向、状态、对方和 unlockNo 正确 | P0 |
| `L1-P0-05` | `GET .../relations/visits` | 默认时间及显式 7 天时间范围 | 按 lastVisitTime,id 倒序；PV、首次/末次时间和首次来源正确 | P0 |
| `L1-P0-06` | `GET .../relations/matches` | matched/invalid、多来源数据 | primarySource、activeSources、状态和真实失效原因正确 | P0 |
| `L1-P0-07` | `GET .../relations/unlocks` | 有效/退款/目标失效数据 | 解锁状态、目标可用性分离；目标失效不改财务事实 | P0 |
| `L1-P1-08` | 四个明细接口 | size 分别为 10/20/50 | 返回对应条数上限、total 正确、稳定倒序 | P1 |
| `L1-P1-09` | 四个明细接口 | size=9/11/100、page=0、时间倒置、非法状态/来源 | 返回业务参数错误 `code=20008`，不查询数据 | P1 |
| `L1-P1-10` | 摘要/四明细 | userId 不存在 | 返回 `code=20009`，不写成功查看审计 | P1 |
| `L1-P1-11` | 列表与关系接口 | 传旧 `hideVisitRecord` 或检查响应 | 新契约无隐藏访问筛选、字段、统计或状态分支 | P1 |
| `L1-P0-12` | 摘要/四明细 | 无 Token | HTTP 401 | P0 |
| `L1-P0-13` | 摘要/四明细 | 有 Token、无 `user:app:relation:view` | HTTP 403，响应不含关系数据，且无越权成功日志 | P0 |
| `L1-P0-14` | 列表 | 无资产权限传 `vipStatus` | HTTP 403，防止通过结果数量推断 VIP | P0 |
| `L1-P0-15` | 摘要/解锁 | 有关系权限、无资产权限 | vipVisible=false、vipStatus=null；coinCost 等资产字段为空、assetVisible=false | P0 |
| `L1-P0-16` | 摘要/四明细 | 模拟审计写入失败 | 返回 `code=20010`，不返回敏感查询结果 | P0 |
| `L1-P1-17` | `GET /admin/users/app/stats` | 列表切换分页/关系筛选前后调用 | 统计仍由独立接口返回，不随列表 size=9 被截断 | P1 |

## 7. L2 Controller 测试

| 用例 ID | Controller | 场景 | 预期结果 | 优先级 |
|---------|------------|------|----------|--------|
| `L2-P0-01` | `AppUserController.list` | 绑定 relationshipAccess/vipStatus | 精确返回 `R<Page<AppUserListVO>>`，参数传给 Service | P0 |
| `L2-P0-02` | `AppUserController.relationSummary` | 合法 userId | 路由、参数与 `R<AppUserRelationSummaryVO>` 正确 | P0 |
| `L2-P0-03` | `AppUserController.relationLikes` | 合法分页请求 | 精确返回喜欢分页类型 | P0 |
| `L2-P0-04` | `AppUserController.relationVisits` | 合法分页请求 | 精确返回访客分页类型 | P0 |
| `L2-P0-05` | `AppUserController.relationMatches` | 合法分页请求 | 精确返回匹配分页类型 | P0 |
| `L2-P0-06` | `AppUserController.relationUnlocks` | 合法分页请求 | 精确返回解锁分页类型 | P0 |
| `L2-P0-07` | 五个关系查询方法 | 反射检查权限注解 | 均声明 `@RequirePermission("user:app:relation:view")` | P0 |
| `L2-P1-08` | 四个分页方法 | 非法 size、page、时间区间 | 参数错误交由统一异常处理，不返回泛型或假成功 | P1 |

## 8. L3 Service 测试

| 用例 ID | Service/规则 | 场景 | 预期结果 | 优先级 |
|---------|--------------|------|----------|--------|
| `L3-P0-01` | `RelationDomainService.recordVisit` | 首次访问 | 生成 event、visit、cursor；PV=1；展示来源为首次事件来源 | P0 |
| `L3-P0-02` | 同上 | 29:59 内新 eventNo | 不新建 visit；PV+1；更新 lastVisitTime；不覆盖展示来源 | P0 |
| `L3-P0-03` | 同上 | 距最近访问恰好 30:00 或更久 | 新建展示记录；cursor 指向新记录 | P0 |
| `L3-P0-04` | 同上/摘要 | 重复 eventNo、7 天边界、多访客、关系后续失效 | 重复事件不增 PV；PV 按实际事件数；UV 按访客去重且精确截取 7 天；关系后续失效不改写已发生的历史 UV/PV | P0 |
| `L3-P0-05` | 匹配来源 | 首个合法来源 | 规范 low/high，创建一条 active match、一条 source、双方各一条 pending popup | P0 |
| `L3-P0-06` | 匹配来源 | 同生命周期追加第二来源及重复来源事件 | 不新建 match/popup；新增来源一次；重复事件幂等 | P0 |
| `L3-P0-07` | 匹配来源 | 撤销一个来源但仍有 active 来源；撤销最后来源 | 前者保持 matched，后者 invalid 并取消双方 pending popup | P0 |
| `L3-P0-08` | 匹配弹窗 | delivered、主动 read、渲染失败、重复回执、新生命周期 | delivered 不算 read；主动动作已读；失败仍 pending；回执幂等；新生命周期重新生成 | P0 |
| `L3-P0-09` | 解锁状态 | active 单条解锁遇会员到期 | 单条解锁保持 active，不重新模糊 | P0 |
| `L3-P0-10` | 解锁状态 | 特批退款完成 | 状态 refunded、marker=NULL、refundNo 保留 | P0 |
| `L3-P0-11` | `RelationLifecycleService` | 取消喜欢、拉黑、冻结、注销、封禁、认证回退 | 按原因失效；解除条件不恢复旧关系；事实不删除；目标失效不自动退款 | P0 |
| `L3-P0-12` | 匿名化组装 | 注销对象有/无 anonymousNo | 有编号只返回 ANON；无编号返回“匿名用户”且绝不回退 PII | P0 |
| `L3-P0-13` | `RelationAuditService` | 审计成功与失败 | 成功日志无 PII；失败抛 20010 并阻断响应 | P0 |
| `L3-P0-14` | 资产权限裁剪 | 有/无 commercial 权限 | VIP、coinCost、资产跳转严格按权限返回 | P0 |
| `L3-P1-15` | 边界契约 | 会话/发送/主页/资产能力 | 公共关系域不实现 PRD-03 会话发送、PRD-04 扣币编排、PRD-05 主页主体 | P1 |
| `L3-P1-16` | 关系准入投影 | 正常开放、正常未开放、冻结/注销 | 分别返回 OPEN/CLOSED/ABNORMAL 及中文展示映射所需 code | P1 |
| `L3-P1-17` | 管理后台组装 | 50 条明细含重复对方用户/解锁号 | 使用固定次数批量查询，无循环逐条 DAO 调用 | P1 |
| `L3-P1-18` | 枚举契约 | 遍历状态、来源、原因、动作枚举 | code 唯一、中文说明非空，并与 DDL/前端映射一致 | P1 |

## 9. L4 管理后台页面测试

| 用例 ID | 页面/流程 | 角色与操作 | 预期结果 | 优先级 |
|---------|-----------|------------|----------|--------|
| `L4-P0-01` | App 用户列表 | 有列表权限进入页面 | 首屏真实列表每页 9 张卡片；统计由独立 stats 请求加载 | P0 |
| `L4-P0-02` | 筛选区 | 切换关系准入/VIP并搜索、重置 | 请求传正式 code，筛选和重置回第 1 页；无隐藏访问条件 | P0 |
| `L4-P0-03` | 模块补充弹窗 | 点击用户卡片“模块补充” | 同时请求摘要和喜欢第一页；不请求其他三个 Tab | P0 |
| `L4-P0-04` | Tab 懒加载 | 依次首次切换访客/匹配/解锁，再来回切换 | 每个 Tab 首次进入才请求；已加载数据不被其他 Tab 清空 | P0 |
| `L4-P0-05` | 请求竞态 | 快速切换 Tab、筛选和分页并让旧请求晚返回 | 旧响应不覆盖当前视图；loading/error/data 各 Tab 独立 | P0 |
| `L4-P0-06` | 明细分页 | 切换 10/20/50、Tab、筛选 | 页容量正确；每次切换回第 1 页；无批量选择 | P0 |
| `L4-P0-07` | 空态与失败态 | Mock 空数据、摘要失败、单 Tab 失败 | 空态显示 0/暂无数据；摘要显示 `-` 和重试；单 Tab 失败不影响其他数据；无 DEMO_USERS 回退 | P0 |
| `L4-P0-08` | 权限差异 | 关系无权限、关系有权限但资产无权限、全权限三类 Token | 关系 Tab/按钮和 VIP 筛选按权限显示；资产字段及跳转被裁剪；后端 403 有明确反馈 | P0 |
| `L4-P0-09` | 注销匿名态 | 打开包含注销对方的各 Tab | 只显示 ANON/匿名用户，不显示昵称、手机号、头像且不可跳用户详情 | P0 |
| `L4-P0-10` | 中文枚举展示 | 展示所有状态、来源、原因和动作 code | 页面只显示确认的中文文案，不直接显示英文 code | P0 |
| `L4-P1-11` | 禁止能力 | 检查列表、弹窗、菜单和请求 | 无隐藏访问、导出、人工补关系、恢复、改状态或独立关系菜单 | P1 |
| `L4-P1-12` | Demo 视觉验收 | 桌面常用视口截图对比合并 Demo | 字段、按钮、弹窗、Tab、分页、状态和反馈还原度不低于 95% | P1 |

## 10. 手工验收与非功能用例

| 用例 ID | 场景 | 验收方法 | 预期结果 | 优先级 |
|---------|------|----------|----------|--------|
| `M-P0-01` | DDL 中文注释抽查 | 在数据库客户端执行 SHOW CREATE TABLE/SHOW FULL COLUMNS | 表和每列有中文注释；枚举列能直接读懂所有 code 含义 | P0 |
| `M-P0-02` | 审计安全 | 查看五类成功日志和失败日志 | action 正确；afterValue 无昵称、手机号、头像、对方清单等 PII | P0 |
| `M-P1-03` | 性能 | 9 条用户列表、摘要、50 条明细压测并采集 SQL/EXPLAIN | 摘要与明细测试环境 P95<=800ms；无 N+1；不设固定 SQL 总数上限 | P1 |
| `M-P1-04` | 数据一致性巡检 | 执行重复 active、状态/marker、匹配无来源、popup 数量巡检 SQL | 不存在违规数据；异常有可定位业务编号 | P1 |
| `M-P1-05` | 跨模块边界 | 代码依赖和路由清单审查 | admin/miniapp 不互相 import；本轮无 miniapp relation 路由和移动端页面改动 | P1 |
| `M-P1-06` | 管理后台 95% 评分 | 按技术方案 9.2 八项权重逐项计分并留截图 | 总分>=95%；低于目标先修复后复测，不直接判通过 | P1 |

## 11. 延期与不执行项

| 用例 ID | 内容 | 本轮处理 | 补测条件 |
|---------|------|----------|----------|
| `DFD-02-01` | `/miniapp/relation/**` 喜欢、访客、匹配、弹窗接口 | `C02-12` 延期，不生成 Controller、cURL 或自动化测试 | 用户明确启动移动端接口开发 |
| `DFD-02-02` | 移动端喜欢/访客/相互喜欢/匹配弹窗/解锁页面 | 不编码、不执行 L4、不计 95% | 补齐蓝湖稿并重新通过移动端设计门禁 |
| `DFD-02-03` | 普通用户 10 条模糊喜欢、会员 20 条加载与两步解锁 | 只保留需求和 handoff 契约，不通过假接口验收 | PRD-04 quote/confirm 与移动端关系接口同步立项 |
| `DFD-02-04` | 风控封禁上游事件接入 | 上游若无正式事件，只验证公共失效方法，不虚构调用点 | 上游提供正式事件/Service 契约 |
| `DFD-02-05` | 真实环境多角色 L1/L4 | 未提供对应 Token 时允许跳过 | 提供合法测试环境和角色 Token |

## 12. 阶段 4 门禁核对

| 门禁项 | 结果 | 证据 |
|--------|------|------|
| 每个本轮 P0/P1 需求至少一个用例 | 通过 | 第 3 节追溯矩阵 |
| 每个本轮 P0/P1 闭环至少一个闭环测试 | 通过 | `CL-P0-01~06` |
| 核心接口覆盖正常、异常、权限 | 通过 | `L1-P0-01~16`、`L1-P1-08~11` |
| 页面覆盖主流程、异常态、权限差异 | 通过 | `L4-P0-01~10` |
| 表与枚举中文注释进入 P0 门禁 | 通过 | `DB-P0-02~03`、`M-P0-01` |
| 管理后台 Demo 还原与证据 | 已定义 | `L4-P1-12`、`M-P1-06`，实现后执行 |
| 未编造移动端接口或测试 | 通过 | 第 11 节明确延期 |

**阶段结论：本轮全部 P0/P1 需求及管理后台闭环已有测试承接，可以进入阶段 5 实现。**
