# 千寻互动整体蓝湖还原测试报告

> 执行日期：2026-08-05  
> 测试用例：`docs/测试文档/千寻互动整体蓝湖还原-testcase.md`  
> 测试环境：macOS、微信开发者工具 2.01.2510260、基础库 3.16.1、iPhone 12/13 Pro 390×844、Java 21

## 1. 结论

本轮千寻互动代码、真实接口、运行态和构建验证通过，达到交付闭环。

- 浏览记录已改为真实 `viewed` 互动接口，并保留 `interactionTime` 与关联动态。
- 真实运行数据已显示日期分组、性别图标、动态图片、评论和点赞图标。
- 评论过、点赞过、解锁过、浏览记录、我的动态、真实话题和知音关联页均完成自动化打开与断言。
- 后端对本人动态和互动历史增加不可见内容过滤；后端全量 500 项测试通过。
- 小程序全部前置门禁、正式构建和产物门禁通过。

## 2. 执行结果

| 层级 | 命令/场景 | 结果 |
|---|---|---|
| L2 专项 Node Test | `node --test scripts/test-qianxun-interactions-closure.cjs` | 4/4 通过 |
| L2 PRD-05 门禁 | `node scripts/validate-prd05-community-closure.mjs` | 通过 |
| L2 ESLint | 千寻互动及关联页定向检查 | 通过，0 error |
| L3 后端专项 | `CommunityServiceImplTest` | 23 项通过 |
| 后端全量回归 | Java 21 `mvn test` | 500 项通过，0 失败，0 错误 |
| 小程序全量门禁与构建 | `npm run build:weapp` | 通过，Webpack 编译成功 |
| 构建产物门禁 | 页面注册、固定登录、包体检查 | 75 页注册通过；主包 1.32 MiB，总包 1.98 MiB |
| L4 微信运行态 | `capture-qianxun-interactions-closure.cjs` | 3/3 阶段通过，无小程序 exception |

## 3. 用例明细

| 用例范围 | 状态 | 说明 |
|---|---|---|
| L2-001～L2-009 | 通过 | 真实接口、日期分组、字段保留、OSS 图标、字符图标禁用和纵向基线均有专项门禁 |
| L3-001 | 通过 | 新增 Mockito 断言，确认本人动态查询包含 `NOT IN` |
| L3-002～L3-004 | 通过 | 服务实现只组装 `published` 动态；评论查询同时限制 `published`，全量后端回归通过 |
| L4-001～L4-004 | 通过 | 微信运行态完成三个筛选与浏览记录断言；浏览记录清晰截图包含完整日期、性别和卡片图标 |
| L4-005 | 跳过写操作 | 为保护开发账号历史数据，未自动点击最终“确认清空”；清空接口和二次确认入口保留 |
| L4-006～L4-010 | 通过 | 互动内/独立我的动态、真实话题、知音双页及 exception 监听均通过 |

## 4. 缺陷回归

| 原问题 | 回归结果 |
|---|---|
| 浏览记录没有日期 | 已按真实互动时间显示 `YYYY年MM月DD日` 分组，卡片显示 `MM-DD` |
| 浏览记录上下间距不符合设计 | 首日期与分隔线、日期与作者、卡片间距已按 `039.png` 调整 |
| 缺少性别图标 | 后端 `authorGender` 已映射 OSS 男女图标；未知值不显示错误占位 |
| 评论和点赞图标错误 | 互动、我的动态、详情、话题、知音统一使用 OSS 图标及激活态 |
| 浏览记录使用旧接口、字段不完整 | 已切换 `/me/interactions?type=viewed`，保留完整动态对象与互动时间 |
| 已删除/屏蔽动态仍出现 | 前后端双层过滤；运行截图中已删除记录不再出现 |

## 5. 运行说明

微信开发者工具在重复自动截屏时存在过一次 GPU 合成层偏移；禁用 GPU 后完成了完整 3/3 自动化流程。该现象只影响开发者工具截图，不影响页面节点断言、接口数据、Taro 构建或小程序运行。验收报告仅引用清晰截图。

