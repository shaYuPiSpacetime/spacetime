# 消息、私信与通知中心测试用例

> 创建日期：2026-08-10  
> 流程阶段：阶段 4 测试用例  
> 测试模式：完整模式  
> 技术方案：`docs/技术方案/2026-07-31-消息、私信与通知中心-tcdesign.md`  
> 对接契约：`docs/技术方案/2026-07-31-消息、私信与通知中心-mobile-api-handoff.md`

## 1. 测试范围与策略

本轮覆盖 PRD-03 的 14 张新增表、消息正文留存、TIM 单一映射、Inbox/Outbox 可靠任务、会话与悄悄话
状态机、移动端后端接口、小程序消息中心、管理后台元数据接口及敏感正文访问边界。小程序 Mock 仅用于
可重复的页面状态和交互回归，不能替代腾讯云 TIM 生产凭据与真实双账号联调。消息模块不再依赖应用层 KMS。

| 维度 | 评估 | 得分 |
|------|------|------|
| 接口数量 | 移动端、回调和管理后台多组接口 | 2 |
| 状态流转 | 会话、悄悄话、Inbox、Outbox、配置版本 | 2 |
| 数据关联 | 14 张新表及关系、资产、举报表 | 2 |
| 第三方依赖 | TIM、微信订阅消息 | 2 |
| 安全权限 | 正文白名单、案件复合授权、审计 | 2 |
| **后端合计** | **高复杂度** | **L1 + L2 + L3；外部环境具备后补真实联调** |

## 2. 数据库与迁移用例

| 用例 ID | 场景 | 验证步骤 | 预期结果 | 优先级 |
|---------|------|----------|----------|--------|
| `DB03-P0-01` | 14 张新增表 | 静态解析生产迁移并在隔离 MySQL 重复执行 | 14 张表齐全，InnoDB/utf8mb4，重复执行成功 | P0 |
| `DB03-P0-02` | 中文注释 | 检查表注释、所有字段注释和枚举注释 | 所有字段均有中文含义，枚举 code 与中文逐项对应 | P0 |
| `DB03-P0-03` | BaseEntity 字段 | 检查 14 张表公共字段 | 均包含 id/create_time/update_time/created_by/updated_by/deleted | P0 |
| `DB03-P0-04` | 正文明文主表 | 检查 `app_message_record` | 存在 nullable `content_text`、`content_cleared_at`；不存在正文密文/审核字段 | P0 |
| `DB03-P0-05` | 正文到期清理 | 执行留存任务 | 仅清空 `content_text` 并写清理时间；消息号、参与方、状态、TIM 映射不删除 | P0 |
| `DB03-P0-06` | TIM 映射单一事实源 | 检查消息与悄悄话表 | `tim_message_id/tim_msg_key` 仅在消息主表；悄悄话只存 request/reply message 外键 | P0 |
| `DB03-P0-07` | Inbox 临时载荷 | 检查 Inbox 字段及清理 SQL | `payload_json` 禁止聊天正文；存在 expires/cleared 时间；成功、dead 或到期均可清空载荷 | P0 |
| `DB03-P0-08` | Outbox 元数据 | 检查 `payload_json` 和服务写入 | 只保存协议与控制元数据；正文按 aggregate_id 从消息主表读取 | P0 |
| `DB03-P0-09` | 幂等唯一约束 | 并发插入重复业务号、客户端号、TIM key、Inbox/Outbox eventKey | 唯一约束拦截重复，仅保留一个事实 | P0 |
| `DB03-P1-10` | 举报扩展 | 检查 evidence 表及 community_report 增量 | 案件号、聊天目标、快照状态和受控明文证据完整；普通查询不返回正文 | P1 |
| `DB03-P0-11` | 私信已读状态 | 检查 `app_message_record` | 存在 `receiver_read_status/receiver_read_at`，枚举中文注释完整，已发送消息默认未读，失败或待投递为不适用 | P0 |
| `DB03-P0-12` | 私信未读索引 | 检查接收方未读联合索引 | 可按接收用户、已读状态、发送状态和会话过滤，避免消息首页未读统计全表扫描 | P0 |

## 3. 领域与可靠任务用例

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `L3-03-P0-01` | 普通私信 TIM 后回调归档 | 以 TIM key 幂等落一条明文消息，重复回调不重复更新业务活动 | L3 | P0 |
| `L3-03-P0-02` | 悄悄话发送 | 资格/报价/资产校验后创建 whisper、message 和 Outbox；三者同事务 | L3 | P0 |
| `L3-03-P0-03` | 悄悄话回复 | 回复投递成功后原子创建/复用匹配和会话，pending->replied，关联两条消息 | L3 | P0 |
| `L3-03-P0-04` | 回复幂等与并发 | 同请求复用原结果；异参冲突；并发只产生一个匹配、会话和回复消息 | L3 | P0 |
| `L3-03-P0-05` | 悄悄话到期 | 仅 sent+pending 且到期的记录 CAS 为 expired，不物理删除、不创建会话 | L3 | P0 |
| `L3-03-P0-06` | Inbox 原子认领 | 多实例仅一个处理者成功；超时 processing 可回收 | L3 | P0 |
| `L3-03-P0-07` | Inbox 载荷清理 | success 同事务清载荷；dead/过期任务清载荷；清后不伪造重试 | L3 | P0 |
| `L3-03-P0-08` | Outbox 投递 | 从消息主表读取正文调用 Provider；成功回写消息 TIM 映射；失败按退避重试 | L3 | P0 |
| `L3-03-P0-09` | TIM 映射冲突 | 同一 TIM key 指向不同本地消息时拒绝并告警，不覆盖原映射 | L3 | P0 |
| `L3-03-P0-10` | 会话失效 | 拉黑、冻结、注销、认证失效后不可发送，历史元数据和举报入口事实保留 | L3 | P0 |
| `L3-03-P1-11` | 系统/助手消息 | 重复上游事件不重复生成；批次已读只更新当前用户所属记录 | L3 | P1 |
| `L3-03-P0-12` | 正文访问边界 | 普通私信列表只返回短摘要；悄悄话参与方详情可读取留存期内申请正文；后台高敏正文仍要求案件授权和审计 | L3 | P0 |
| `L3-03-P0-13` | 统一重试策略 | Inbox、TIM Outbox、社区 Outbox 按 1m/5m/15m/1h/6h/12h/24h 退避，第 8 次进入 dead | L3 | P0 |
| `L3-03-P0-14` | TIM 本地映射审计 | 检出 sent/dead 与消息主表状态或 MsgKey 不一致，只告警不盲目重发 | L3 | P0 |
| `L3-03-P1-15` | 账号安全消息 | 冻结/注销状态提交后生成安全消息并失效关系；崩溃窗口由账号事实补齐 | L3 | P1 |
| `L3-03-P1-16` | 资产结果消息 | VIP/千寻币订单、退款和悄悄话补偿生成幂等资产消息，失败不回滚资产 | L3 | P1 |
| `L3-03-P1-17` | 邀请奖励消息 | 奖励成功/最终失败均生成幂等邀请消息，缺失事件按奖励日志补齐 | L3 | P1 |
| `L3-03-P1-18` | 社区与治理消息 | 社区 Outbox 只传用户可见摘要，后台原因不进入系统消息 | L3 | P1 |
| `L3-03-P1-19` | 平台公告 | 按文章 ID + 版本幂等懒加载，优先返回完整正文纯文本且不截断 | L3 | P1 |
| `L3-03-P0-26` | 移除消息 KMS 门禁 | production 未配置任何 `MESSAGE_TEXT_*` 参数时读取消息首页、生成系统/助手消息并冻结举报证据 | 不触发 KMS 错误；三类数据按受控明文规则写入并正常读取 | L3 | P0 |
| `L3-03-P0-20` | 配置版本回退 | 历史悄悄话缺少版本时，新会话统一使用 `MSG-CFG-INIT-001` | L3 | P0 |
| `L3-03-P0-21` | 私信已读确认 | 当前会话成员提交最后已读消息编号 | 仅将该会话内发给当前用户且不晚于游标的已发送消息改为已读，重复提交幂等 | L3 | P0 |
| `L3-03-P0-22` | 聚合消息未读 | 同时准备私信、悄悄话、助手和系统未读 | `messageUnreadCount` 等于四类未读之和；`platformUnreadCount` 继续表示悄悄话、助手和系统三类兼容口径 | L3 | P0 |
| `L3-03-P0-23` | App 用户消息四列表 | 私信、悄悄话、系统/助手、聊天举报均准备多状态数据 | 四类独立分页，固定每页 5 条；私信和悄悄话显示对方昵称与 U 编号，昵称缺失才回退脱敏编号；私信显示发送/已读/失败原因，空列表仍返回标准空页 | L3 | P0 |
| `L3-03-P0-24` | App 用户查看高敏 | 无权限、理由不足、正文已清理、读取异常、合法请求 | 进入正文读取前先写 pending 审计；正文不可用记 denied、读取异常记 error、合法请求记 allowed 并按条返回；响应禁止缓存 | L3 | P0 |
| `L3-03-P0-25` | 首版消息类型边界 | 普通文本、Unicode Emoji、TIM 图片/视频等富媒体 | 文本和 Unicode Emoji 按文本通过；非平台悄悄话自定义消息之外的富媒体统一拒绝且不落消息记录 | L3 | P0 |

## 4. Controller 与接口契约用例

| 用例 ID | 接口/控制器 | 场景 | 预期结果 | 优先级 |
|---------|-------------|------|----------|--------|
| `L2-03-P0-01` | `MiniappMessageController` | 反射检查公开路由和返回类型 | Controller 返回精确 `R<T>`；普通私信发送/历史仍由 TIM 承接，新增本地已读确认接口 | P0 |
| `L2-03-P0-02` | `MiniappImController` | 获取凭证 | 只返回 sdkAppId/imUserId/userSig/expireAt，不返回 SecretKey | P0 |
| `L2-03-P0-03` | 腾讯回调 Controller | 校验失败、重复回调、合法回调 | 使用腾讯协议响应，不使用通用 R；重复回调幂等 | P0 |
| `L2-03-P0-04` | 管理后台消息接口 | 检查权限注解 | 每个接口均声明技术方案中的 `@RequirePermission` | P0 |
| `L2-03-P0-05` | 登录后新增高敏权限 | Token 权限快照不含新权限，数据库当前角色已授权 | 权限拦截放行并把数据库最新权限同步到本次请求上下文，Service 不再二次误拒绝 | P0 |
| `L1-03-P0-01` | 会话列表/详情与已读确认 | 正常、失效、越权用户和重复已读 | 列表只返回业务状态与 TIM 映射；已读接口校验参与方和消息归属并返回剩余未读，不返回正文/历史 | P0 |
| `L1-03-P0-02` | 悄悄话列表/详情 | received/sent、pending/processed | 接收方双分组独立游标；发送方只查 pending；列表不返回 TIM 字段，详情返回留存期内正文，发送方看不到 readAt | P0 |
| `L1-03-P0-03` | 悄悄话发送/回复 | 正常、余额不足、报价过期、重复幂等键 | 错误码和状态机符合 handoff，失败不产生半状态 | P0 |
| `L1-03-P0-04` | 系统/助手列表和批次已读 | 归属正确、混入他人编号 | 只更新当前用户记录，返回平台未读汇总 | P0 |
| `L1-03-P0-05` | 后台元数据查询/导出 | 各角色与筛选 | 正文和摘要字段固定排除，无案件号临时提权 | P0 |
| `L1-03-P0-06` | 案件证据正文 | 无案件、非处理人、理由不足、合法请求 | 前三者拒绝且留审计；合法请求只返回一条冻结证据并 `no-store` | P0 |
| `L1-03-P0-07` | App 用户私信消息列表 | queued/sent/failed 与 unread/read 数据 | 查询消息事实而非会话；显示方向、对方脱敏标识、发送状态、接收方已读状态、失败原因和时间，不返回正文 | P0 |
| `L1-03-P0-08` | App 用户系统/助手消息列表 | 两类消息交错创建 | 单一列表按业务时间倒序合并分页，返回渠道和已读状态，不返回标题或正文 | P0 |
| `L1-03-P0-09` | 管理端消息互动 UI | 四类有数据、任一类无数据、翻页和查看高敏 | 四个列表始终展示且服务端强制各自 5 条/页，客户端传 20/100 也不能突破；空态明确；高敏弹窗关闭后仍停留在原用户和原分页 | P0 |

## 5. 管理后台 E2E 用例

| 用例 ID | 场景 | 验证 | 预期结果 | 优先级 |
|---------|------|------|----------|--------|
| `L4-03-P0-01` | 真实消息互动闭环 | 管理员登录，搜索真实用户，进入“心动 & 消息/消息互动”，检查摘要、四列表和高敏二次确认 | 四列表始终存在且首屏各 5 条；对方显示“昵称（U编号）”；顶部不展示高敏审计说明卡；普通列表无正文；合法高敏查看返回审计编号，关闭后外层弹窗保持 | P0 |
| `L4-03-P0-03` | 无高敏权限交互 | 移除当前管理员 `message:sensitive-content:view` 权限后进入消息互动 | 每条私信和悄悄话仍显示眼睛图标但不可点击，悬停提示“无查看权限” | P0 |
| `L4-03-P1-02` | 快速切换用户 | 延迟 A 用户私信请求，在响应前关闭并打开 B 用户；待 A 响应返回后复核 B 数据 | A 的旧响应不得覆盖 B 的摘要、列表、加载或错误状态 | P1 |

## 6. 外部联调与门禁

| 用例 ID | 前置条件 | 验证 | 当前无条件时处理 |
|---------|----------|------|------------------|
| `EXT03-01` | 腾讯云 TIM 测试应用与回调公网地址 | UserSig、账号同步、前回调拒绝、REST 投递、后回调和漫游历史 | 标记跳过，不得以本地 Provider 宣称通过 |
| `EXT03-03` | 隔离 MySQL 与 Redis | 迁移重复执行、并发 claim、真实事务和补偿 | 缺少环境时仅执行静态 SQL 与单元测试 |
| `EXT03-04` | 真实 TIM 账号导入与 UserSig | 从开发库选择两名满足准入的真实 App 用户，分别调用 `/miniapp/im/credentials` | 两个本地用户各生成唯一 `imUserId`，腾讯 `account_import` 成功，凭证仅返回短期 UserSig，不泄露 SecretKey |
| `EXT03-05` | 真实 TIM 悄悄话投递 | 用户 A 通过后端业务接口向用户 B 发送悄悄话，观察业务主表、Outbox、腾讯 REST 响应和后回调 | 业务消息从 `queued` 变为 `sent`，Outbox 变为 `sent`，TIM MsgId/MsgKey 只写消息主表，接收方列表可查询且不重复 |
| `EXT03-06` | 真实 TIM 普通私信与已读 | 已匹配的 A/B 使用 TIM 账号发送文本并触发前回调、后回调和已读回调 | 前回调按关系/准入/拉黑/女性保护放行；后回调幂等归档正文；已读仅推进接收方水位和对应消息，不污染其他会话 |
| `EXT03-07` | 回调鉴权与幂等 | 使用错误路径 Token、错误签名、过期 `RequestTime` 和重复合法回调请求公网入口 | 非法请求返回腾讯协议 `FAIL` 且不落业务事实；合法重复回调返回 `OK` 且数据库只保留一份事实 |
| `EXT03-08` | 运行配置门禁 | 分别检查本地真实配置实例和公网生产回调 | Provider、账号同步任务和回调服务均读取同一组 `TENCENT_IM_*`；公网入口不得返回 `callback is not configured` |
| `L3-03-P0-61` | Redis 报价时间往返 | 使用全局 Jackson 配置把含 `LocalDateTime` 的 `WhisperQuoteSnapshot` 写入并读回 | 时间格式保持 `yyyy-MM-dd HH:mm:ss` 且对象完全一致，创建接口不再误报 `30021` |
| `L2-03-P0-62` | 生产 TIM 配置透传门禁 | 检查应用绑定、生产环境示例、部署脚本和后端发布流水线 | 启用时校验必填项，完整写入容器 `runtime.env`；仓库不包含真实 SecretKey 或回调鉴权 Token |

## 7. 通过标准

1. P0 自动化用例全部通过，既有消息模块基线失败已单独说明或修复。
2. 14 张表、三项结构修正与 Java 实体/DAO/Mapper 保持一致。
3. 构建通过；消息首页和普通私信列表只返回 50 字摘要，悄悄话正文只在参与方详情接口返回。
4. TIM 外部资源缺失时只允许标记未联调，不得宣称生产闭环完成；KMS 不再是消息模块前置条件。
5. 测试执行结果写入 `docs/测试文档/消息私信通知中心-testreport.md`。
6. 本地数据库至少选择 2 个真实 `app_user`，构造并通过接口核验私信、悄悄话、系统/助手和举报数据；不得使用前端写死数据冒充流程通过。

## 8. 2026-08-12 消息首页与悄悄话契约增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `L2-03-P0-26` | 悄悄话公开路由 | 存在列表、详情、批量已读、单条隐藏、按分组全部隐藏、回复接口；全部返回精确 `R<T>` | L2 | P0 |
| `L2-03-P0-27` | 移动端 DTO 精简 | 首页和悄悄话 DTO 仅含文档字段，不返回 `allowed` 兼容字段及 TIM 原始消息编号 | L2 | P0 |
| `L3-03-P0-28` | 接收方双分组游标 | `received+pending` 只查未处理有效申请；`received+processed` 查 replied/expired/invalid；两组游标彼此不可混用 | L3 | P0 |
| `L3-03-P0-29` | 发送方列表范围 | `sent` 仅允许 `pending`；发送方不展示已回复、过期、失效和接收方隐藏字段 | L3 | P0 |
| `L3-03-P0-30` | 接收方逻辑隐藏 | 仅接收方可隐藏单条或一个分组；不修改业务状态、不写 BaseEntity.deleted，发送方和管理后台仍可查询 | L3 | P0 |
| `L3-03-P0-31` | 悄悄话详情动作 | 返回平台正文、正文可用性、对方摘要、生命周期时间及七个动作布尔值；申请我的和我申请的均可举报该申请正文，用户举报另由对方主页入口控制 | L3 | P0 |
| `L3-03-P0-32` | 来源场景绑定 | 预检只接受五个来源枚举；社区/反向申请必须传来源业务号；创建请求必须与报价中的来源完全一致 | L3 | P0 |
| `L3-03-P0-33` | 回复成功迁移 | 回复经 TIM 成功后才变为 replied 并创建/复用匹配与会话；响应不返回 TIM 原始编号 | L3 | P0 |
| `L3-03-P1-34` | 举报图片直传 | 当前用户可获取 report-evidence 票据；最多提交 3 张当前用户所有且真实存在的 OSS 图片，非法或过期票据拒绝 | L3 | P1 |
| `DB03-P0-13` | 悄悄话来源与隐藏字段 | 迁移含 `source_scene/source_biz_no/receiver_hidden_at/receiver_hide_type`，字段及枚举均有中文注释，升级 SQL 幂等 | DB | P0 |
| `DOC03-P0-01` | 文档代码一致性 | 消息首页和悄悄话每个入参/出参均有字段名、类型、中文含义、查询范围；文档不存在代码未实现字段或旧接口 | 静态 | P0 |

## 9. 2026-08-13 私信、助手与系统消息闭环增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `L3-03-P0-35` | 会话详情安全模式 | 有效会话返回 `normal`；失效会话返回 `safety_readonly`，可进入历史但不能发送或打开主页 | L3 | P0 |
| `L3-03-P0-36` | 私信会话举报能力 | 仅存在当前用户收到、发送成功且正文仍可取证的对方消息时返回 `canReportChat=true` 和 `private_chat` 举报上下文 | L3 | P0 |
| `L3-03-P0-37` | 女性保护详情 | 男方保护期内 `canSend=false`，返回 `female_protection` 和 `protectionUntil`；其他场景不误拦截 | L3 | P0 |
| `L3-03-P0-38` | TIM 会话已读回调 | `C2C.CallbackAfterMsgReport` 使用 Report/Peer 账号映射及 LastReadTime，只推进当前接收方在该会话的未读记录 | L3 | P0 |
| `L3-03-P0-39` | 助手展示快照 | 消息创建时固化 `cardType/actionText`，移动端分页逐条返回，不受后续模板变更影响 | L3 | P0 |
| `L3-03-P0-40` | 系统消息展示快照 | 固化 `contentFormat/actionText`；`rich_text` 经服务端白名单清洗后以受控明文保存并返回 | L3 | P0 |
| `L3-03-P0-41` | 已读水位迟到归档 | TIM/HTTP 已读先单调推进成员水位；迟到的已读范围内消息直接归档为 `read`，不得重新增加未读 | L3 | P0 |
| `L3-03-P0-42` | 终态会话迟到回调 | 关系失效后到达的 TIM 后回调仅归档并隔离，`readStatus=not_applicable`，不得更新会话列表投影 | L3 | P0 |
| `L3-03-P0-43` | 活跃会话成员完整性 | 活跃会话缺少接收方成员映射时后回调失败并等待重试，消息主表和会话投影均不写入 | L3 | P0 |
| `L3-03-P0-44` | 安全只读资料隔离 | 失效会话不查询或返回对方最新昵称头像；无 TIM 映射时降级 `canEnterConversation=false`，仍保留可用举报上下文 | L3 | P0 |
| `L3-03-P0-45` | 女性保护缺失截止时间 | 保护开启、女方未首发且截止时间为空时按保守策略禁止男方发送，详情与 TIM 前回调保持一致 | L3 | P0 |
| `DB03-P0-14` | 移动端展示契约迁移 | 模板、助手消息和系统消息新增字段均有中文注释和枚举中文说明；存量数据回填默认值 | DB | P0 |
| `DB03-P0-15` | 会话成员已读水位 | 成员表包含 `last_read_message_time/last_read_at` 及中文注释，升级 SQL 可重复执行且不覆盖已有展示快照 | DB | P0 |
| `DB03-P0-16` | TIM 定位与生命周期索引 | 会话表具备用户对+创建/失效时间索引，消息表具备会话+TIM 消息 ID 索引；基础和升级 SQL 均可重复执行 | DB | P0 |
| `L3-03-P0-46` | 迟到消息绑定旧生命周期 | 旧会话失效且双方重新匹配后收到旧消息发送回调 | 按消息发送时间绑定旧会话，仅归档并隔离，不更新新会话投影 | L3 | P0 |
| `L3-03-P0-47` | 迟到已读绑定旧生命周期 | 新生命周期建立后收到旧读水位回调 | 按 `LastReadTime` 只推进对应旧会话成员水位，不修改新会话 | L3 | P0 |
| `L3-03-P0-48` | TIM 定位已读补偿 | 不知道平台消息编号，只提交当前会话的 `timMessageId/timMsgKey` | 定位同一归档消息并推进单调水位；跨会话或定位冲突拒绝 | L3 | P0 |
| `L3-03-P0-49` | TIM 定位单条举报 | 只提交会话编号与对方消息 TIM 定位字段 | 服务端验证参与方和发送方向，冻结前5+目标+后2，举报主目标仍为会话 | L3 | P0 |
| `DOC03-P0-02` | 私信对接流程完整性 | 文档说明平台投影、TIM 漫游、TIM 已读回调、平台已读补偿和小程序本地失败 Outbox 的职责与调用顺序 | 静态 | P0 |
| `SCOPE03-P0-01` | 小程序前端交付边界 | 消息 SDK 只进入消息分包；主包只维护平台未读汇总；页面禁止硬编码真实业务数据和退役接口 | 静态 | P0 |

## 10. 2026-08-13 管理后台遗漏菜单闭环增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `L2-03-P0-50` | 消息记录高敏正文路由 | `POST /admin/message/records/{recordNo}/content-view` 使用 `message:sensitive-content:view`，返回精确 `R<AdminSensitiveMessageContentVO>` 且响应 `no-store` | L2 | P0 |
| `L3-03-P0-51` | 消息统计固定口径 | 统计不接收列表筛选；今日私信、待回应悄悄话、系统消息、关联举报按固定口径返回 | L3 | P0 |
| `L3-03-P0-52` | 用户信息正常展示 | 列表、详情和导出返回昵称与 `U{userId}` 所需字段，不再返回 `USR-********1234` 形式掩码 | L3 | P0 |
| `L3-03-P0-53` | 详情正文分流 | 私信/悄悄话普通详情不返回正文；系统/助手详情直接返回标题和正文，不存在应用层密钥材料 | L3 | P0 |
| `L3-03-P0-54` | 通用高敏查看 | 私信按消息编号返回单条；悄悄话按业务编号返回申请和回复；原因、权限、留存和审计失败均阻断正文 | L3 | P0 |
| `L3-03-P0-55` | 导出提交筛选 | 导出复用页面已提交筛选，最多 10000 条，字段白名单不包含任何类型正文 | L3 | P0 |
| `L3-03-P0-56` | 配置普通规则发布 | 女性保护、保护天数、有效期、冷却期生成新版本；备注 5-100 字；历史对象快照不回算 | L3 | P0 |
| `L3-03-P0-57` | 全局发送开关 | 使用独立整数版本 CAS、5-100 字原因，仅风控/超管可操作，成功即时影响新发送并写日志 | L3 | P0 |
| `L4-03-P0-26` | 消息通知记录页面 | 固定统计、筛选、分页、操作按钮“详情”、类型分流详情和按已提交条件导出闭环 | L4 | P0 |
| `L4-03-P0-27` | 消息配置页面 | 两个独立输入编辑悄悄话有效期/冷却期；普通规则与安全开关两条保存链路；日志抽屉闭环 | L4 | P0 |
| `L4-03-P0-28` | 一期不做项隐藏 | 页面 DOM 中不存在“用户通知设置页”和“后台通知偏好中心”卡片或入口 | L4 | P0 |
| `L4-03-P1-29` | 两页视觉与响应式 | 1440x900、1280x800 无横向溢出，按 Demo 已确认差异后首屏 >=97%、整体 >=95% | L4 | P1 |

## 11. 2026-08-13 管理后台异常修复增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `DB03-P0-17` | 消息、悄悄话与举报升级脚本 | 执行 PRD-03 消息迁移和 071 移动端契约迁移后，悄悄话来源字段、举报证据字段及系统/助手详情字段完整可查 | DB | P0 |
| `L3-03-P0-58` | peter 登录角色上下文 | 登录时将启用角色编码写入用户上下文，超级管理员可通过风险操作员校验 | L3 | P0 |
| `L3-03-P0-59` | 消息配置版本发布 | 旧活动版本显式退休并清空活动标记后再插入新版本，不触发活动版本唯一键冲突 | L3 | P0 |
| `L3-03-P0-60` | 消息记录中文导出 | 表头、记录类型、消息类型、系统分类和状态均导出中文，且复用页面筛选条件 | L3 | P0 |
| `L4-03-P0-30` | 消息记录自动筛选 | 关键词、枚举和日期变化后 300ms 自动查询，不展示“查询”按钮，重置与分页仍正确 | L4 | P0 |
| `L4-03-P0-31` | 列表与详情枚举中文化 | 消息记录列表和详情中的消息类型、状态均展示中文；私信/悄悄话按高敏权限查看正文，系统/助手直接明文展示 | L4 | P0 |
| `L4-03-P1-32` | 隐藏配置版本卡片 | 页面不展示无业务操作价值的“当前配置版本”统计卡片，版本号仍作为后端并发控制参数使用 | L4 | P1 |

## 12. 2026-08-14 小程序消息闭环增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `MA03-P0-01` | 消息接口契约 | 小程序覆盖凭证、首页、未读、会话、悄悄话、助手、系统消息和举报接口；禁用已退役动作 | 静态/L2 | P0 |
| `MA03-P0-02` | LiteChat 网关 | 固定 SDK 精确版本，支持凭证刷新、登录、文本发送/重发、漫游历史、事件订阅和已读 | 静态/L3 | P0 |
| `MA03-P0-03` | 分包与生命周期 | LiteChat 仅随消息分包加载；App 主包只轮询平台未读；前后台切换正确暂停与恢复 | 构建/L3 | P0 |
| `MA03-P0-04` | 私信闭环 | 首页进入私信列表和会话，文本发送立即回显；失败重发复用 `clientMsgId`；只读态禁止发送 | L4 | P0 |
| `MA03-P0-05` | 已读闭环 | 会话已读同时推进 TIM 和平台水位，清零会话及首页未读；重复提交幂等 | L3/L4 | P0 |
| `MA03-P0-06` | 举报闭环 | 会话拉黑成功后进入举报；单条消息携带稳定 TIM 定位字段；举报请求号稳定且原因使用后端八类枚举 | L3/L4 | P0 |
| `MA03-P0-07` | 悄悄话闭环 | 收到申请可回复并进入私信；主动申请先预检/报价再提交；60 字限制和幂等键有效 | L3/L4 | P0 |
| `MA03-P0-08` | 助手与系统消息 | 两个频道均展示分页内容、动作和已读状态；未知跳转类型安全降级 | L3/L4 | P0 |
| `MA03-P0-09` | 公开资料发起悄悄话 | 公开资料返回稳定 `userNo`，资料页、推荐、社区和心动入口均使用该编号预检 | L2/L3 | P0 |
| `MA03-P0-10` | 生产 IM 凭证失败 | 未配置 TIM 时返回 `30023` 且页面进入保守只读；配置完整时只返回短期 UserSig，不泄露 SecretKey | L2/L4 | P0 |
| `MA03-P0-11` | 微信正式构建 | 全部发布门禁、页面注册和包体检查通过；主包和总包不超过既定门禁 | 构建 | P0 |
| `MA03-P0-12` | H5 可重复 E2E | 固定测试登录态和 Mock Provider 下覆盖首页、私信发送、拉黑举报、单条举报、悄悄话收发、助手和系统频道 | L4 | P0 |

## 13. 2026-08-14 消息链路性能与正文来源复核增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `DB03-P0-18` | TIM 消息 ID 全局定位索引 | 基础建表和独立升级脚本均创建以 `tim_message_id` 为首列的独立索引；生产流水线同步并执行升级脚本 | DB/L3 | P0 |
| `MA03-P0-13` | 普通私信内容审核口径 | LiteChat 发送与重发显式设置 `excludedFromContentModeration=true`，与已确认的“不做本期发送前文本审核、举报后治理”决策一致 | 静态 | P0 |
| `MA03-P0-14` | 私信首屏并发加载 | 同一会话的 `useEffect` 与 `useDidShow` 并发加载合并为一个请求；详情状态变化不再次触发首屏全量加载，重新进入前台仍可刷新 | L3/L4 | P0 |
| `MA03-P0-15` | 悄悄话详情正文来源 | 页面直接展示详情接口的 `content/contentAvailable`，不翻页扫描 TIM 历史，不存在约 150 条消息后的静默正文丢失 | 静态/L4 | P0 |

## 14. 2026-08-16 历史会话对方 TIM 账号自愈增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `L3-03-P0-56` | 有效会话的对方无本地 TIM 映射 | 查询会话详情时自动创建并导入对方 TIM 账号，重新读取唯一映射并返回可用 `timConversationId`，不再误报“对方即时通信账号暂不可用” | L3 | P0 |
| `L3-03-P0-57` | 对方 TIM 映射处于待同步或失败状态 | 查询会话详情时触发账号同步并返回已同步映射；同步失败时才返回 `30023`，不得返回虚假的可进入、可发送状态 | L3 | P0 |
| `L3-03-P0-58` | 对方 TIM 映射已同步 | 查询会话详情直接复用稳定映射，不重复调用腾讯云账号导入接口 | L3 | P0 |
| `EXT03-09` | 生产历史会话账号自愈 | 使用生产有效会话访问双方凭证与会话详情，再由双方 LiteChat 登录、加载历史并互发文本 | 凭证与会话详情成功；双方账号存在且为 `synced`；文本收发、后回调归档和已读推进正常；缺少真实双账号登录态时标记跳过，不得以单元测试冒充通过 | L1/L4 | P0 |

## 15. 2026-08-16 TIM 应用切换后账号归属自愈增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `L3-03-P0-61` | 历史 `synced` 映射没有 SDKAppID 归属 | 签发凭证或检查会话对方账号时，使用原稳定 UserID 调用当前 TIM 应用 `account_import`，成功后记录当前 SDKAppID | L3 | P0 |
| `L3-03-P0-62` | 历史 `synced` 映射属于其他 SDKAppID | 不得复用虚假的已同步状态；必须把同一 UserID 重新导入当前 TIM 应用并更新归属 | L3 | P0 |
| `L3-03-P0-63` | `synced` 映射已属于当前 SDKAppID | 账号就绪检查直接复用本地映射，不重复调用腾讯云导入接口 | L3 | P0 |
| `L3-03-P0-64` | 有效会话详情检查已同步对方账号 | 会话详情始终委托 Provider 校验当前应用归属；Provider 本地命中时无远程请求，归属失效时自动导入 | L3 | P0 |
| `DB03-P0-19` | TIM 账号 SDKAppID 归属迁移 | 基础表与幂等升级脚本包含 `sdk_app_id`；生产流水线同步并执行升级脚本，不覆盖稳定 UserID | DB/L3 | P0 |

## 16. 2026-08-16 悄悄话来源场景契约修复增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `MA03-P0-17` | 预检与创建来源字段 | 小程序严格使用 handoff 定义的 `sourceScene/sourceBizNo`，预检报价与创建请求字段完全一致，不再发送 `scene/sourcePostNo` | 静态/L3 | P0 |
| `MA03-P0-18` | 资料、推荐和社区入口 | 用户主页使用 `profile`，推荐使用 `recommendation`，社区动态使用 `community_post` 并携带帖子业务编号；所有入口均传 `USR-` 稳定用户编号 | 静态/L4 | P0 |
| `MA03-P1-19` | 旧路由与技术错误降级 | 旧版未携带来源的主页路由按 `profile` 安全恢复；字段级校验异常转为用户可理解的返回提示，不展示 `sourceScene/targetUserNo` 等技术字段 | L3/L4 | P1 |
| `L2-03-P0-65` | 后端来源契约边界 | 合法 `sourceScene/sourceBizNo` 透传至 Service；缺少必填来源时返回 4001 且不进入业务层 | L2 | P0 |
| `MA03-P0-16` | 旧聊天页重发遇到 TIM 20003 | 识别 SDK 数字错误码或错误消息中的 `code:20003`，先请求会话详情触发对方账号自愈，再用原 `clientMsgId` 重发；恢复失败只展示业务提示，不泄露腾讯原始错误与 UserID | 静态/L3 | P0 |
| `EXT03-10` | 生产 TIM 20003 回归 | 当前用户凭证和会话对方账号均在生产 SDKAppID 下完成归属确认，LiteChat 双账号互发文本不再返回 UserID 无效或不存在 | L1/L4 | P0 |

## 17. 2026-08-16 TIM 单聊会话号格式修复增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `L3-03-P0-66` | 后端生成标准 TIM 单聊会话号 | 对方 UserID 为 `tu_peer_2` 时返回 `C2Ctu_peer_2`，严格遵循 LiteChat 的 `C2C${userID}` 格式，不额外插入下划线 | L3 | P0 |
| `MA03-P0-20` | 小程序解析标准与历史错误会话号 | `C2Ctu_peer_2` 和存量错误值 `C2C_tu_peer_2` 均解析为真实目标 `tu_peer_2`；禁止发送给 `_tu_peer_2` | L3/L4 | P0 |
| `MA03-P0-21` | 历史错误消息重发 | 重发必须使用刷新后的 `timConversationId` 重建正确目标消息，不得忽略参数并继续重发原 `_tu_*` SDK 消息 | L3/L4 | P0 |
| `DOC03-P0-03` | PRD 与 handoff 会话号示例 | 所有普通私信示例均使用 `C2C${userID}`，文档与 LiteChat 4.4.2 运行时契约一致 | 静态 | P0 |
| `EXT03-11` | 生产单聊真实发送 | 生产会话详情返回 `C2Ctu_*`；LiteChat 创建消息的 `to` 为 `tu_*`，真实文本发送不再返回 `20003` | L1/L4 | P0 |

## 18. 2026-08-16 私信与悄悄话双链路完整闭环增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `MA03-P0-22` | 消息首页正式 DTO | 小程序按 `unreadSummary/whisperSummary/likesMeSummary/assistantSummary/systemSummary/conversationPage` 消费生产响应，不再读取旧 `fixedEntries/recentConversationBindings/platformUnreadSummary` | 静态/L3 | P0 |
| `MA03-P0-23` | 私信列表平台投影 | 私信列表直接展示平台会话摘要、未读和最后消息，不提前初始化 TIM，也不因列表项没有 `timConversationId` 被过滤为空 | 静态/L4 | P0 |
| `MA03-P0-24` | 私信详情按需初始化 TIM | 先查询会话详情；仅 `canEnterConversation=true` 且有标准 TIM 会话号时获取凭证和历史；安全只读不可进入时不调用 SDK、不报初始化异常 | L3/L4 | P0 |
| `MA03-P0-25` | 悄悄话接收方双分组 | “申请我的”独立请求、展示和翻页 `pending/processed`，使用各自总数、游标、加载和错误状态；“我申请的”只请求 `sent+pending` | L3/L4 | P0 |
| `MA03-P0-26` | 悄悄话逻辑隐藏 | 接收方支持单条删除和按 `pending/processed` 分组全部删除，操作前确认、成功后只更新对应分组；发送方不展示删除入口 | L3/L4 | P0 |
| `MA03-P0-27` | 悄悄话回复与反向申请 | 待回复申请提交后进入后端返回的私信会话；已结束且允许反向申请时使用 `whisper_reverse+原 whisperNo+稳定 USR 编号` 重新预检和创建 | L3/L4 | P0 |
| `MA03-P0-28` | 写操作幂等重试 | 同一正文的悄悄话创建或回复失败重试复用原请求号；成功后清理，正文或业务对象变化后生成新请求号 | L3/L4 | P0 |
| `MA03-P0-29` | 聊天举报正式契约 | 举报原因来自 `/miniapp/community/config`；私信与悄悄话统一提交 `targetType=chat/targetId/sourceType`，不再依赖历史 `targetBizNo` 契约；可选证据最多 3 张并使用 report-evidence 票据上传 | 静态/L4 | P0 |
| `MA03-P1-30` | 双链路异常恢复 | 列表、详情、预检、发送、回复、删除、举报失败均保留可恢复状态和用户可理解提示；`30021` 清理旧报价并要求重新确认，技术字段和 TIM UserID 不直接展示 | L3/L4 | P1 |
| `EXT03-12` | 生产双账号最终互发 | 两个真实生产小程序登录态依次完成悄悄话预检/创建、接收方列表/已读/回复、进入私信、双方文本互发/重试/已读、举报或拉黑 | 全链无 4xx/5xx/腾讯错误且平台投影一致；缺少真实双账号登录态时必须标记未执行，不得用 Mock 冒充 | L1/L4 | P0 |

## 19. 2026-08-16 私信首次进入白屏与交互卡死修复增量

| 用例 ID | 场景 | 预期结果 | 层级 | 优先级 |
|---------|------|----------|------|--------|
| `MA03-P0-31` | 私信首次进入首屏 | 消息首页、私信列表和官方频道不提前同步加载完整 TIM 标准包；私信页面壳与返回按钮先渲染，再按需加载文本私信基础包 | 静态/构建/L4 | P0 |
| `MA03-P0-32` | TIM 首次连接缓慢或失败 | 会话详情、导航和输入区持续可操作；连接有明确状态和超时恢复，不出现无限白屏或全页不可点击 | L3/L4 | P0 |
| `MA03-P0-33` | 连接过程中输入并发送 | 正文允许提前输入；点击发送会复用当前连接任务，发送中禁止重复提交；失败恢复原正文并展示可理解提示 | L3/L4 | P0 |
| `MA03-P0-34` | 发送请求异常或超时 | 页面解除发送中状态、保留或恢复正文，返回按钮仍可立即返回；不得产生未处理 Promise 或静默无响应 | L3/L4 | P0 |
| `MA03-P0-35` | 微信分包体积回归 | 消息通用分包不再携带完整 TIM 标准包，微信正式构建、页面注册、主包与总包门禁全部通过 | 构建 | P0 |
