# 我的认证重复注册与空文案修复验收

## 问题基线

- 点击“立即完善”后进入 `pages/verification/my-certification`，微信运行时提示同一文件重复注册 `Page`。
- 页面卡片图标和按钮背景存在，但认证标题、说明、状态文案为空。
- 本次用户截图是故障证据，不是蓝湖目标设计基线，因此不对视觉还原度做主观打分。

## 根因与修复

- `my-certification.tsx` 直接转出另一个已注册页面 `triple.tsx`，Taro 构建后在同一入口执行了两次页面注册。两个页面现只复用普通组件 `VerificationCenterPage`。
- 本地数据库缺少认证中心文案及认证字典。新增并执行 `049_prd01_verification_runtime_seed.sql`，只补认证流程配置，不恢复登录、首页和个人中心的动态文案。
- 新增页面入口隔离门禁，并接入开发、构建前置检查，阻止页面入口再次导入或转出另一个页面入口。
- 新增认证运行时完整性校验和统一错误信息转换，配置缺失时显示明确错误，不再静默渲染空卡片或出现 `[object Object]`。

## 验收结果

- 真实链路：`首页 -> 立即完善 -> 我的认证`。
- 当前路由：`pages/verification/my-certification`。
- 必需文字：我的认证、三重认证、头像认证、实名认证、学历认证、审核状态均已显示。
- 微信运行时异常：0。
- 构建产物：`my-certification.js` 注册 1 次，`triple.js` 注册 1 次，公共分包注册 0 次。
- 接口数据：认证中心缺失文案 0；审核状态 6 条、学历人群 2 条、学历方式 4 条、头像来源 2 条。
- 自动化测试：30/30 通过；页面入口隔离检查 55/55 通过；小程序正式构建通过。

## 截图证据

- `docs/验收报告/截图证据/2026-07-14-我的认证页-运行态.png`

## 视觉评分

- 未评分：缺少该页面的蓝湖目标稿截图或完整尺寸标注。本次只确认故障修复、流程连通、运行态文字完整和现有布局未被接口接入破坏。
