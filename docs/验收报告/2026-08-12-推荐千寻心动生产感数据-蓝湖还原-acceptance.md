# 推荐、千寻、心动生产感数据与右上角图标验收报告

## 验收结论

- 推荐、理想型右上角的历史记录与偏好设置图标，已按蓝湖标注的 `18px × 18px` 映射为小程序 `36rpx × 36rpx`；可见图标缩小，但继续保留 `70/72rpx` 点击热区。
- 推荐等待聚合页不再用固定横坐标摆放图标，改为根据微信胶囊真实位置动态计算，避免不同设备上与胶囊重叠。
- 已为手机号 `17366629764` 的验收账号在当前开发/测试数据库写入可重复执行的丰富数据：推荐、理想型、千寻动态、评论回复、点赞、关注、浏览记录、喜欢我的、我喜欢的、访客与互相心动均有数据。
- SQL 已连续执行两次，第二次数量不增长，幂等验证通过。

## 蓝湖设计基线

| 元素 | 蓝湖逻辑尺寸 | 2x 原始切图 | 小程序渲染尺寸 | 点击热区 |
| --- | ---: | ---: | ---: | ---: |
| 推荐三天回看 | 18px × 18px | 72px × 72px | 36rpx × 36rpx | 70rpx |
| 推荐偏好设置 | 18px × 18px | 72px × 72px | 36rpx × 36rpx | 70rpx |
| 理想型历史记录 | 18px × 18px | 72px × 72px | 36rpx × 36rpx | 72rpx |

设计基线来源：蓝湖推荐首页、推荐空态与理想型入口稿；原始切图位于 `docs/验收报告/截图证据/2026-08-05-PRD08推荐理想型-蓝湖还原/设计基线/切图原始/`。

## 数据覆盖

| 模块 | 已写入数据 |
| --- | ---: |
| 推荐候选 | 12 人 |
| 三天回看 | 12 条 |
| 理想型筛选结果 | 12 人 |
| 千寻图文动态 | 12 条，共 28 张动态图片 |
| 千寻评论/回复 | 24 条 |
| 喜欢我的 | 8 人 |
| 我喜欢的 | 8 人 |
| 访客 | 10 人 |
| 互相心动 | 4 组 |
| 候选人主页媒体 | 48 条 |

候选人使用自然中文昵称、年龄、职业、学校、标签、自我介绍、认证状态与错落的行为时间；动态覆盖多个话题、普通动态和诚意贴，并带有多图、楼中楼回复、点赞、关注和浏览记录。

## 数据安全与一致性

- 仅通过手机号定位验收账号；虚拟候选人不写手机号、身份证或微信号。
- 使用 `RICH-QX-*` 稳定业务编号和 `seedKey`，重复执行不会重复插入。
- 数据脚本位于 `deploy/sql/ops/`，不会随生产自动迁移执行。
- 动态计数不一致：0。
- 评论回复孤儿：0。
- 关系点赞用户孤儿：0。
- 匹配来源孤儿：0。
- 访客事件孤儿：0。
- 理想型候选孤儿：0。
- 新增的 4 个 OSS 主页素材抽查均返回 HTTP 200。

## 自动验证

- `node --test scripts/test-rich-module-seed.cjs`：6/6 通过。
- `node --test miniapp/scripts/test-prd08-recommend-ideal-closure.cjs`：17/17 通过。
- `npm run build:weapp`：通过，Webpack 编译成功。
- 页面注册门禁：84 个页面全部通过。
- 小程序产物门禁：通过，`dist/app.json` 与本次涉及页面产物均存在。
- 包体门禁：主包 1.36 MiB、千寻分包 116.1 KiB、总包 2.10 MiB。

## 运行态截图说明

已尝试复用本机微信开发者工具的 `9480/9420` 自动化端口执行截图，但工具连接在自动化响应阶段超时，未生成新的可靠截图。因此本报告只确认蓝湖尺寸、布局计算、编译产物、专项测试和真实数据库数据，不虚报新的运行态截图还原分数。开发者工具恢复自动化响应后，可直接运行：

```bash
cd miniapp
WX_IDE_PORT=57814 WX_AUTO_PORT=9480 node scripts/verify-prd08-recommend-ideal-runtime.cjs
```
