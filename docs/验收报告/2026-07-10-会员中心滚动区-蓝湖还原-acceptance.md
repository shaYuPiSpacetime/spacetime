# 会员中心滚动区蓝湖还原验收报告

> 本报告的早期静态评分已被 `2026-07-10-会员权益全页复核-acceptance.md` 取代；本次复核补充了套餐轨道、我的会员横幅、会员详情与订阅管理的检查。

## 范围

- 页面：小程序会员中心，未开通会员且底部支付栏固定展示的状态。
- 蓝湖参考：项目 `时空邂逅0625`，节点 `会员中心-会员未开通，支付按钮固定下方`（`77fad5f8-086b-4f02-81f5-a356b7f69e8b`）；整页参考图为 `miniapp/.lanhu-ref/lanhu-full-2026-07-07/images/63-会员中心-会员未开通，支付按钮固定下方.png`。
- 用户问题：会员卡背景不匹配；中部权益内容没有完全展示。

## 变更与证据

| 项目 | 结果 | 证据 |
|---|---|---|
| 中部权益可滚动到末项 | 通过 | 根容器改为纵向 flex，滚动区使用剩余空间，支付栏为普通 flex 子项；不再依赖固定 `328rpx` 预留。 |
| 底部安全区 | 通过 | 支付栏使用 `max(30rpx, env(safe-area-inset-bottom))`，基础留白只取一次。 |
| 交互实现 | 通过 | 套餐、协议和开通按钮均为真实组件，未使用整页截图、透明热区或隐藏覆盖层。 |
| 会员卡背景 | 部分通过 | 已将菱形纹理整体右移、移除多余左下暗斑并校正底色；仍保留结构化 `MemberHeroPattern`，因为蓝湖 MCP 未提供无文案背景切片，不能把含旧头像/文案的 `member-vip-bg.webp` 作为动态背景。 |

## 验证

- `node scripts/validate-membership-payment-ui.mjs`：本轮布局断言已通过，但脚本继续在既有后端条件 `resolveEffectivePayAmount` 缺失处失败，和本次 UI 修改无关。
- `node scripts/validate-commerce-ui-coverage.mjs`：本轮会员页结构检查通过，但脚本继续在既有 `useMembership.ts` 原生支付成功提示规则处失败，和本次 UI 修改无关。
- `npm run build:weapp`：通过，微信小程序目标编译成功。
- 未执行 H5 编译或浏览器截图：用户已明确 H5 编译乱码，只能进行微信小程序编译。本轮未获得真机/开发者工具截图，因此不将静态检查视为像素级截图验收。

## 差异与评分

- 中部滚动和底部支付栏：`100/100`，根因已移除，代码门禁已覆盖。
- 会员卡背景：`78/100`，已修正纹理起点、左下暗斑和底色，但缺少蓝湖无文案背景切片，不能宣称像素级一致。
- 页面综合静态还原度：`88/100`。该分数不替代真机截图差异；补齐背景切片并提供微信开发者工具或真机截图后，才能完成最终视觉验收。

## 待补项

- 设计侧导出无头像、无昵称、无权益文案的会员卡背景切片；收到后替换 `MemberHeroPattern` 并重新做微信小程序截图差异验收。
