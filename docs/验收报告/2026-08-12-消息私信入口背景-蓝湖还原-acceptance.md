# 消息私信入口背景验收报告

## 验收结论

消息首页“私信”入口已替换为用户提供的完整背景图。源图按原始字节上传 OSS，运行时按 340×158rpx 等比展示；标题和副标题仍为真实文字，整张卡片仍为真实点击入口。H5 隔离运行态已验证背景显示、尺寸、公网地址和进入私信列表的跳转链路。

## 基线与结果

| 验收项 | 基线 | 结果 |
| --- | --- | --- |
| 原图 | 680×316px RGBA PNG | 通过 |
| 运行尺寸 | 340×158rpx | 通过；375px H5 下为 170×79px |
| 资源完整性 | 原始字节上传 | 通过；本地与 OSS SHA-256 一致 |
| 旧视觉 | 不再叠加旧私信气泡插画 | 通过 |
| 文案 | 独立真实组件 | 通过 |
| 点击 | 整卡进入私信列表 | 通过 |
| 背景事件 | 仅视觉，不拦截点击 | 通过 |

## 运行态证据

- [375×812 消息首页截图](截图证据/2026-08-12-消息私信入口背景/H5运行-375x812/626cd513-消息首页.png)
- 背景卡片与用户素材形状、环形渐变和白色对话气泡一致，未出现拉伸变形。
- 页面视觉一致度：99/100；扣 1 分仅因本轮证据为 H5 固定视口，不是真机微信截图。

## 自动验证

- `node scripts/validate-message-18-lanhu.mjs`：通过。
- `node scripts/validate-qianxun-topic-demo-closure.mjs`：通过。
- H5 隔离运行态断言：背景唯一、OSS 地址正确、尺寸正确、整卡点击进入私信列表，全部通过。
- `npm run build:h5`：通过，仅有仓库既有包体积警告。
- `npm run build:weapp`：通过；84 个页面注册门禁、无开发 Token 门禁和包体门禁均通过，主包 1.35 MiB、总包 2.10 MiB。
- `git diff --check`：通过。

## 已知非本轮问题

全量图标校验仍受既有资源问题影响：旧千寻币资源公网字节不一致，以及旧推荐模块部分本地源文件缺失。本轮新增私信背景已单独完成公网回读哈希校验，不受该问题影响。
