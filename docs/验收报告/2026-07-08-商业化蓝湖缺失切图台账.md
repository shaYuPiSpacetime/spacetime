# 2026-07-08 商业化蓝湖缺失切图台账

## 用途

本台账用于跟踪千寻币、会员中心、订阅管理页面中未能通过蓝湖 MCP slices 或明确导出素材获得的切图缺口。禁止从整页参考图热区硬裁为运行切图；未通过 MCP slices 或明确导出的局部素材，只能作为结构化占位并在验收报告登记。

蓝湖全量设计稿 index 连续性：manifest.index 已按 1..93 严格连续校验，避免商业化状态清单漏用或错用参考图编号。

## 2026-07-10 全页面图片复查补录

- 小程序页面与共享组件共扫描 66 个图片资源，其中 61 个被页面或设计清单引用；未发现新的整页截图叠加交互控件问题。
- 千寻币用途已从整卡切图拆为 8 个独立 2x PNG，并按源文件字节原样上传上海 2 OSS；运行时改为真实 `Image` + 文案组件，不再引用整卡切图。
- 精选主图仍保留人物照片原图，底部旧文案和心形仅由运行时遮罩清理，昵称、资料和操作区使用真实组件承接；缩略图只取照片区域。
- 我的页提升人气按钮、VIP 横幅已纳入 OSS 原字节清单，前端引用生成的 OSS 清单，不在构建过程中转码或压缩。
- 登录场景、认证教程、头像和推荐背景均按原文件保留；认证教程截图属于说明内容，不叠加同位置交互控件。

## 商业化页面缺失切图复查

| 缺口 | MCP 结果 | 当前处理 | 后续要求 |
|------|----------|----------|----------|
| 千寻币用途：已从整卡切图拆为 8 个独立 2x PNG，并已原样上传 OSS | 蓝湖 MCP 仅给整卡切片，未获得 8 个独立用途 icon | 8 个独立 PNG 已原样上传上海 2 OSS，运行时引用生成清单 | 后续新增图标继续执行 `npm run assets:upload-icons` |
| 千寻币明细-暂无数据：totalSlices=0，缺空态插画独立切图 | totalSlices=0 | `EmptyState`、`EmptyPlusMark`、`EmptyRingMark` 结构化占位 | 补导出空态插画切图后替换占位函数 |
| 会员记录：totalSlices=0，缺会员菱形图标和退款章切图 | totalSlices=0 | `MemberRecordDiamond`、`RefundStamp` 结构化占位 | 补导出会员菱形图标和退款章星形/圆弧细节切图 |
| 会员记录详情：仅返回纯色矩形 shape，不接入业务切图 | totalSlices=1，且只有纯色矩形 shape | 不接入无语义矩形切图，继续源码结构还原卡片 | 如详情页后续出现业务图标，需重新导出有效切图 |
| 会员中心状态页 60/61/62/63：totalSlices=0，缺无文案会员卡背景切图 | totalSlices=0 | `MemberHeroPattern` 结构化几何纹理占位，权益图标沿用 08 切片 | 补导出无文案会员卡背景，替换 CSS 纹理 |
| 订阅管理：totalSlices=0，缺会员卡背景和 STEP1/STEP2 微信流程截图切图 | totalSlices=0 | `SubscriptionHeroPattern`、`SearchPlaceholder`、`RenewPlaceholder` 结构化占位 | 补导出会员卡背景、两张微信流程截图、红框/箭头局部素材 |
| 微信支付态 65/71：totalSlices=0，微信支付键盘为微信原生 UI | totalSlices=0 | 不手写键盘；`WechatMockPayPanel` 仅作 demo fallback | 正式小程序用 `wx.requestPayment` 真机截图验收 |
| 底部协议弹层 76/79：totalSlices=0，按整页参考图采样手写 | totalSlices=0 | 按 bbox 采样手写底部弹层 | 若蓝湖补导出弹层组件切图，再替换局部图形 |
| 会员套餐第 4 张卡片完整展开：蓝湖首屏只露出右缘，缺完整展开标注 | 首屏只露出右侧边缘 | 当前按普通年卡数据占位 | 补完整状态稿或标注后校准第 4 张卡片完整内容 |

## 缺失切图替换映射

| 缺口 | 页面/状态 | 源码占位 | 替换目标 | 不可硬裁说明 |
|------|----------|----------|----------|--------------|
| 千寻币用途 8 个独立 icon | 09-千寻币 | `coin-usage/` 8 个独立 PNG + OSS 清单 | OSS 原样 URL + 真实文字组件 | 不再引用整卡切图 |
| 千寻币明细空态插画 | 67-千寻币明细-暂无数据 | `EmptyState` / `EmptyPlusMark` / `EmptyRingMark` | 空态插画独立 PNG/WebP | 不从 67 整页图热区硬裁 |
| 会员中心无文案会员卡背景 | 60/61/62/63 会员状态页、78-订阅管理 | `MemberHeroPattern` / `SubscriptionHeroPattern` | 无文案会员卡背景切图 | 不复用含旧文案的 `member-vip-bg.webp` |
| 会员记录图标与退款章 | 58-会员记录 | `MemberRecordDiamond` / `RefundStamp` | 会员菱形图标、退款章独立切图 | 不从 58 整页图热区硬裁 |
| 会员记录详情纯色 shape | 59/77 会员记录详情 | `SummaryCard` / `InfoCard` 源码结构 | 有业务图标时重新导出切片 | 不接入无语义纯色矩形 shape |
| 订阅管理 STEP 微信流程图 | 78-订阅管理 | `SearchPlaceholder` / `RenewPlaceholder` | STEP1/STEP2 微信流程截图切图 | 不从 78 整页图热区硬裁 |
| 微信支付原生键盘 | 65/71 微信支付态 | `WechatMockPayPanel` demo fallback | 真机 `wx.requestPayment` 原生截图 | 不手写数字键盘，不把 fallback 当生产 UI |
| 底部协议弹层 | 76/79 底部弹层 | `UnpaidBottomSheet` / `AgreementConfirmSheet` | 蓝湖组件切片或截图差异验收 | 不从 76/79 整页图热区硬裁 |

## 占位函数 allowlist

- `EmptyState`、`EmptyPlusMark`、`EmptyRingMark`：千寻币明细暂无记录插画。
- `MemberHeroPattern`：会员中心会员卡无文案背景。
- `MemberRecordDiamond`、`MemberRecordGemLine`、`RefundStamp`、`RefundStampArc`、`RefundStampStar`：会员记录会员图标和退款章。
- `SubscriptionHeroPattern`：订阅管理会员卡无文案背景。
- `SearchPlaceholder`、`RenewPlaceholder`：订阅管理取消续费指引里的两张微信流程截图。

## 当前结构化占位锚点

- 千寻币明细空态：浅灰插画整体 `left:226rpx/top:526rpx/298rpx*254rpx`；暂无记录文字 `left:321rpx/top:779rpx/108rpx*25.5rpx`。
- 会员记录占位：会员菱形金色主体 `left:51rpx/top:229rpx/48rpx*38rpx`；退款章灰色主体 `left:320.5rpx/top:440.5rpx/189.5rpx*85.5rpx`。
- 会员记录菱形内部：`MemberRecordGemLine` 为结构化线段占位，缺真实会员菱形图标切图。
- 会员记录退款章内部：`RefundStampArc` 断圆弧和 `RefundStampStar` 星形装饰为结构化占位，缺真实退款章星形/圆弧切图。
- 会员卡头像金边：60/61/62/78 为 `left:52rpx/top:241rpx/92rpx*92rpx`，63 为 `left:51rpx/top:241rpx/92rpx*92rpx`。
- 订阅管理 STEP 图：STEP1 白底图 `left:51rpx/top:1136rpx/340.5rpx*230rpx`，STEP2 白底图 `left:51rpx/top:1496rpx/341.5rpx*390rpx`。
- 订阅管理 STEP 红色标注：STEP1 箭头 `left:346rpx/top:1246rpx/17rpx*34.5rpx`、前往红框 `left:318.5rpx/top:1294.5rpx/65.5rpx*43rpx`；STEP2 箭头 `left:305.5rpx/top:1657rpx/44.5rpx*41.5rpx`、服务项红框 `left:73.5rpx/top:1707.5rpx/313.5rpx*95.5rpx`。
- 微信支付键盘：微信原生系统 UI，不绘制数字键盘；demo fallback 只保留支付成功/取消动作。

## 不可交付为 1:1 的原因

- 缺失切图处仍是占位，不作为 1:1 完成项。
- 微信支付键盘是微信原生系统 UI，demo fallback 不能作为生产支付面板 1:1 证据。
- 当前未运行小程序截图闭环，不能给出像素差异评分或 95%+ 高还原结论。
