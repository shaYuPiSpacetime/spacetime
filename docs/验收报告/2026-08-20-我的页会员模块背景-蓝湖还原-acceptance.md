# “我的”页会员模块背景蓝湖还原验收报告

## 1. 验收结论

会员模块背景已由近似手绘渐变替换为蓝湖 `VIP` 图层原始 SVG。背景的尺寸、圆角、渐变、透明度和右上装饰路径直接沿用设计源文件；真实标题、按钮、到期时间和点击事件未被烘焙进图片。

本次范围“会员模块背景”验收通过。由于本机没有微信开发者工具，未补充真机/模拟器整页截图；不据此宣称整张“我的”页面已完成全页像素级复验。

## 2. 基线与映射

| 项目 | 蓝湖基线 | 运行实现 | 结果 |
| --- | --- | --- | --- |
| 图层 | `VIP` | `vip-banner-bg.svg` | 通过 |
| 位置 | `x=25, y=512` | `left=25rpx, top=512rpx` | 通过 |
| 尺寸 | `700 × 128` | `700rpx × 128rpx` | 通过 |
| 圆角 | `12` | 容器与 SVG 均为 `12` | 通过 |
| 渐变 | `#151515 -> #484848` | 蓝湖原始 SVG 渐变 | 通过 |
| 右上装饰 | `#F7D9A5 / 20%` | 蓝湖原始 SVG 路径 | 通过 |
| 标题与按钮 | 独立图层 | 真实 `Text/View` 组件 | 通过 |
| 状态 | 未开通/已开通/已过期 | 三态共用背景，文案和操作独立 | 通过 |

## 3. 变更前后差异

| 检查项 | 变更前 | 变更后 |
| --- | --- | --- |
| 背景渐变 | 代码估算的三段渐变 | 蓝湖原始两端渐变 |
| 装饰图案 | 两个旋转矩形近似绘制 | 蓝湖原始矢量路径 |
| 设计可追溯性 | 依赖手工参数 | 原始 SVG 直接落库 |
| 主包体积 | 无新增资源 | `1.39 MiB`，门禁内 |
| 交互 | 真实组件 | 保持不变 |

## 4. 验证结果

- `node scripts/validate-membership-benefit-pages.mjs`：通过。
- `./node_modules/.bin/taro build --type weapp`：通过，Webpack 编译成功。
- `node scripts/validate-built-page-registrations.mjs`：通过，84 个页面注册正确。
- `node scripts/validate-built-dev-fixed-login.mjs`：通过。
- `node scripts/validate-built-package-size.mjs`：通过，主包 `1.39 MiB`，总包 `2.40 MiB`。
- 源文件与构建产物 SHA-256 一致：`249036dd558343817865c3dece45bc8e067721e73f3e1ff59a7de8a613ea6f04`。

## 5. 已知环境缺口

- `npm run build:weapp` 的全量前置流程在既有门禁处中断：仓库缺少 `.lanhu-ref/lanhu-full-2026-07-07/images/08-会员中心-全.png`。该缺口与本次代码无关；改为直接执行 Taro 编译并单独完成产物门禁。
- 本机没有微信开发者工具，因此运行态整页截图与真机字体抗锯齿复核待具备工具后补充。
