# 学历认证与心动 Tab 异常闭环验收

## 1. 验收结论

本轮三个问题均已修复并通过自动化回归：学历认证在校学生页顺序与背景分层对齐蓝湖；请求 ID `3738c2f0004d4dacb081df92fbfc9a03` 对应的评论列表 NPE 已消除；心动页匹配弹层不再阻断底部 Tab。

## 2. 蓝湖对齐结果

| 检查项 | 蓝湖基线 | 实现结果 |
|--------|----------|----------|
| 页面内容高度 | 1678rpx | 1678rpx |
| 学生资料卡 | top=500rpx，min-height=725rpx，白色卡片 | 一致 |
| 空材料上传区 | 640×306rpx | 一致 |
| 提交按钮 | top=1258rpx，700×98rpx | 一致 |
| 协议行 | top=1382rpx，位于提交下方 | 一致 |
| 客服入口 | 协议后独立间距 | 一致 |
| 背景分割 | 渐变页底 + 白色资料卡 + 独立底部操作区 | 一致 |

有材料时保留蓝湖 148×148rpx 缩略图网格和继续上传入口；表单、按钮、勾选与客服均为真实组件。

## 3. 测试证据

| 层级 | 结果 |
|------|------|
| 后端故障复现 | 新增一级评论空父 ID 用例，修复前稳定复现同栈 NPE |
| 后端定向测试 | `CommunityAdminServiceImplTest#getCommentPage_rootCommentWithoutParent_shouldReturnNormally` 通过 |
| 后端全量测试 | 493 项，0 failure，0 error，0 skipped，BUILD SUCCESS |
| 学历静态/TDD | `test-verification-onboarding-flow.cjs` 9/9 通过 |
| 关系静态/TDD | `test-relation-feedback-flow.cjs` 9/9 通过 |
| 小程序正式构建 | Webpack 成功；76 个页面注册通过；主包 1.30 MiB，总包 1.97 MiB |
| 关系微信运行态 | 匹配弹层存在时成功切换到“我的”；完整关系闭环通过 |
| 学历微信运行态 | 390px 模拟器按 750rpx 比例核对资料卡、提交、协议、客服坐标通过；异常监听为 0 |

微信开发者工具本轮截图接口超时，限制记录保存在 `docs/验收报告/截图证据/2026-08-05-学历认证在校学生-闭环/截图限制说明.txt`；运行态节点、坐标、路由切换和异常监听均已执行，不以静态源码替代运行验证。

## 4. 发布边界

本轮没有数据库结构或配置变化，不需要新增 SQL。代码推送触发发布后，需再次验证生产评论列表与服务健康状态。
