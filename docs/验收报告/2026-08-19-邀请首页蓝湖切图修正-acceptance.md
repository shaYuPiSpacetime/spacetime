# 邀请首页蓝湖切图修正验收报告

## 1. 结论

本次邀请首页蓝湖切图修正的代码、OSS 原字节、H5 375×812 运行态、微信开发者工具运行态和自动门禁均通过。首屏还原度评估为 **97%**，长页整体评估为 **95%**，达到约定阈值。

微信开发者工具版本为 `2.02.2608182`，本地 wechatide skill/tool 均为 `0.3.9`。2026-08-20 已在原生模拟器完成首屏、进度区、原生分享、规则页跳转、接口和控制台复核。总体验收结论为 **通过**。

## 2. 验收范围

- 页面：`/pages/promotion/invite-home`。
- 未改动：邀请记录页、活动说明页、后端接口、分享归因和邀请业务类型。
- 动态数据：本地 Mock 返回注册奖励 19、进度 0、档位 10/30/50；页面按接口值展示。

## 3. 素材验收

| 资源 | 蓝湖原始 | OSS 回下载 | 结论 |
|---|---|---|---|
| 1500×5024 背景 | 1,130,459 字节；`c207b79c...5b33e` | 字节数与 SHA-256 完全一致 | ✅ 原字节 |
| 968×176 图标组 | 25,677 字节；`c498ff33...9b35` | 字节数与 SHA-256 完全一致 | ✅ 原字节 |

- 客户端仅引用 `miniappOssIcons.promotionInviteBackground` 与 `promotionInviteEquationSprite`。
- 未使用蓝湖 CDN、本地运行时回退、图片转换、压缩或 CDN 图片处理参数。
- 原始切图与 `SHA256SUMS.txt` 已归档到本次截图证据目录。

## 4. 视觉差异台账

接受基线：`docs/验收报告/截图证据/2026-07-29-邀请好友五页/蓝湖基线/01-邀请好友.png`。

最新渲染：`docs/验收报告/截图证据/2026-08-19-邀请首页蓝湖切图修正/运行态/03-微信开发者工具-首屏.png`；H5 375×812 截图作为跨端补充证据。

截图方法：H5 使用浏览器原生 375×812 视口；最终验收使用微信开发者工具 `simulator_screenshot`，保留原始 PNG 718×1554（模拟器逻辑视口约 359×777）。同一轮视觉 QA 已同时查看接受基线和最新微信渲染。

| 对比点 | 基线证据 | 渲染证据/处理 | 结果 |
|---|---|---|---|
| 顶部主视觉 | 品牌文案和人物为完整视觉切图 | 使用指定 1500×5024 背景按 375×1256 铺底，删除重复代码标题和人物 | ✅ 无重影 |
| 奖励图标 | 分享、用户、千寻币三枚同一视觉体系 | 三个独立 44×44 `Image` 裁窗使用同一无损 sprite | ✅ 重量一致 |
| 奖励文案 | 基线示例为“获得20千寻币” | Mock 动态显示“获得19千寻币” | ✅ 有意动态差异 |
| 卡片标题 | 紫色居中梯形标题 | 真实卡片组件保持居中，背景不承载点击区 | ✅ |
| 阶梯轨道 | 节点按实际阈值分布 | 10/30/50 映射为 20%/60%/100%，节点、轨道和进度共享坐标 | ✅ |
| 交互区 | 立即邀请与活动说明为可见控件 | H5 完成复制降级，微信完成原生分享和规则页导航 | ✅ 无透明热区 |
| 长页连续性 | 进度、记录、规则依次排列 | 375×812 可滚动到全部区块，无横向溢出 | ✅ |
| 滚动导航 | 导航悬浮时不得与卡片标题叠字 | 滚动超过 8px 后切换实色紫色背景并遮挡下穿内容 | ✅ |
| 色彩与圆角 | 紫色背景、白卡、浅紫描边与渐变主按钮 | 微信原生截图与基线主色、卡片节奏和按钮重量一致 | ✅ |

首屏允许文案清单与蓝湖一致：`邀请好友`、`好友同行·奖励加倍`、`一起遇见更好的缘分`、`好友完成注册即邀请成功，关系永久有效`、`邀请注册得千寻币`、三段标签、`立即邀请`、新用户提示、`活动说明`。代码未新增未批准的首屏品牌文案；唯一有意文字差异为接口动态金额 19，替代设计示例 20。微信系统状态栏、胶囊和刘海属于平台外壳差异，不计为页面偏差。

## 5. 交互验收

| 场景 | H5 | 微信开发者工具 |
|---|---|---|
| 页面滚动 | ✅ | ✅ 可达进度、记录、规则；导航无叠字 |
| 立即邀请 | ✅ 复制降级提示 | ✅ 打开原生会话选择面板 |
| 活动说明入口 | ✅ 进入规则页 | ✅ 进入 `pages/promotion/invite-rules` |
| 动态奖励金额 | ✅ 显示 19 | ✅ 显示 19 |

## 6. 自动门禁

- `npm run validate:prd07-miniapp`：归因 4/4、阶梯 3/3、静态 UI 15/15。
- `npm run build:h5`：成功，仅有仓库既有体积告警。
- E2E 配置直编 `npx taro build --type weapp --no-check`：Webpack 完整编译成功；`--no-check` 仅绕过 Taro 4.1.9 `plugin-doctor` 读取 macOS 系统配置时的原生崩溃，不跳过业务编译。
- 同配置 `npm run postbuild:weapp`：84 个页面注册、固定登录产物和包体 3 项门禁通过；主包 1.16 MiB，总包 1.90 MiB。
- 完整 `npm run build:weapp`：被无关会员中心缺失蓝湖文件 `08-会员中心-全.png` 阻塞；未影响本页直接微信编译结果。
- `git diff --check`：通过。

## 7. 微信补测闭环

- 首屏：`运行态/03-微信开发者工具-首屏.png`。
- 进度区：`运行态/04-微信开发者工具-进度区.png`。
- 原生分享：`运行态/05-微信开发者工具-原生分享.png`。
- 运行态发现透明导航在滚动后允许卡片标题下穿；根因是导航始终使用透明背景。已新增 `handleHomeScroll`，滚动超过 8px 后切换实色背景，并加入静态回归门禁。
- 聚合接口 HTTP 200、动态金额 19、档位 10/30/50；本页验证期间控制台无错误。

完整 `npm run build:weapp` 仍受非本页会员中心基线文件缺失影响；邀请首页直编微信产物、84 页面注册、固定登录产物和包体门禁均已通过，不影响本页验收结论。
