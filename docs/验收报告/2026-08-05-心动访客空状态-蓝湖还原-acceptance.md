# 心动与访客空状态插画还原验收报告

> 验收日期：2026-08-05  
> 验收范围：小程序“对我心动”“访客”两个空数据状态

## 1. 验收结论

两个空态已分别使用用户截图对应的高清爱心/人像插画，并显示指定文案；两张插画保持水平居中，插画与文案组合已在导航和底部操作区之间垂直居中。正常空态不显示“重新加载”，错误态重试能力保留。

## 2. 逐项结果

| 验收项 | 结果 | 证据 |
|--------|------|------|
| 对我心动空态图 | 通过 | `qianxunEmptyHeart`，对应原始 `empty-heart.png` |
| 对我心动文案 | 通过 | “暂时没有对我心动人员” |
| 访客空态图 | 通过 | `qianxunEmptyFollowing`，对应原始 `empty-following.png` |
| 访客文案 | 通过 | “暂时还没有访客” |
| 图片完整展示 | 通过 | `Image mode="aspectFit"`，330rpx × 230rpx 展示框 |
| 插画水平居中 | 通过 | 100% 宽度 flex 容器，`justifyContent: center` |
| 空态垂直居中 | 通过 | 空态占满首屏剩余高度，运行态组合中心误差 <= 2px |
| 状态隔离 | 通过 | 插画只在 `state === 'empty'` 时渲染 |
| 空态操作 | 通过 | 两个空态均不传 `onRetry`；组件仅在 `state === 'error'` 时渲染“重新加载” |
| OSS 内容校验 | 通过 | 87/87 图标字节校验通过；两张资源均返回 HTTP 200、`image/png` |
| 关系专项测试 | 通过 | 9/9；专项闭环门禁通过 |
| 微信运行态空态 | 通过 | 水平中心差 <= 1px、垂直中心差 <= 2px，空态 WXML 不含“重新加载” |
| 运行截图 | 通过 | `04-喜欢列表空态.png`、`04b-访客列表空态.png` |
| 正式小程序构建 | 通过 | Webpack 成功；75 个页面注册正确；主包 1.30 MiB，总包 1.96 MiB |
| 发布态产物隔离 | 通过 | 登录首页启动，无开发 Token |

## 3. 截图验收说明

微信开发者工具默认自动化端口未开放后，改用当前工具实际 HTTP 端口完成运行态自动化。脚本通过本地 Mock 分别进入“对我心动”和“访客”空态，读取空态、插画、文案与会员入口的实际位置和尺寸：水平中心差不超过 1px，“插画+文案”组合的垂直中心差不超过 2px，空态区域延伸到会员入口上方；同时检查空态节点 WXML，不包含“重新加载”。

截图证据位于 `docs/验收报告/截图证据/2026-08-04-关系反馈与互动链路-小程序闭环/`：

- `04-喜欢列表空态.png`
- `04b-访客列表空态.png`

本轮自动化截图与完整运行态业务断言均通过。自动化结束后重新执行正式构建，产物已恢复为登录首页且不含开发 Token。

## 4. 还原评价

| 维度 | 得分 | 依据 |
|------|------|------|
| 页面结构与信息层级 | 20/20 | 两个真实空数据分支独立渲染 |
| 尺寸、间距、对齐 | 20/20 | 运行态水平中心差 <= 1px、垂直中心差 <= 2px |
| 字体、颜色、圆角、阴影 | 19/20 | 沿用现有关系页设计 token，截图无明显偏差 |
| 素材、图标、图片裁剪 | 15/15 | 蓝湖原始透明 PNG，`aspectFit` 完整展示 |
| 交互状态与弹窗浮层 | 15/15 | 空态无重试，错误态保留重试 |
| 响应式与安全区 | 10/10 | iPhone 12/13 模拟器截图无遮挡 |
| **总分** | **99/100** | 达到关键首屏 >= 97% 的交付门禁 |
