# 2026-07-10 千寻币充值须知与明细修复验收记录

## 验收范围

- 充值须知弹窗：`/pages/coins/index?variant=recharge-notice`
- 千寻币明细：`/pages/coins/detail`
- GET 查询参数清理：`miniapp/src/services/request.ts`

设计基线：`miniapp/.lanhu-ref/lanhu-full-2026-07-07/images/72-千寻币-充值须知.png`，画布 `750×1624`。

## 数据核对

当前用户服务端数据：

| 项目 | 结果 |
|------|------|
| 千寻币余额 | 6000 |
| 有效流水 | 1 条 |
| 流水金额 | `+6000` |
| 流水类型 | `recharge` |
| 关联订单 | 27，支付成功 |
| 入账时间 | `2026-07-10 17:44:10` |

无筛选接口返回该流水，因此本次问题不是数据库漏写流水或缺索引。

## 修复结果

| 验收项 | 蓝湖/业务基线 | 实现结果 |
|--------|---------------|----------|
| 请求参数 | 未筛选时不传 `flowType` | URL 为 `?page=1&size=20` |
| 页面返回刷新 | 充值后返回应重新查询 | `useDidShow` 每次展示刷新 |
| 遮罩 | `rgba(51,51,51,0.4)` | 一致 |
| 弹窗外框 | `65/386/620×538rpx` | 一致 |
| 圆角 | `32rpx` | 一致 |
| 内边距 | `51 45 28rpx` | 一致 |
| 标题 | `32/45rpx`、500 | 一致 |
| 正文 | 原文、`530×282rpx`、`24/40rpx` | 一致 |
| 按钮 | `253×68rpx`、间距 24、圆角 8 | 一致 |
| 按钮文案 | `联系客服 / 好的` | 一致 |

## 自动验证

- `node scripts/validate-coin-closure.mjs`：通过。
- `npx eslint src/services/request.ts src/services/payment.ts src/pages/coins/detail.tsx src/pages/coins/index.tsx`：通过，清理了明细页未使用导入。
- `npm run build:weapp`：通过；仅保留既有大资源和异步分包性能警告。
- Playwright 请求监听：`http://localhost:8080/miniapp/coin/flows?page=1&size=20`，通过。

## 截图状态

- 蓝湖原始截图已核对，MCP 节点尺寸、字体、颜色和间距已逐项落到源码门禁。
- H5 不能作为本页视觉证据：项目使用小程序 `rpx` 内联样式，H5 会忽略这些值，渲染结果不等价。
- 微信开发者工具自动化端口可连接，但当前 IDE 会话的 `App.getCurrentPage` 无响应，未能导出运行截图。
- 因缺少最终小程序运行截图，本报告不宣称像素级还原度分数；源码规格和微信构建已通过，仍需在开发者工具恢复响应后补一张 `750×1624` 运行图完成像素差异评分。

## 结论

明细空状态的两个前端根因均已修复，当前 6000 账户无需数据库补数。充值须知弹窗已按蓝湖节点精确恢复，并有静态门禁防止文案和关键视觉规格再次回退。
