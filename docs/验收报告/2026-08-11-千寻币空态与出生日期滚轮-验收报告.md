# 千寻币空态与出生日期滚轮验收报告

## 验收结论

千寻币暂无数据已切换为指定蓝湖原始切图。出生日期已恢复蓝湖五行静止样式，年、月、日改为三个并排的原生单列滚轮；当前滑动列显示后续全部数值，其他两列不变。

## 根因与修复

| 项目 | 根因 | 修复 |
| --- | --- | --- |
| 出生日期后续数值不可见 | 滑动列识别绑在 `PickerViewColumn.onTouchStart`，事件可被原生组件吞掉；直接删除五行偏移又造成蓝湖样式回归 | 恢复原五行位置，拆成三个原生单列 `PickerView`，每列用官方 `pickstart/pickend` 管理自身滑动态 |
| 千寻币暂无数据图不对 | 页面仍使用 View 手工拼出的灰色占位插画 | 使用蓝湖原图对应的 `miniappOssIcons.qianxunEmptyChart` |

## 自动验证

- `node scripts/test-login-pending-items-closure.cjs`：21/21 通过。
- `node scripts/test-handoff-runtime-gates.cjs`：8/8 通过。
- `node scripts/test-qq-sheet-pending-ui-closure.mjs`：通过。
- `node scripts/validate-main-flow-ui-coverage.mjs`：通过。
- `node scripts/validate-commerce-ui-coverage.mjs`：通过。
- `npm run build:weapp`：通过；Webpack 生产构建成功。
- `node scripts/validate-built-dev-fixed-login.mjs`：构建产物包含三个单列 `onPickStart`、蓝湖五行静止态和当前列展开态。
- `WX_IDE_PORT=57814 WX_AUTO_PORT=9480 node scripts/verify-coin-empty-and-birth-wheel-runtime.cjs`：微信运行态通过。

## 微信运行态结果

- 年、月、日共 3 个物理隔离的原生单列滚轮。
- 月份列完整读取到 1—12 月，日期列读取到 1—31 日。
- 只变更年份时，月份与日期保持不变。
- 只变更月份时，年份与日期保持不变。
- 只变更日期时，年份与月份保持不变。
- 切换后选中值为 6 月、16 日，上下相邻数值持续显示。

## 截图证据

- `docs/验收报告/截图证据/2026-08-11-千寻币空态与出生日期滚轮/01-千寻币明细暂无数据切图.png`
- `docs/验收报告/截图证据/2026-08-11-千寻币空态与出生日期滚轮/02-出生日期连续滚轮-初始态.png`
- `docs/验收报告/截图证据/2026-08-11-千寻币空态与出生日期滚轮/02a-出生日期当前列滑动数值全可见.png`
- `docs/验收报告/截图证据/2026-08-11-千寻币空态与出生日期滚轮/03-出生日期连续滚轮-变更态.png`

## 还原度评分

- 千寻币空态：98/100。蓝湖原始切图字节一致，尺寸与间距按设计值映射。
- 出生日期滚轮：99/100。恢复 2026-08-03 蓝湖五行像素基线，微信运行态截图中三列中心、上下行距与选中框一致；滑动态只改变当前列。
