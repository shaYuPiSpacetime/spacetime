# 知音双动态 UI 验收报告

## 验收范围

- 心灵搭子：固定配置手机号对应作者的普通动态。
- 时空站台：工作人员发布的 `sincere_post` 动态。
- 视觉基线：改造前“时空站台”的 700rpx 动态卡片。
- 本次没有新增设计稿素材；按用户要求将两页收敛到同一套既有 UI。

## 运行态证据

| 页面 | 截图 | 结果 |
| --- | --- | --- |
| 心灵搭子 | `截图证据/2026-09-09-知音双动态/01-心灵搭子.png` | 使用统一动态卡片 |
| 时空站台 | `截图证据/2026-09-09-知音双动态/02-时空站台.png` | 使用统一动态卡片；工作人员显示发布按钮 |

运行态脚本使用等长夹具分别打开两个 Tab，已验证：

1. 两页均渲染 `.qianxun-zhiyin-post-card`。
2. 卡片 `style` 完全一致。
3. 卡片实际宽高完全一致。
4. 作者、关注、正文、活跃时间、悄悄话、评论、心动和更多操作结构一致。
5. “心灵搭子”不显示发布按钮；“时空站台”仅在服务端返回工作人员能力时显示发布按钮。

## 差异清单

| 项目 | 是否允许 | 结论 |
| --- | --- | --- |
| Tab 名称与选中态 | 是 | 符合 |
| 请求数据源 | 是 | 符合 |
| 空态文案 | 是 | 符合 |
| 时空站台发布按钮 | 是，权限差异 | 符合 |
| 卡片布局、尺寸、颜色、交互位置 | 否 | 无差异 |

## 还原度评分

- 组件同源：100/100
- 运行态尺寸一致：100/100
- 交互结构一致：100/100
- 综合：100/100

## 验证命令

- `npm run build:weapp`
- `QIANXUN_SKIP_E2E_BUILD=true node scripts/verify-qianxun-sincere-layout-runtime.cjs`
