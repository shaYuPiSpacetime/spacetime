# 全局定义（端专属层）- {{端：管理后台 / 移动端}}，每端一份

> **全局端专属层 = 这个端的全端共性，管理后台一份，移动端一份。**
> 这里放：全端信息架构原则、角色/登录态基线、权限控制原则、UI 通用空态/错误态、字段展示通用规则、跨模块端文案 key。
> 不放：单模块菜单树、单模块页面树、单模块权限矩阵、单模块审计清单、模块级文案 key。这些放模块端内定义（如 `APP-07_端内定义.md` / `ADM-07_端内定义.md`）或模块公共定义。
> 跨模块项目级术语、枚举、状态机、错误码、业务规则在 `10_模板_全局定义_共享层.md`；单模块跨端定义在模块 PRD 或模块公共定义中。本文只引用不复述。
>
> ⚠️ 本文件是**空模板**，仅含结构与占位。业务值未拍板前请标注 `[待确认]`。

| 版本 | 日期 | 修改人 | 变更摘要（**改动须列出受影响的页面 ID**） |
|------|------|--------|----------|
| v0.1 | YYYY-MM-DD | | 初稿 |

---

## 1. 适用范围与归属边界

| 内容类型 | 放置位置 | 说明 |
|----------|----------|------|
| {{端}}全端导航原则、登录态/角色基线、通用 UI 状态、字段展示通用规则 | 本文件 | 对全部 {{端}} 模块默认生效 |
| 单模块菜单树、页面树、模块权限矩阵、端内 UI 状态 | 模块端内定义 | 例如 `APP-{{模块号}}_端内定义.md` / `ADM-{{模块号}}_端内定义.md` |
| 跨端或多页面复用的模块文案 | 模块公共定义 | 使用 `M{{模块号}}-TXT-*` |
| 跨模块业务术语/规则/错误码 | `10_模板_全局定义_共享层.md` | 使用 `GLB-*` |
| 单模块跨端业务定义 | 模块 PRD 或模块公共定义 | 使用 `M{{模块号}}-*` |

---

## 2. 全端信息架构原则

> 写这个端的导航/菜单/入口共性原则，不列单模块页面清单。

| 原则 | 说明 |
|------|------|
| 入口组织 | {{如：管理后台按业务域组织一级菜单；移动端按 Tab/我的页服务区承载模块入口}} |
| 页面 ID 归属 | 单模块页面 ID 在模块端内定义登记 |
| 跨模块入口 | 若入口跨 2 个及以上模块复用，才在本文登记原则或公共入口 |

---

## 3. 角色 / 登录态基线

> 管理后台登记全局角色基线；移动端登记未登录/已登录等登录态基线。
> 单模块的角色职责映射、操作权限矩阵放模块端内定义。

### 3.1 角色或登录态清单

| code / 状态 | 显示名 | 全端职责或默认能力 |
|-----------|--------|-------------------|
| `GLB-ROLE-{{标识}}` / {{登录态}} | {{显示名}} | {{职责/能力}} |

### 3.2 权限控制原则

| 原则 | 说明 |
|------|------|
| 菜单/入口权限 | {{无权限时入口隐藏/置灰/引导登录}} |
| 页面权限 | {{无权限时页面不可访问或展示无权限态}} |
| 操作权限 | {{按钮隐藏/置灰，后端必须二次校验}} |
| 数据权限 | {{按角色/组织/本人/账号范围控制；模块需补充具体范围}} |
| 导出权限 | {{导出敏感数据需单独授权并记录审计日志}} |

### 3.3 全端敏感操作审计基线

| 操作类型 | 是否二次确认 | 是否记录审计日志 | 审计日志保留 |
|----------|-------------|-----------------|-------------|
| {{操作类型}} | 是/否 | 是/否 | {{时长}} |

---

## 4. UI 通用空态 / 错误态 / 降级态

> 页面级状态默认继承以下规则，有特殊需求在页面规格中覆盖。
> 后台和移动端的通用表现/文案不同，所以放全局端专属层；单模块特有状态放模块端内定义或页面规格。

| 场景 | 表现 | 默认文案 |
|------|------|----------|
| 列表无数据 | 居中插画 + 文案 + 操作引导 | {{文案}} |
| 搜索无结果 | 居中插画 + 文案 + 清除搜索建议 | {{文案}} |
| 请求失败（网络） | toast 提示 + 页面保留上次数据 | {{文案}} |
| 请求失败（服务端） | toast 提示 + 重试按钮 | {{文案}} |
| 无权限 | 页面不展示 / 操作按钮置灰 | {{文案}} |
| 第三方不可用（降级） | 功能入口置灰 + hover 说明 | {{文案}} |
| 字典加载失败 | 显示 code 原值 + toast 提示 | — |
| 数据为空（对象不存在） | 空态页面 + 返回入口 | {{文案}} |

---

## 5. 字段展示通用规则

> 写这个端跨模块统一的字段展示口径。页面级字段默认继承以下规则，有特殊需求必须在页面规格说明原因。

| 字段类型 | {{端}}展示要求 | 说明 |
|----------|----------------|------|
| 数据库内部主键 ID | {{如：管理后台列表、详情、搜索条件、导出字段、详情路由均不展示或直接使用}} | 页面需要定位记录时使用业务编号、UUID、订单号、流水号等业务标识；模块需在端内定义补充具体映射 |
| 枚举 / 状态 | {{如：只展示中文名称}} | code 仅作为接口/字典内部值或文档引用 ID |
| datetime 时间 | {{如：yyyy-MM-dd HH:mm:ss}} | 列表、详情、导出等展示场景统一到同一格式 |

---

## 6. 端级文案 key 表

> 只登记跨多个模块复用的端级文案 key。跨端或多页面复用的单模块文案使用 `M{{模块号}}-TXT-*`，放模块公共定义；仅某一端内部复用的 UI 文案可放模块端内定义。
> 后台文案和移动端文案分开维护，但不要在全局端专属层登记单模块文案。

| 文案 ID | 默认文案 | 所属场景 | 后台可配 | 备注 |
|---------|----------|----------|----------|------|
| `GLB-TXT-{{标识}}` | {{默认文案}} | {{跨模块场景}} | 是/否 | 模块文案不要写在本文 |
