# 成家立业移动端细化 PRD-05

## 1. 文档信息

- 文档主题：推荐模块（朋友、社区与内容互动）
- 对应原始关联模块：
  - `推荐（朋友）`
  - `推荐（社区）`
  - `社区动态`
  - `关注/粉丝`
- 关联前置文档：
  - `细化PRD-01_用户准入与资料认证初始化`
  - `细化PRD-03_消息、私信与通知中心`
  - `细化PRD-04_商业化（VIP、成家币、解锁与资产中心）`
- 目标：输出一份可直接支撑产品设计、前后端开发、联调、测试的细化 PRD

## 2. 模块定位

本模块负责承接 `推荐` 一级导航下的非算法社交与内容场景，把分散在 `朋友`、`社区`、`用户主页动态` 中的内容消费和轻互动统一收口为：

1. `朋友场景`：
   承接弱关系兴趣社交，包括 `觅知音`、`悦目`、`诚意贴`。
2. `社区场景`：
   承接公开图文内容、话题参与、点赞评论、举报治理。
3. `关系延伸场景`：
   承接关注/粉丝、个人动态展示、内容驱动的主页访问。

它承担三类职责：

1. 内容消费职责：
   提供公开图文与兴趣内容浏览，增强留存与活跃。
2. 轻社交职责：
   通过关注、点赞、评论、话题参与建立弱关系连接。
3. 风险隔离职责：
   在不突破前序 PRD 约束的前提下，隔离内容互动与恋爱匹配、私信链路。

## 3. 设计原则

1. 与匹配解耦：本模块不展开推荐算法排序逻辑，只定义前台内容形态、互动规则和审核链路。
2. 浏览优先、重互动慎开放：优先保证公开内容浏览，发布和高风险互动遵循认证门槛。
3. 公开内容与私域聊天隔离：关注、点赞、评论不直接等价于私信开口条件。
4. 审核前置：发布、评论、举报、处罚都必须纳入审核或风控链路。
5. 配置优先：话题、举报原因、空态文案、发帖限制、联系方式开关等均支持后台配置。
6. 机审优先：所有 UGC 文本与图片内容，首版先对接微信小程序官方内容安全能力，再进入人工抽检或人工审核链路。

## 4. 页面范围

本模块涉及以下页面：

1. `推荐首页容器页`
2. `朋友 - 觅知音页`
3. `朋友 - 悦目页`
4. `朋友 - 诚意贴列表页`
5. `诚意贴详情页`
6. `社区首页`
7. `发布动态页`
8. `动态详情页`
9. `话题聚合页`
10. `我的动态 / 用户主页动态区`
11. `关注列表 / 粉丝列表`
12. `举报弹窗 / 举报结果承接`

## 5. 与 PRD-01、PRD-03、PRD-04 的关系

本模块严格继承以下已确认规则：

1. 未完成三项认证前：
   - 不开放社区动态发布
   - 不进入恋爱匹配与真实聊天链路
2. 普通私信仅在 `匹配成功后` 开放，本模块中的 `关注` 不直接解锁私信。
3. `悄悄话`、`VIP`、`成家币` 规则仍以后续消息和商业化模块为准，本模块不新增平行私信规则。
4. `充值成功 / 成家币变动 / VIP 到期提醒` 已在通知模块确认，本模块补充承接 `点赞 / 评论 / 关注 / @ / 审核结果` 等内容互动类通知。

补充明确：

1. 推荐与社区内容浏览对已登录用户开放。
2. 发布动态、发布诚意贴、发表评论、@用户、关注他人等互动动作，首版按 `三项认证全部通过后开放` 设计。
3. 举报动作允许已登录用户发起，用于提前形成安全治理闭环。

## 6. 用户流程总览

## 6.1 推荐首页浏览链路

1. 用户点击底部 `推荐` Tab
2. 进入推荐首页容器
3. 顶部切换：
   - `朋友`
   - `社区`
4. 用户按卡片流或列表流浏览内容
5. 点击头像或内容卡片进入详情页或用户主页

## 6.2 内容发布链路

1. 用户点击 `发布`
2. 系统校验登录状态与三项认证状态
3. 进入发布页填写文本、图片、话题、@用户
4. 提交后进入审核判断：
   - 机审拦截直接驳回
   - 机审通过则按内容类型进入发布或抽检
5. 发布结果通过站内通知反馈

## 6.3 内容互动链路

1. 用户在列表页或详情页执行：
   - 点赞
   - 评论
   - 关注
   - 举报
2. 系统校验互动资格
3. 成功后更新前台状态
4. 对被互动方生成对应通知

## 7. 功能边界

### 7.1 本模块产出

1. 朋友内容浏览
2. 社区动态浏览
3. 动态与诚意贴发布
4. 点赞、评论、关注、举报
5. 话题参与
6. 关注/粉丝列表
7. 内容审核结果前台承接

### 7.2 本模块不展开

1. 推荐排序算法
2. 图片消息 / 语音消息 / 实时聊天室
3. 短视频内容形态
4. 内容商业投流细则
5. 任务激励、勋章体系
6. 官方活动运营后台

## 8. 数据对象定义

## 8.1 动态对象 `Post`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| postId | Long | 是 | 动态 ID |
| authorId | Long | 是 | 作者用户 ID |
| postType | Enum | 是 | community / sincere_post |
| text | String | 否 | 正文内容 |
| imageList | List<String> | 否 | 图片列表 |
| topicId | Long | 否 | 话题 ID |
| mentionUserIds | List<Long> | 否 | @用户列表 |
| status | Enum | 是 | pending / published / rejected / deleted / blocked |
| likeCount | Int | 是 | 点赞数 |
| commentCount | Int | 是 | 评论数 |
| createdTime | Datetime | 是 | 创建时间 |

## 8.2 评论对象 `Comment`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| commentId | Long | 是 | 评论 ID |
| postId | Long | 是 | 所属动态 ID |
| authorId | Long | 是 | 评论用户 ID |
| content | String | 是 | 评论内容 |
| parentCommentId | Long | 否 | 父评论 ID |
| status | Enum | 是 | pending / published / rejected / deleted / blocked |
| createdTime | Datetime | 是 | 创建时间 |

## 8.3 话题对象 `Topic`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| topicId | Long | 是 | 话题 ID |
| topicName | String | 是 | 话题名称 |
| cover | String | 否 | 话题封面 |
| postCount | Int | 是 | 内容数量 |
| status | Enum | 是 | on / off |

## 8.4 关注关系对象 `FollowRelation`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| relationId | Long | 是 | 关系 ID |
| followerId | Long | 是 | 关注方 |
| targetUserId | Long | 是 | 被关注方 |
| status | Enum | 是 | follow / unfollow / blocked_invalid |
| createdTime | Datetime | 是 | 创建时间 |

## 8.5 举报对象 `ReportRecord`

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| reportId | Long | 是 | 举报 ID |
| reporterId | Long | 是 | 举报人 ID |
| targetType | Enum | 是 | post / comment / user |
| targetId | Long | 是 | 举报对象 ID |
| reasonCode | String | 是 | 举报原因 |
| extraText | String | 否 | 补充说明 |
| status | Enum | 是 | pending / processed / rejected |
| createdTime | Datetime | 是 | 创建时间 |

## 9. 核心状态与规则

## 9.1 准入与互动规则

1. 已登录用户可浏览推荐与社区公开内容。
2. `发布动态`、`发布诚意贴`、`评论`、`关注`、`@用户`、`点赞` 首版统一要求：
   `实名认证 + 头像认证 + 学历认证` 三项全部通过。
3. 举报动作对已登录用户开放。
4. 本模块不提供“因为互相关注而直接开私信”的新规则。
5. 从推荐/社区进入用户主页，只承接主页浏览，不改变恋爱匹配或私信规则。

## 9.2 内容状态规则

1. 动态 / 诚意贴状态包括：
   - `pending`
   - `published`
   - `rejected`
   - `deleted`
   - `blocked`
2. 评论状态包括：
   - `pending`
   - `published`
   - `rejected`
   - `deleted`
   - `blocked`
3. 用户主动删除后：
   - 前台立即不可见
   - 后台保留记录供审核与申诉
4. 被举报并核实违规后：
   - 内容可转为 `blocked`
   - 同步触发违规通知或处罚链路

## 9.3 审核规则

### 9.3.1 微信小程序官方机审能力接入

首版内容机审需优先对接微信小程序官方服务端内容安全能力：

1. 文本内容：
   使用官方 `文本内容安全识别 /wxa/msg_sec_check`
2. 图片内容：
   使用官方 `多媒体内容安全识别 /wxa/media_check_async`
3. 接入方式：
   - 由服务端统一调用
   - 前端只提交内容，不直接调用微信安全接口
4. 适用范围：
   - 社区动态正文
   - 诚意贴标题
   - 诚意贴正文
   - 评论内容
   - 动态图片
   - 诚意贴图片

说明：

1. 微信官方机审只作为首层安全校验，不替代平台自身的人工审核、人工抽检与处罚流程。
2. 如微信官方接口异常、超时或返回不确定状态，内容应进入 `待人工复核`，不直接公开。

### 9.3.2 按内容类型的审核流程

1. 发布动态：
   - 先走微信官方机审
   - 文本先校验 `msg_sec_check`
   - 图片异步校验 `media_check_async`
   - 任一校验不通过则直接驳回
   - 全部通过后可发布，并进入人工抽检队列
2. 诚意贴：
   - 先走微信官方机审
   - 文本与图片均通过后，进入人工审核队列
   - 仅人工审核通过后公开
3. 评论：
   - 先走微信官方文本机审
   - 通过后前台展示
   - 不通过则直接驳回
4. 审核不通过时：
   - 通过通知中心反馈驳回结果
   - 支持用户修改后重新提交
5. 举报后的复核：
   - 已公开内容被举报后，可再次进入人工复核
   - 必要时触发下架、封禁、通知与处罚链路

## 9.4 互动规则

1. 点赞：
   - 需完成三项认证后才可操作
   - 单用户对单条内容仅可点赞一次
   - 再次点击取消点赞
2. 评论：
   - 支持一级评论和回复评论
   - 评论内容需通过机审
3. 关注：
   - 单向关注
   - 互相关注只生成关系标识，不自动开放私信
4. 举报：
   - 举报后提示 `已提交审核`
   - 审核结果通过通知中心返回

## 9.5 内容类型规则

### 9.5.1 觅知音

1. 基于兴趣标签、学校、资料标签展示用户列表
2. 展示内容以人卡+摘要为主
3. 点击头像或卡片进入用户主页

### 9.5.2 悦目

1. 双列瀑布流展示公开动态首图
2. 卡片展示：
   - 图片
   - 作者头像
   - 点赞数
3. 点击进入动态详情页

### 9.5.3 诚意贴

1. 必选话题
2. 必填标题
3. 正文不少于 `20` 字
4. 图片最多 `9` 张
5. 是否允许留联系方式由后台开关控制
6. 首版默认不允许留联系方式，后台可开启
7. 首版公开前需先完成微信官方机审
8. 微信机审通过后，再进入人工审核并通过后公开

### 9.5.4 社区动态

1. 文本最多 `500` 字
2. 图片最多 `9` 张
3. 必选 `1` 个话题
4. 支持 `@用户`
5. 发布后先走微信官方机审
6. 机审通过后发布，并进入人工抽检
7. `@用户` 仅支持在发布动态时使用，每条最多 `5` 人，并向被 @ 用户发送通知

## 9.6 通知规则

本模块新增承接以下通知类型：

1. 收到点赞
2. 收到评论
3. 收到评论回复
4. 收到新关注
5. 被 @ 提及
6. 动态审核结果
7. 诚意贴审核结果
8. 举报处理结果

说明：

1. 以上通知类型正式纳入 `消息、私信与通知中心`。
2. 是否展示为独立分组、是否计入总红点，由通知中心统一承接。

## 10. 页面级详细需求

## 10.1 推荐首页容器页

### 10.1.1 页面目标

统一承接 `朋友` 和 `社区` 两类内容场景，作为推荐一级导航下的总入口。

### 10.1.2 页面结构

1. 顶部二级切换：
   - 朋友
   - 社区
2. 内容流区域
3. 右下角发布入口

### 10.1.3 交互规则

1. 默认进入上次停留的二级 Tab
2. 切换 Tab 时保留各自滚动位置
3. 发布入口按当前子场景决定跳转：
   - 社区 -> 发布动态
   - 朋友 -> 发布诚意贴

## 10.2 朋友 - 觅知音页

### 10.2.1 页面目标

承接弱关系兴趣匹配用户列表，不走恋爱推荐规则。

### 10.2.2 列表卡片

1. 头像
2. 昵称
3. 学校
4. 共同兴趣标签高亮
5. 最新动态摘要

### 10.2.3 操作

1. 点击头像进入主页
2. 点击卡片进入主页
3. 点击关注发起关注

## 10.3 朋友 - 悦目页

### 10.3.1 页面目标

以公开动态首图形成轻内容消费场景。

### 10.3.2 页面结构

1. 双列瀑布流
2. 卡片内展示图片 + 点赞数
3. 下拉刷新、上拉加载更多

### 10.3.3 交互

1. 点击卡片进入动态详情
2. 点击头像进入用户主页

## 10.4 朋友 - 诚意贴列表页

### 10.4.1 页面目标

承接带明确话题目的的长文社交内容。

### 10.4.2 列表项

1. 话题标签
2. 标题
3. 正文摘要
4. 图片缩略图
5. 点赞数
6. 评论数
7. 作者信息

### 10.4.3 交互

1. 点击进入详情
2. 点击话题进入话题聚合页
3. 点击发布进入发帖页

## 10.5 社区首页

### 10.5.1 页面目标

承接公开图文动态内容流。

### 10.5.2 页面结构

1. 顶部排序切换：
   - 推荐
   - 最新
2. 信息流列表
3. 发布入口

排序说明：

1. 首版保留 `推荐 + 最新` 双入口
2. `推荐` 排序在推荐算法未落地前，先采用规则流或后台人工配置兜底

### 10.5.3 卡片内容

1. 头像
2. 昵称
3. 文本摘要
4. 图片预览
5. 话题标签
6. 点赞数
7. 评论数

## 10.6 发布动态页

### 10.6.1 页面目标

承接社区动态发布。

### 10.6.2 表单项

1. 文本输入框
2. 图片上传
3. 话题选择
4. @用户选择

审核说明：

1. 文本提交后服务端先调用微信官方 `msg_sec_check`
2. 图片上传后服务端再发起 `media_check_async`
3. 仅在全部机审通过后，内容才允许进入后续发布或人工审核流程

### 10.6.3 校验规则

1. 文本最多 `500` 字
2. 图片最多 `9` 张
3. 必选 `1` 个话题
4. 未完成三项认证不可提交
5. 话题仅允许从后台预置话题中选择，用户不可临时创建新话题

## 10.7 动态详情页

### 10.7.1 页面目标

展示完整内容与评论互动。

### 10.7.2 页面结构

1. 作者信息区
2. 正文与图片区
3. 话题区
4. 点赞 / 评论 / 举报操作区
5. 评论列表区

### 10.7.3 交互

1. 点赞
2. 评论
3. 回复评论
4. 举报
5. 点击作者头像进入主页

## 10.8 话题聚合页

### 10.8.1 页面目标

汇总展示同一话题下的动态与诚意贴内容。

### 10.8.2 页面结构

1. 话题头图与简介
2. 内容列表
3. 参与人数 / 内容数

## 10.9 我的动态 / 用户主页动态区

### 10.9.1 页面目标

承接个人公开内容展示与自管理。

### 10.9.2 功能

1. 查看本人已发布动态
2. 删除本人动态
3. 编辑本人动态文字
4. 查看审核中 / 驳回状态

说明：

1. 首版仅支持编辑文字，不支持替换已发布图片或修改话题。
2. 动态编辑后需重新走微信官方机审。

## 10.10 关注列表 / 粉丝列表

### 10.10.1 页面目标

承接关注关系展示与取消关注操作。

### 10.10.2 列表项

1. 头像
2. 昵称
3. 学校
4. 关注状态
5. 是否互关

### 10.10.3 交互

1. 点击进入主页
2. 取消关注
3. 回关

## 11. 接口草案

1. `GET /api/recommend/friends/list`
2. `GET /api/recommend/yuemu/list`
3. `GET /api/recommend/sincere-post/list`
4. `GET /api/community/feed`
5. `POST /api/community/post/create`
6. `POST /api/community/post/update`
7. `POST /api/community/post/delete`
8. `GET /api/community/post/detail`
9. `POST /api/community/comment/create`
10. `POST /api/community/comment/delete`
11. `POST /api/community/like/toggle`
12. `POST /api/user/follow/toggle`
13. `POST /api/report/submit`
14. `GET /api/topic/detail`
15. `GET /api/topic/posts`

## 12. 返回结构建议

## 12.1 动态详情

```json
{
  "postId": 1001,
  "authorId": 2001,
  "postType": "community",
  "text": "今天图书馆自习打卡",
  "imageList": ["https://img.example.com/1.jpg"],
  "topicId": 88,
  "status": "published",
  "likeCount": 32,
  "commentCount": 6,
  "createdTime": "2026-05-09 10:00:00"
}
```

## 12.2 评论列表

```json
{
  "total": 2,
  "list": [
    {
      "commentId": 9001,
      "postId": 1001,
      "authorId": 2002,
      "content": "同校同学加油",
      "status": "published",
      "createdTime": "2026-05-09 10:03:00"
    }
  ]
}
```

## 13. 异常与边界场景

1. 未完成三项认证用户点击发布：
   提示先完成认证，不进入发布链路
2. 用户内容微信官方机审未通过：
   内容不公开，通知中心反馈驳回原因
3. 用户内容机审接口异常：
   内容进入待人工复核，不直接公开
4. 用户删除动态：
   前台立即不可见，后台保留审计记录
5. 被举报内容核实违规：
   内容下架，并按处罚规则处理作者
6. 用户互相关注：
   只更新关系状态，不自动开放私信
7. 评论作者被封禁：
   历史评论按后台规则处理，新增评论不可再发送
8. 话题被下线：
   历史内容保留，但话题入口不再允许新增参与

## 14. 机审能力调研结论

结合微信小程序官方文档，首版可明确写入的官方平台能力如下：

1. 微信小程序服务端提供 `文本内容安全识别 /wxa/msg_sec_check`
2. 微信小程序服务端提供 `多媒体内容安全识别 /wxa/media_check_async`
3. 因此本项目中的动态、诚意贴、评论审核，应统一采用：
   - 文本先走官方文本安全识别
   - 图片先走官方多媒体安全识别
   - 再进入业务侧人工抽检 / 人工审核 / 举报复核流程

## 15. 埋点建议

1. `recommend_tab_show`
2. `friends_tab_show`
3. `community_tab_show`
4. `post_publish_click`
5. `post_publish_success`
6. `post_like_click`
7. `post_comment_submit`
8. `follow_click`
9. `topic_detail_show`
10. `report_submit`

## 16. 首版研发建议

### 16.1 必做

1. 推荐首页容器
2. 觅知音
3. 悦目
4. 诚意贴列表与详情
5. 社区首页
6. 发布动态
7. 动态详情
8. 点赞、评论、关注、举报
9. 话题聚合页
10. 关注/粉丝列表

### 16.2 可延后

1. 复杂话题运营页
2. 评论楼中楼深层结构
3. 动态二次编辑历史
4. 内容草稿箱
5. 社区商业投流能力

## 17. 待甲方确认清单

| 编号 | 待确认项 | 当前不清晰点 | 我的意见 | 甲方答案 |
| --- | --- | --- | --- | --- |
| 1 | 推荐与社区模块的浏览准入 | 当前已明确未完成三项认证前不能发布社区动态，但是否允许浏览推荐/社区公开内容没有完全写死。 | 建议首版统一为：`已登录即可浏览推荐和社区公开内容`，不要求三项认证。 | 采纳我的意见。 |
| 2 | 发布、评论、关注的认证门槛 | 当前总 PRD 只明确“未完成三项认证前，不开放社区动态发布”，但评论、关注、@用户是否也要受同样门槛限制没有完全写死。 | 建议首版统一为：`发布动态、发布诚意贴、评论、关注、@用户` 均要求三项认证全部通过。 | 采纳我的意见。 |
| 3 | 点赞是否要求三项认证 | 若点赞对未认证用户开放，会带来一套更宽松的互动边界；若关闭，又会降低内容场景的轻互动活跃。 | 建议首版统一为：`点赞也要求三项认证通过`，保持互动口径一致。 | 采纳我的意见。 |
| 4 | 互相关注是否开放私信 | 总 PRD 旧口径里出现过“互相关注后可免费私信”，但 PRD-03 已确认普通私信仅在匹配成功后开放。这里需要彻底统一。 | 建议首版统一为：`互相关注仅形成关系标识，不开放普通私信`。 | 采纳我的意见。 |
| 5 | 社区首页默认排序 | 总 PRD 写了“默认推荐算法排序，可切换最新”，但当前阶段推荐算法暂缓，首版是否还做“推荐/最新”双排序没有完全拍板。 | 建议首版保留：`推荐 + 最新` 双入口，但推荐排序先用规则流/人工配置兜底，不依赖复杂算法。 | 采纳我的意见。 |
| 6 | 诚意贴是否必须人工审核后公开 | 总 PRD 写的是“需人工审核后公开展示”，而普通社区动态是“机审通过即时可见，进入人工抽检队列”。两类内容的审核门槛不同，需要继续确认。 | 建议首版统一为：`诚意贴人工审核通过后公开；普通社区动态机审通过即可展示，进入人工抽检`。 | 采纳我的意见。 |
| 7 | 动态编辑范围 | 总 PRD 里写了“发布者可编辑文字（图片不可编辑）”，但是否允许修改话题、是否有编辑时限没有完全写死。 | 建议首版统一为：`仅允许编辑文字，不允许修改图片和话题；编辑后重新走机审`。 | 采纳我的意见。 |
| 8 | 评论能力范围 | 当前只明确了评论与机审，但是否支持二级回复、是否支持删除自己的评论、是否支持楼中楼没有完全写死。 | 建议首版统一为：`支持一级评论和回复评论；支持删除自己的评论；不做复杂楼中楼`。 | 采纳我的意见。 |
| 9 | @用户的能力边界 | 总 PRD 提到了发布动态可 @用户，但没有明确：是否支持评论里 @用户、最多可 @几人、是否触发通知。 | 建议首版统一为：`仅发布动态时支持 @用户；每条最多 @5 人；被 @用户收到通知`。 | 采纳我的意见。 |
| 10 | 话题体系来源 | 当前文档写了“必选话题，可搜索”，但没有明确话题是否只允许后台预置，还是允许用户临时创建新话题。 | 建议首版统一为：`话题由后台统一维护，用户不可自由创建新话题`。 | 采纳我的意见。 |
| 11 | 诚意贴是否允许留联系方式 | 总 PRD 已提到“后台可配是否允许留联系方式”，但是否首版默认允许没有确认。 | 建议首版统一为：`默认不允许，保留后台开关`。 | 采纳我的意见。 |
| 12 | 互动通知是否全部纳入消息中心 | PRD-03 已为社区相关通知预留了位置，但点赞、评论、关注、@ 的通知是否全部首版纳入，需要在这里正式确认。 | 建议首版统一纳入：`点赞、评论、评论回复、关注、@、动态审核结果、诚意贴审核结果、举报处理结果`。 | 采纳我的意见。 |
