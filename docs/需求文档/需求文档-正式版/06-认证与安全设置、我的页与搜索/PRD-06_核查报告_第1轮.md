我已读完所有必读文件，现输出核查报告。

---

# PRD-06 核查报告 - 第 1 轮

## 结论
- **门禁结论**：有条件通过
- **P0 数量**：3
- **P1 数量**：5
- **P2 数量**：3

## P0 问题

| 编号 | 文件路径 | 问题 | 影响 | 建议 |
|------|----------|------|------|------|
| P0-01 | `管理后台/页面规格/` | **安全提示内容配置（`ADM-06-PAGE-safety-content-config`）页面规格缺失**。端内定义和模块 PRD 均列出该页面，但 `页面规格/` 目录下无对应 `.md` 文件。移动端帮助与客服页、关于我们页依赖该后台配置的安全提示 H5 链接。 | Demo 和技术方案无法确定该页面的字段、操作、布局；UI 无画板依据。 | 补写 `安全提示内容配置.md`，参照 `公告与协议配置.md` 结构，字段覆盖：标题、内容类型（反诈指南/安全提示/平台规范）、跳转方式、排序、启停。 |
| P0-02 | `移动端/页面规格/APP-01_我的页.md` 等多文件 | **部分核心验收标准缺少完整 Given/When/Then**。例如 `APP-06-AC-my-login-state`、`APP-06-AC-my-cert-guide`、`APP-06-AC-my-dependency-fallback` 只有 AC ID 和场景摘要，未写 Given/When/Then 模板。违反 PRD 编写规范 §8。涉及至少 5 个页面。 | QA 无法直接转测试用例；AI 编码时"完成"无定义。 | 每个 P0 AC 补写 Given/When/Then，至少覆盖：未登录→我的页→引导登录；已登录未认证→我的页→认证引导卡；跨模块未就绪→降级。 |
| P0-03 | `PRD-06_模块公共定义.md` §4.2 + `移动端/页面规格/APP-07_账号注销确认弹窗.md` | **注销状态机 `blocked→requested` 流转路径与弹窗阻断逻辑存在歧义**。状态机写 `blocked` 可以通过"阻断原因解除后重新提交"进入 `requested`，但弹窗操作表写硬阻断时确认按钮禁用且只能联系客服——没有"解除阻断后重新提交"的 UI 路径。同时 `none→blocked` 和 `none→requested` 的分叉依赖后端校验结果，弹窗未区分"首次提交即被阻断"和"阻断后再次尝试"两种场景。 | 前后端状态流转不一致，开发时会在"用户被阻断后如何重试"上产生分歧。 | 明确：①弹窗打开时先展示校验结果，硬阻断→只展示原因+客服入口（不创建申请）；②仅命中可确认风险→允许勾选确认后提交（创建 requested）；③`blocked` 状态下用户再次进入弹窗时重新校验，若阻断已解除则走正常流程。 |

## P1 问题

| 编号 | 文件路径 | 问题 | 影响 | 建议 |
|------|----------|------|------|------|
| P1-01 | `移动端/模块PRD文档/模块PRD_APP-06...md` §4 | **核心数据模型缺少注销阻断原因的存储结构**。`account_cancellation_request` 表有 `blockReasons` 字段但未说明是 JSON 数组还是逗号分隔、每个阻断项的字段结构（类型、来源模块、是否可自动解除）。 | 数据库设计和接口字段定义时需要二次沟通。 | 补充 `blockReasons` 的 JSON schema，至少包含 `type`（penalty/refund/dispute/vip）、`sourceModule`（PRD-04/风控）、`autoResolvable`（bool）。 |
| P1-02 | `移动端/页面规格/APP-05_设置页.md` | **设置页入口列表字段 `M06-CFG-settings-entry-list` 为 json[]，但设置页实际入口只有 5 个固定项**（手机号、微信、隐私、合规清单×2、关于我们），用完整配置表略显过度。且设置页规格 §4.1 字段表将设置入口定义为"列表字段"但页面本身不是列表。 | 技术方案可能过度设计；前后端对接口返回结构预期不一致。 | 明确设置页接口 `/api/app/settings` 返回固定字段结构（phone/wechatBound/entries/accountStatus），entries 仅为可配置的合规清单和关于我们链接。 |
| P1-03 | `管理后台/页面规格/搜索配置.md`、`一期上线目标.md` | **搜索配置页面优先级标记为 P1，但一期上线目标 ADM-MVP-032 明确为必做**。端内定义也将该页面标为 P1。同属移动端配置管理的公告协议配置为 P0，但搜索配置降为 P1。 | 若严格按 P1 排期，一期搜索 Tab 启停和空态文案可能无法按时上线。 | 确认搜索配置是否一期必做；若是，将优先级改为 P0 与一期上线目标对齐。 |
| P1-04 | 全部移动端页面规格 | **所有页面"对应设计稿"均为"当前未绑定蓝湖画板"**。虽然有 ASCII 线框和画板拆分表，但 UI 设计师缺少 Figma 链接作为起始参考。 | UI 从零出稿周期不可控；设计还原验证缺少基准。 | 优先为 P0 页面（我的页、设置页、搜索首页/结果页、反馈箱、注销弹窗）绑定蓝湖画板或提供参考设计稿链接。 |
| P1-05 | `移动端/页面规格/APP-12_搜索首页.md` §3.1 | **搜索字段描述模糊**："由 sourceScene 决定，默认用户昵称/学校/职业摘要、动态标题/正文、话题名称"。职业摘要的实际搜索字段（职业名称？公司？行业？）未定义；不同 sourceScene 各自搜哪些字段缺少对照表。 | 搜索接口设计和索引建立时无法确定字段范围。 | 按 sourceScene 列出搜索字段对照表：global→用户(昵称/学校/成家号)、动态(标题/正文)、话题(名称/简介)；community→动态+话题（不搜用户）。 |

## P2 问题

| 编号 | 文件路径 | 问题 | 影响 | 建议 |
|------|----------|------|------|------|
| P2-01 | `PRD-06_甲方前置准备清单.md` | **8 项必须提供项中 6 项（合规文档、客服链接、反馈类型、热词、屏蔽词、安全提示）当前内容为空**，标注"未提供时使用占位/默认值"。 | 不阻塞 Demo 和技术方案，但阻塞上线验收——合规文档缺位会导致小程序审核被拒。 | 将 M06-RISK-01（合规文档未定稿）提升为上线门禁，在甲方未提供前标记上线阻塞。 |
| P2-02 | `移动端/模块PRD文档/模块PRD_APP-06...md` §4.1 | **跨模块字段引用表写"会员状态、千寻币余额→PRD-04未就绪时隐藏角标"，但我的页字段表仍列出 `vip-summary` 和 `coin-balance` 为必填**。字段表必填=是，但数据来源 PRD-04 未就绪时"隐藏"，前后矛盾。 | 开发时对接口是否必须返回这些字段产生疑问。 | 统一口径：若 PRD-04 未就绪，我的页接口中这两个字段返回 null，前端隐藏对应 UI 元素。在字段表"必填"列改为"条件必填（PRD-04 已上线时）"。 |
| P2-03 | `管理后台/页面规格/App用户管理-设置字段补充.md` | **"最近搜索时间"来源写"搜索日志摘要"，但模块公共定义中无搜索日志实体定义**。端内定义也未说明搜索日志的保留策略和聚合方式。 | 技术方案设计数据库时需要额外补充搜索日志表。 | 在模块公共定义 §4 数据模型中增加搜索日志摘要说明：统计口径（最近 30 天）、更新方式（异步聚合）、数据来源（搜索服务日志）。 |

## 找茬清单闭环评价

| 问题范围 | 评价 | 遗漏 |
|----------|------|------|
| M06 系列（16 项） | ✅ **全部闭环**。M06-01~M06-16 均在模块公共定义 §1 中有明确结论和文档落点，无未处理项。 | 无 |
| A06 系列（7 项） | ✅ **全部闭环**。A06-01~A06-07（含 A06-04 与 M06-07 合并）均有处理结论。A06-06 明确"客服不代改设置"。 | 无 |
| C 系列（涉及 PRD-06 的 C-02/05/08/10/12） | ✅ **全部闭环**。C-02（底部 Tab）、C-05（demo 入口隐藏）、C-08（算法开关不纳入）、C-10（注销数据处置）、C-12（熟人屏蔽不做）均有对应规则落点。 | 无 |
| 一期目标 §10 设置页收敛口径 | ✅ **全部一致**。正式版设置页与一期目标 §10 的 11 项收敛口径完全对齐，无遗漏或扩大。 | 无 |

**评价**：找茬清单 PRD-06 相关的所有 M06/A06/C 类问题均有明确的处理结论并回写到模块公共定义，闭环完整。一期目标中的边界收敛全部在正式版中体现。

## 页面规格完整性抽查

| 端 | 覆盖页面 | 评价 | 风险 |
|----|----------|------|------|
| 移动端 | 14 个页面规格（含弹窗）全部到位 | ✅ 字段表 10 列、操作表 8 列、状态 6 类、Given/When/Then 骨架基本完整 | P0-02：部分 AC 缺完整 Given/When/Then；P1-04：全部缺设计稿链接 |
| 管理后台 | 9/10 个页面规格到位，**安全提示内容配置缺失** | ⚠️ 缺失 1 个页面规格（P0-01） | P0-01：Demo/技术方案阻塞；其余 9 个页面规格结构完整 |

## Demo / 技术方案门禁

- **可进入静态 Demo**：有条件 — 需先补齐 P0-01（安全提示内容配置页面规格）；P0-02（AC 补全）和 P1-04（设计稿链接）不阻塞 Demo，但建议同步推进
- **可进入技术方案**：有条件 — P0-01 阻塞后台表设计和接口；P0-03（注销状态机歧义）需在技术方案前澄清；P1-01（阻断原因 JSON schema）建议在数据模型设计前补全
- **必须先整改**：
  1. **P0-01**：补写 `安全提示内容配置.md` 页面规格
  2. **P0-03**：澄清注销状态机 `blocked→requested` 的 UI 路径和重试机制
  3. **P0-02**：为核心流程 AC 补写完整 Given/When/Then（至少登录引导、认证引导、注销阻断三条）
