Now I have all the data needed to compile the review. Let me organize and output the report.

---

# PRD-03 第一轮核查报告

## 1. 读取范围

实际读取文件共 20 个：

**规范文件（3）：**
- `.claude/skills/prd-design/SKILL.md`
- `docs/需求文档/标准/00_PRD编写规范.md`
- `docs/需求文档/标准/05_PRD评审清单.md`

**原始资料（3）：**
- `docs/需求文档/移动端/细化PRD-03_消息、私信与通知中心.md`
- `docs/需求文档/管理后台/管理后台细化PRD-03_消息、私信与通知中心.md`
- `docs/需求文档/一期上线目标.md`

**正式版核心文件（6）：**
- `PRD-03_模块公共定义.md`
- `PRD-03_甲方前置准备清单.md`
- `APP-03_端内定义.md`
- `ADM-03_端内定义.md`
- `模块PRD_APP-03_消息、私信与通知中心.md`
- `模块PRD_ADM-03_消息私信通知后台承接.md`

**页面规格抽查（8）：**
- `APP-01_消息列表页.md`
- `APP-02_私信对话页.md`
- `APP-05_悄悄话消息页.md`
- `APP-06_通知中心页.md`
- `App用户详情-消息互动区块.md`
- `消息通知记录查询页.md`
- `社交权限与消息配置页.md`
- `举报处理-聊天举报字段补充.md`

## 2. 结构概览

| 原始 PRD 需求 | 正式版承接 | 状态 |
|--------------|-----------|------|
| 消息列表页（原始 10.1） | `APP-03-PAGE-message-list` → `APP-01_消息列表页.md` | ✅ 已覆盖 |
| 私信对话页（原始 10.2） | `APP-03-PAGE-private-chat` → `APP-02_私信对话页.md` | ✅ 已覆盖 |
| 悄悄话卡片/回复态（原始 10.3） | `APP-03-PAGE-whisper-message` → `APP-05_悄悄话消息页.md` | ✅ 已覆盖 |
| 通知中心页（原始 10.4） | `APP-03-PAGE-notification-center` → `APP-06_通知中心页.md` | ✅ 已覆盖 |
| 通知管理页（原始 10.5） | 按一期目标移除，`APP-03_端内定义.md` 1.1 节标"隐藏" | ✅ 正确移除 |
| 官方助手/官方消息（一期目标） | `APP-03-PAGE-official-assistant` + `APP-03-PAGE-official-message-detail` | ⚠️ 未被抽查 |
| 邀请响应（一期目标 MVP-PAGE-040） | `APP-03-PAGE-invite-response` → `APP-08_邀请响应页.md` | ⚠️ 未被抽查 |
| 用户列表消息字段补充（原始后台 4.1.1） | `ADM-03-PAGE-user-list-message-fields` | ⚠️ 未被抽查 |
| 用户详情消息互动（原始后台 4.1.2） | `ADM-03-PAGE-user-message-section` → `App用户详情-消息互动区块.md` | ✅ 已覆盖 |
| 消息通知记录查询（原始后台 4.1.2D） | `ADM-03-PAGE-message-record-query` → `消息通知记录查询页.md` | ✅ 已覆盖 |
| 社交权限与消息配置（原始后台 4.3/4.4） | `ADM-03-PAGE-message-config` → `社交权限与消息配置页.md` | ✅ 已覆盖 |
| 聊天举报字段补充（原始后台 4.2.1） | `ADM-03-PAGE-report-chat-fields` → `举报处理-聊天举报字段补充.md` | ✅ 已覆盖 |
| 文案与消息中心模板（原始后台 4.2.2） | `ADM-GLB-PAGE-copy-message-center`（复用全局） | ✅ 已声明 |

## 3. P0 关键问题

| 编号 | 问题 | 涉及文件 | 影响 | 建议处理方式 |
|------|------|----------|------|-------------|
| P0-01 | **模块PRD页面清单中的 `App用户管理列表-消息字段补充.md` 未被纳入本次核查范围，无法确认其是否存在及合规**。ADM-03 端内定义（第1节）和 ADM-03 模块PRD（第8节）将 `ADM-03-PAGE-user-list-message-fields` 标记为 P0，路径 `页面规格/App用户管理列表-消息字段补充.md`。原始后台PRD-03（第4.1.1节）详细定义了用户列表的搜索条件和展示字段，正式版必须在页面规格中落地。 | `ADM-03_端内定义.md:31`、`模块PRD_ADM-03_消息私信通知后台承接.md:154` | 管理后台用户列表无法承接消息字段（有效会话数、悄悄话待回复数等），运营无法在列表层面快速筛选消息状态。属于P0遗漏风险。 | 1) 确认该文件是否已存在但未列入核查清单；2) 若不存在，立即按30_模板创建，字段覆盖原始后台PRD-03第4.1.1节的完整搜索条件和列表字段；3) 补充到核查范围。 |
| P0-02 | **一期上线目标中的 4 个移动端页面未被本次抽查，存在质量盲区**。模块PRD页面清单（第8节）列出 8 个移动端页面，但本次只抽查了 4 个：APP-01（消息列表）、APP-02（私信对话）、APP-05（悄悄话）、APP-06（通知中心）。未抽查的 4 个页面为：`APP-03_官方助手聊天页.md`、`APP-04_官方消息详情页.md`、`APP-07_通知详情页.md`、`APP-08_邀请响应页.md`。这些页面都在一期上线目标（MVP-PAGE-035~040）中，且 `APP-08_邀请响应页` 涉及 PRD-03 与 PRD-07 的关键边界。 | `模块PRD_APP-03_消息、私信与通知中心.md:179-188` | 官方助手、官方消息详情、通知详情、邀请响应4个页面的字段表、操作表、6类状态和验收标准未经核查，无法确认是否满足 PRD 编写规范12条要求。若存在质量问题（如只写字段名、缺操作表、未穷举状态），将在开发阶段暴露。 | 1) 在下轮核查中纳入这4个页面；2) 重点检查 APP-08 邀请响应页与 PRD-07 的边界是否清晰（PRD-03 只承接消息展示与跳转）；3) 检查 APP-03 官方助手是否与原始PRD中模糊的"官方助手"概念一致。 |
| P0-03 | **通知中心页面规格中，通知详情跳转未覆盖 `invite_response` 跳转类型的页面表现**。模块公共定义 `M03-ENUM-jump-type` 包含 `invite_response`（跳转到邀请响应页），`M03-ENUM-notification-biz-type` 包含 `invite_response`（邀请响应通知）。但 APP-06 通知中心页规格的状态表、操作表和数据联动规则中，均未描述点击邀请响应类型通知后的具体跳转行为和目标页面。 | `APP-06_通知中心页.md:115-144`、`PRD-03_模块公共定义.md:142-150` | 邀请响应通知的跳转路径存在文档空白：点击后应到 `APP-03-PAGE-invite-response` 还是 PRD-07 的邀请页面？UI和前端各自猜测会导致返工。 | 在 APP-06 通知中心页的数据联动规则中增加一行：`jumpType=invite_response` → 跳转 `APP-03-PAGE-invite-response`。在 APP-08 邀请响应页规格中明确从通知跳转进入时的页面初始状态。 |

## 4. P1 重要偏差

| 编号 | 问题 | 涉及文件 | 影响 | 建议处理方式 |
|------|------|----------|------|-------------|
| P1-01 | **消息列表页字段附加属性表包含不适合移动端的列概念**。APP-01 消息列表页第 4.1 节"列表字段附加属性"包含"列宽"、"是否固定"、"是否可拖拽调整列宽"，这三列是后台管理表格的概念。移动端消息列表使用卡片式布局而非表格，这些属性对 UI 和开发无指导意义。 | `APP-01_消息列表页.md:89-93` | UI 和前端会困惑于"列宽160px"在移动端如何解释。规范层面不严谨。 | 将移动端页面规格的"列表字段附加属性"改为移动端适用的属性集：如"是否主操作区"、"是否支持长按菜单"、"是否支持左滑操作"。或在模板层面区分移动端/后台的字段附加属性列定义。 |
| P1-02 | **后台消息通知记录查询页中"记录类型"筛选选项与模块枚举引用不一致**。筛选条件 `FILTER-record-type` 的选项来源写为"私信/悄悄话/官方消息/通知"，但模块公共定义中对应的枚举是 `M03-ENUM-conversation-type`（会话类型）和 `M03-ENUM-message-type`（消息类型），两者混用。私信/悄悄话是会话级别概念，消息/通知是记录级别概念，筛选维度的语义不一致会导致后端接口设计混乱。 | `消息通知记录查询页.md:88`、`PRD-03_模块公共定义.md:70-98` | 后端开发时不确定"记录类型"筛选应该用哪个枚举、跨表查询如何处理。 | 明确"记录类型"的后台筛选逻辑：按 `M03-ENUM-conversation-type` 筛选会话维度记录，按 `M03-ENUM-message-type` + `M03-ENUM-notification-type` 联合筛选消息/通知维度记录。在字段表中增加此说明。 |
| P1-03 | **悄悄话消息页（发送方视角）缺少等待态下的可执行操作说明**。APP-05 悄悄话消息页的状态表包含 `业务态-pending`（待回复），但在操作表中只定义了接收方的 `ACT-reply` 和 `ACT-ignore`，未定义发送方在 `pending` 状态下是否可查看对方主页、举报或拉黑。原始PRD-03（第10.3.1节）明确"在对方未回复前，不开放普通私信输入，不重复触发匹配成功"，但对其他操作未说明。 | `APP-05_悄悄话消息页.md:113-119` | 发送方在等待回复期间的操作能力不明确，可能遗漏合理的用户操作入口（如查看主页）。 | 在操作表中增加发送方在 `pending` 状态下可执行的操作：查看对方主页（`ACT-view-profile`）、举报（`ACT-report`），并标注"不可重复发送悄悄话"的约束。 |
| P1-04 | **模块公共定义中引用的 `GLB-ROLE-*` 全局角色未在本次核查中验证一致性**。APP-03 端内定义引用 `GLB-ROLE-app-user`、`GLB-ROLE-visitor`；ADM-03 端内定义引用 `GLB-ROLE-customer-service`、`GLB-ROLE-operation`、`GLB-ROLE-auditor`、`GLB-ROLE-risk`、`GLB-ROLE-super-admin`。这些全局角色应定义在全局共享层，但本次未读取全局共享层文件验证角色是否已定义、是否与权限矩阵一致。 | `APP-03_端内定义.md:48-54`、`ADM-03_端内定义.md:53-63` | 若全局共享层缺少对应角色定义，权限矩阵将失去锚点，开发阶段需重新协商权限分配。 | 在全局共享层确认 `GLB-ROLE-app-user`、`GLB-ROLE-visitor`、`GLB-ROLE-customer-service`、`GLB-ROLE-operation`、`GLB-ROLE-auditor`、`GLB-ROLE-risk`、`GLB-ROLE-super-admin` 均已定义。 |
| P1-05 | **原始PRD中的"官方助手"功能定义薄弱，正式版虽拆分了页面但未纳入抽查**。原始移动端PRD-03仅在消息列表页中提到"官方消息"卡片，未对"官方助手"给出独立页面级需求。一期上线目标（MVP-PAGE-035）和正式版模块PRD新增了 `APP-03-PAGE-official-assistant`（官方助手聊天页），但该页面的原始需求基础薄弱——原始PRD没有定义官方助手应展示哪些系统提示、是否可交互、与官方消息详情页的区别是什么。 | `模块PRD_APP-03_消息、私信与通知中心.md:141`、原始PRD-03全文 | UI 出稿和开发阶段对"官方助手聊天页"的理解可能分歧：是纯展示页还是可发送消息？与"官方消息详情页"的区别是否只是入口不同？ | 在下轮核查中审查 APP-03_官方助手聊天页.md，重点确认：1) 是否只做系统提示与客服引导，不接人工客服IM（已在前置准备清单 M03-PREP-07 标记）；2) 与官方消息详情页的字段和操作差异；3) 消息类型是否包含可点击的引导卡片。 |

## 5. P2 轻微问题

| 编号 | 问题 | 涉及文件 | 影响 | 建议处理方式 |
|------|------|----------|------|-------------|
| P2-01 | **所有页面规格的"对应设计稿"均写"待补充"**。符合规范（未出稿时写"待补充；设计画板按第2.x节输出"），但需在UI出稿后同步更新 Figma 链接，确保 C2 维度可评审。 | 全部8个页面规格 | 当前无影响，但若UI出稿后不更新链接，将导致PRD↔设计稿一致性无法自动检查。 | UI定稿后批量更新 Figma 链接。 |
| P2-02 | **通知中心页的类型筛选缺少"邀请响应"的显式选项**。`M03-ENUM-notification-type` 只有 interaction/system/asset/activity 四种，`invite_response` 是 `M03-ENUM-notification-biz-type` 的子类，归属于 interaction。筛选器只按一级类型筛选，用户无法直接筛选邀请响应通知。 | `APP-06_通知中心页.md:77`、`PRD-03_模块公共定义.md:119-138` | 用户体验层面：邀请响应类通知混在互动通知中，不够突出。一期不阻塞。 | 若一期邀请响应是核心转化路径，考虑在通知中心增加"邀请"二级筛选标签或独立入口；否则保持现状。 |
| P2-03 | **模块PRD中"通知详情页"和"邀请响应页"在页面清单中列出但缺少对应的页面规格验证**。APP-07（通知详情页）和 APP-08（邀请响应页）是 P0 页面但未被抽查。 | `模块PRD_APP-03_消息、私信与通知中心.md:187-188` | 与 P0-02 联动，下轮核查需覆盖。 | 纳入下轮核查。 |
| P2-04 | **原始移动端PRD中的消息列表URL路径与正式版不一致**。原始PRD-03使用 `GET /api/message/conversations`，正式版模块公共定义使用 `GET /api/app/message/conversations`（增加了 `/app` 前缀）。这是合理的规范化改进，但原始PRD中接口路径已在前端开发中可能被引用，需在技术方案中明确映射关系。 | `细化PRD-03_消息、私信与通知中心.md:558`、`PRD-03_模块公共定义.md:278` | 若前端已按原始PRD接口路径开发，需做适配。 | 在技术方案文档中列出原始PRD路径到正式版路径的映射表。 |
| P2-05 | **模块公共定义中的"未读汇总"实体在消息列表页的响应结构中与原始PRD略有差异**。正式版 `unreadSummary` 中新增了 `displayText` 字段（"9"），原始PRD只有 `totalUnreadCount`（数字7）。这是一个合理的 UX 优化（预计算展示文案），但需确认前端是否按 `displayText` 还是自行格式化 `totalUnreadCount`。 | `PRD-03_模块公共定义.md:308`、`细化PRD-03_消息、私信与通知中心.md:594` | 前端可能忽略 `displayText` 字段自行格式化，浪费服务端计算。 | 在技术方案中明确：前端直接使用 `displayText` 展示红点数字，不做客户端格式化。 |

## 6. 合理拆分与做得好的地方

1. **文档分层结构完整**：模块公共定义 → 端内定义 × 2 → 模块PRD × 2 → 页面规格 × 15，完全符合 00_PRD编写规范 第3节的分层要求。

2. **一期目标对齐精准**：
   - 正确移除用户自助通知管理页（原始10.5），在端内定义第1.1节和模块PRD第6节明确标注"隐藏，不建页面规格"
   - 正确收敛普通私信开口规则为"匹配成功后开放"，废止"互关即可私信"旧口径（`M03-RULE-private-chat-open`）
   - 正确保留聊天举报 + 悄悄话举报，统一由后台举报处理承接（`M03-RULE-report-handoff`）

3. **模块公共定义质量高**：
   - 8个枚举全部按"值/显示名/说明/排序/是否默认/状态"6列规范定义
   - 3个状态机（会话、悄悄话、通知已读）均有完整流转表，覆盖了异常分支
   - 14条业务规则，每条关联到具体端/页面和判定逻辑
   - 9个配置项，标注了默认值、类型、高风险标记和二次确认要求
   - 10个错误码，每个含 HTTP code、业务code、用户提示文案、是否可重试

4. **跨PRD边界清晰**：
   - PRD-01 核心准入：引用 `M01-RULE-core-access`，未就绪时展示认证引导
   - PRD-02 匹配成功：引用 `M02-SM-mutual-match`，未就绪时私信入口隐藏/置灰
   - PRD-04 商业化：引用 `M04-RULE-whisper-pay`，PRD-03 只定义触发与状态
   - PRD-07 邀请：PRD-03 只承接消息展示和跳转（`APP-03-PAGE-invite-response`）
   - PRD-05 举报处理：聊天举报统一生成举报工单，后台按来源区分上下文

5. **页面规格抽查部分整体达标**：抽查的 8 个页面规格全部覆盖了：
   - 字段表10列（无"只写名字"）
   - 操作表8列（含失败态和权限）
   - 6类状态穷举（加载/空/错误/无权限/业务态/降级）
   - Given/When/Then 验收标准
   - UI画板拆分清单

6. **甲方前置准备清单实用**：9项上线前准备（含微信订阅消息模板、举报原因字典、内容安全策略等）、8项UI设计前置、5项跨PRD承接项，每项有负责人、截止建议和未准备时的处理方式。

## 7. 总结与门禁结论

| 维度 | 评估 |
|------|------|
| 原始需求覆盖率 | 高：移动端8个页面+后台7个承接点，核心链路无遗漏 |
| 一期目标对齐 | 正确：3个关键收敛（移除通知设置、废止互关私信、保留聊天举报）全部落实 |
| PRD规范符合度 | 抽查页面全部达标（字段10列、操作8列、6类状态、Given/When/Then AC） |
| 跨PRD边界 | 清晰：PRD-01/02/04/05/07 均以引用方式对接 |
| P0 问题 | 2 项未闭环：P0-01（文件存在性未确认）+ P0-02（4个页面未被抽查），均属核查覆盖度问题，非文档质量问题 |
| P0-03 | 通知跳转 invite_response 路径缺页面表现描述 |

**门禁结论：有条件通过**

**条件清单：**
1. **P0-01**：确认 `App用户管理列表-消息字段补充.md` 文件存在且按30_模板规范填写；若不存在则创建。
2. **P0-02**：在下轮核查中纳入 APP-03（官方助手）、APP-04（官方消息详情）、APP-07（通知详情）、APP-08（邀请响应）共4个页面规格。
3. **P0-03**：在 APP-06 通知中心页或 APP-08 邀请响应页中补全 `jumpType=invite_response` 的页面表现描述。

以上3项全部闭环后，PRD-03 正式版可评定为**通过**。本次抽查到的8个页面规格整体质量良好，无"只写字段名""只写happy path""待确认蔓延"等上一版PRD的反模式。
