# 页面规格 - APP-05-PAGE-community-greeting 社区打招呼页

| 版本 | 日期 | 修改人 | 变更摘要 |
|------|------|--------|----------|
| 版本01 | 2026-07-06 | Codex | 按一期上线目标补充社区打招呼页 |
| 版本02 | 2026-07-07 | Codex | 按移动端 Demo 审查明确招呼语模板、消耗确认和余额不足状态验收 |
| 版本03 | 2026-07-20 | Codex | 千寻币消耗确认、余额不足和充值入口改为调用模块 08 UI，PRD-05 不重复出稿 |

- **页面 ID**：`APP-05-PAGE-community-greeting`
- **所属模块 PRD**：`模块PRD_APP-05_推荐模块（朋友、社区与内容互动）`
- **页面路由**：`/pages/community/greeting`
- **入口来源**：信息流作者区、动态详情作者区、个人动态区、社区更多操作
- **对应设计稿**：待补充；设计画板按第 2.4 节输出
- **对应移动端 / 技术方案**：`MVP-PAGE-010`、`M03-RULE-whisper-send`

## 1. 页面定位

- **目标用户**：已登录且满足核心准入的用户
- **核心任务**：从社区场景向目标用户发送悄悄话式招呼
- **页面类型**：表单页/半屏页

## 2. 布局（给 UI）

### 2.1 整体布局

```
┌────────────────────────┐
│ 返回  打招呼            │
├────────────────────────┤
│ 目标用户卡片            │
│ 来源内容摘要            │
│ 招呼语选择 / 输入        │
│ 免费次数/千寻币提示      │
│          发送招呼        │
└────────────────────────┘
```

### 2.2 区块说明

| 区块 | 位置 | 内容 | 是否可折叠 | 是否记住展开状态 |
|------|------|------|------------|------------------|
| 目标用户卡片 | 顶部 | 头像、昵称、认证摘要、关系状态 | 否 | 否 |
| 来源上下文 | 中部 | 来源动态/评论/主页摘要 | 否 | 否 |
| 招呼语区 | 中部 | 预设招呼语、文本输入 | 否 | 否 |
| 资产提示 | 底部上方 | 免费次数、千寻币余额、消耗说明 | 否 | 否 |
| 操作区 | 底部 | 发送招呼、取消 | 否 | 否 |

### 2.3 弹层 / 抽屉 / 模态

| 弹层 | 触发方式 | 大小 | 内容 | 关闭方式 |
|------|----------|------|------|----------|
| 发送确认 | 点击发送且需消耗千寻币 | 模块 08 通用资产弹窗 | 消耗数量、余额、确认发送 | 取消/确认 |
| 余额不足提示 | 余额不足 | 模块 08 通用资产弹窗 | 余额不足、去充值 | 取消/去充值 |
| 发送成功反馈 | 发送成功 | toast 或轻弹窗 | 已发送，等待对方回复 | 自动关闭 |

### 2.4 UI 画板拆分（必填）

| 画板 ID | 画板名称 | 设计内容 | 备注 |
|---------|----------|----------|------|
| `APP-05-greeting-01` | 社区打招呼页-默认态 | 目标用户、招呼语、发送按钮 | P0 |
| `APP-05-greeting-04` | 社区打招呼页-待回复态 | 已有待回复悄悄话 | P0 |
| `APP-05-greeting-05` | 社区打招呼页-目标不可用 | 对方状态异常 | P1 |

千寻币消耗确认、余额不足和充值入口由模块 08 提供通用 UI 画板；PRD-05 仅携带业务场景、目标用户和扣费结果调用，不将这些资产弹窗计入本模块画板。

### 2.5 编辑控件口径

| 区域 | 展现形式 | 编辑控件 | 新增/删除方式 | 保存方式 |
|------|----------|----------|----------------|----------|
| 招呼语 | 单选 + 输入 | 单选卡片、多行输入框 | 切换预设或输入自定义内容 | 点击发送时保存 |

## 3. 筛选与搜索

### 3.1 搜索

本页无搜索。

### 3.2 筛选条件

本页无筛选条件。

### 3.3 筛选交互

本页不涉及筛选交互。

## 4. 字段表

### 4.1 列表字段

本页无列表字段。

#### 列表字段附加属性

本页无列表字段附加属性。

### 4.2 详情/表单字段

| 字段 ID | 显示名 | 类型 | 必填 | 取值范围 | 校验规则 | 默认值 | 可编辑 | 敏感级别 | 数据来源 |
|---------|--------|------|------|----------|----------|--------|--------|----------|----------|
| `APP-05-PAGE-community-greeting-FIELD-target-user-id` | 目标用户 | string | 是 | 业务编号 | 目标用户正常且可见 | 无 | 否 | 普通 | 来源页面 |
| `APP-05-PAGE-community-greeting-FIELD-target-profile` | 目标用户摘要 | object | 是 | 头像/昵称/认证摘要 | 昵称头像取已审核版本 | 无 | 否 | 普通 | PRD-01 |
| `APP-05-PAGE-community-greeting-FIELD-source-type` | 来源类型 | enum | 是 | post/comment/profile/yuemu/sincere | 由入口传入 | post | 否 | 普通 | 来源页面 |
| `APP-05-PAGE-community-greeting-FIELD-source-id` | 来源对象 | string | 否 | 业务编号 | 来源存在时必填 | 无 | 否 | 普通 | 来源页面 |
| `APP-05-PAGE-community-greeting-FIELD-template-id` | 招呼语模板 | string | 否 | 模板编号 | 选择预设时必填 | 默认第一条 | 提交前可编辑 | 普通 | PRD-03 |
| `APP-05-PAGE-community-greeting-FIELD-content` | 招呼内容 | string | 是 | 1-200 字 | 内容安全校验，去首尾空格后非空 | 模板文案 | 提交前可编辑 | 敏感，加密存储 | 用户输入 |
| `APP-05-PAGE-community-greeting-FIELD-pay-type` | 消耗方式 | enum | 是 | 免费次数/千寻币 | 引用 PRD-03/PRD-04 返回 | 系统计算 | 否 | 普通 | PRD-03/PRD-04 |
| `APP-05-PAGE-community-greeting-FIELD-balance` | 千寻币余额 | int | 条件必填 | >=0 | 需消耗千寻币时展示 | 0 | 否 | 普通 | PRD-04 |
| `APP-05-PAGE-community-greeting-FIELD-whisper-status` | 悄悄话状态 | enum | 否 | `M03-SM-whisper` | 存在待回复记录时展示 | 无 | 否 | 普通 | PRD-03 |

## 5. 操作表

### 5.1 行级操作

本页无行级列表操作。

### 5.2 批量操作

本页不支持批量操作。

### 5.3 页面级操作

| 操作 ID | 操作名 | 位置 | 触发条件 | 前置权限 | 二次确认 | 成功态 | 失败态 |
|---------|--------|------|----------|----------|----------|--------|--------|
| `APP-05-PAGE-community-greeting-ACT-send` | 发送招呼 | 底部 | 内容非空且目标用户可见 | `M05-RULE-community-greeting-entry`、`M03-RULE-whisper-send` | 需消耗千寻币时确认 | 触发 PRD-03 悄悄话发送并提示等待回复 | `M03-ERR-whisper-duplicate-pending`、`M03-ERR-whisper-quota-insufficient`、内容安全失败 |
| `APP-05-PAGE-community-greeting-ACT-view-profile` | 查看主页 | 用户卡片 | 目标用户正常 | `GLB-ROLE-app-user` | 否 | 跳转用户主页 | `M05-ERR-community-target-unavailable` |
| `APP-05-PAGE-community-greeting-ACT-cancel` | 取消 | 顶部/底部 | 页面打开 | `GLB-ROLE-app-user` | 否 | 返回来源页 | 无 |

## 6. 数据联动规则

| 触发字段 | 触发事件 | 影响字段 | 联动行为 | 备注 |
|----------|----------|----------|----------|------|
| 招呼语模板 | 选择 | 招呼内容 | 自动填入模板文案，可继续编辑 | 引用 PRD-03 |
| payType | 需消耗千寻币 | 发送按钮 | 点击发送前展示消耗确认 | 引用 PRD-04 |
| whisperStatus | pending | 操作区 | 展示等待对方回复，隐藏发送按钮 | `M03-RULE-whisper-repeat-limit` |
| balance | 不足 | 操作区 | 调用模块 08 余额不足与充值入口 UI | PRD-04 提供余额规则，模块 08 提供界面 |
| 目标用户状态 | 异常 | 页面整体 | 禁用发送，展示目标不可用 | `M05-ERR-community-target-unavailable` |

## 7. 状态与异常

| 状态类型 | 触发场景 | 页面表现 | 用户可做的操作 | 引用 |
|----------|----------|----------|----------------|------|
| 加载态 | 首次进入 | 骨架屏 | 等待 | 通用态 |
| 空态（无数据） | 目标用户不存在 | 目标不可用提示 | 返回 | `M05-ERR-community-target-unavailable` |
| 空态（搜索无结果） | 本页无搜索 | 本节不适用 | — | — |
| 错误态（网络） | 加载或提交失败 | toast + 重试 | 重试/返回 | 通用态 |
| 无权限态 | 未登录或未通过核心准入 | 登录/认证引导 | 去登录/去认证 | `M05-RULE-community-greeting-entry` |
| 业务态-可发送 | 资格通过 | 展示发送按钮 | 发送招呼 | `M03-RULE-whisper-send` |
| 业务态-待回复 | 已有 pending 悄悄话 | 展示等待回复 | 查看悄悄话/返回 | `M03-RULE-whisper-repeat-limit` |
| 业务态-余额不足 | 免费次数和余额不足 | 调用模块 08 通用资产弹窗 | 去充值/返回 | `M03-ERR-whisper-quota-insufficient` |
| 降级态 | PRD-03 服务不可用 | 提示稍后再试 | 返回 | PRD-03 |

## 8. 查询与列表

本页为单目标表单页，不提供分页、排序、批量选择或导出。

## 9. 验收标准

| AC ID | 场景 | 类型 | 优先级 |
|-------|------|------|--------|
| `APP-05-AC-community-greeting-send` | 发送社区打招呼 | 正常 | P0 |
| `APP-05-AC-community-greeting-template` | 选择招呼语模板后填入正文 | 正常 | P0 |
| `APP-05-AC-community-greeting-pending` | 已有待回复悄悄话不可重复发送 | 异常 | P0 |
| `APP-05-AC-community-greeting-quota` | 余额或次数不足时拦截 | 异常 | P0 |

```
AC-ID: APP-05-AC-community-greeting-send
Given 用户已登录、满足核心准入，目标用户正常可见，且符合 `M03-RULE-whisper-send`
When  选择招呼语并点击发送
Then  系统触发 PRD-03 悄悄话发送，记录社区来源上下文，并提示等待对方回复
```

```
AC-ID: APP-05-AC-community-greeting-template
Given 页面加载到 PRD-03 返回的招呼语模板
When  用户点击任一模板
Then  正文输入框填入模板文案，用户仍可继续编辑；需消耗千寻币时发送前展示确认
```

```
AC-ID: APP-05-AC-community-greeting-pending
Given 用户对同一目标已有状态为 pending 的悄悄话
When  从社区入口再次进入打招呼页
Then  页面展示等待对方回复提示，不展示发送按钮
```

## 10. 关联

| 关联类型 | 引用 ID | 说明 |
|----------|---------|------|
| 依赖的模块规则 | `M05-RULE-community-greeting-entry` | 社区入口规则 |
| 依赖的模块规则 | `M03-RULE-whisper-send` | 悄悄话发送资格 |
| 依赖的模块规则 | `M03-RULE-whisper-repeat-limit` | 重复发送限制 |
| 依赖的模块 | PRD-04 | 千寻币余额、免费次数与扣费规则 |
| 依赖的模块 | 模块 08 | 千寻币消耗确认、余额不足和充值入口 UI |
| 依赖的其他页面 | `APP-03-PAGE-whisper-message` | 发送后查看悄悄话状态 |

## 11. “申请认识”文案别名

蓝湖中的“申请认识”不是新的关系类型。入口展示“申请认识”时仍携带 `standardAction=greeting` 进入本页，资格、免费次数/千寻币消耗、待回复限制与目标不可用状态全部复用本页和 PRD-03 悄悄话规则。埋点同时记录 `displayLabel=申请认识`，便于比较入口文案效果。
