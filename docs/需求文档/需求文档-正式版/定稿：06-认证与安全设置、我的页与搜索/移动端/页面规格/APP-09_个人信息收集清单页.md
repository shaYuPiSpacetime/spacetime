# 页面规格 - APP-06-PAGE-personal-info-list 个人信息收集清单页

| 版本 | 日期 | 修改人 | 变更摘要 |
|------|------|--------|----------|
| 版本01 | 2026-07-08 | Codex | 创建页面规格 |

- **页面 ID**：`APP-06-PAGE-personal-info-list`
- **所属模块 PRD**：`模块PRD_APP-06_认证与安全设置、我的页与搜索`
- **页面路由**：`/pages/my/compliance/personal-info`
- **入口来源**：设置页 -> 个人信息收集清单
- **对应设计稿**：当前未绑定蓝湖画板；设计画板按第 2.4 节输出
- **对应移动端 / 技术方案**：`MVP-PAGE-067`

## 1. 页面定位

- **目标用户**：访客、App 用户
- **核心任务**：查看平台个人信息收集清单
- **页面类型**：合规内容页

## 2. 布局（给 UI）

### 2.1 整体布局

同 `APP-06-PAGE-third-party-list`：标题/更新时间 + H5 或原生正文。

### 2.2 区块说明

| 区块 | 位置 | 内容 | 是否可折叠 | 是否记住展开状态 |
|------|------|------|------------|------------------|
| 头部 | 顶部 | 标题、版本、生效时间 | 否 | 否 |
| 内容 | 主体 | 信息收集清单正文 | 否 | 否 |

### 2.3 弹层 / 抽屉 / 模态

本页无弹层。

### 2.4 UI 画板拆分（必填）

| 画板 ID | 画板名称 | 设计内容 | 备注 |
|---------|----------|----------|------|
| `APP-06-personal-info-01` | 个人信息收集清单-内容页 | 标题和正文 | P0 |
| `APP-06-personal-info-02` | 个人信息收集清单-链接缺失 | 空态和返回 | P0 |

### 2.5 编辑控件口径

本页无编辑控件。

## 3. 筛选与搜索

本页无筛选搜索。

## 4. 字段表

### 4.1 列表字段

| 字段 ID | 显示名 | 类型 | 必填 | 取值范围 | 校验规则 | 默认值 | 可编辑 | 敏感级别 | 数据来源 |
|---------|--------|------|------|----------|----------|--------|--------|----------|----------|
| `APP-06-PAGE-personal-info-list-FIELD-title` | 标题 | string | 是 | 1-40 字 | 不能为空 | 个人信息收集清单 | 否 | 普通 | 后台配置 |
| `APP-06-PAGE-personal-info-list-FIELD-version` | 版本 | string | 否 | 1-20 字 | 可为空 | 无 | 否 | 普通 | 后台配置 |
| `APP-06-PAGE-personal-info-list-FIELD-effective-time` | 生效时间 | datetime | 否 | yyyy-MM-dd HH:mm:ss | 可为空 | 无 | 否 | 普通 | 后台配置 |
| `APP-06-PAGE-personal-info-list-FIELD-link-type` | 承接方式 | enum | 是 | `M06-ENUM-link-type` | h5/native | h5 | 否 | 普通 | 后台配置 |
| `APP-06-PAGE-personal-info-list-FIELD-content-url` | 内容地址 | string | 条件必填 | URL/小程序路径 | H5 时必填 | 无 | 否 | 普通 | 后台配置 |

#### 列表字段附加属性

| 字段 ID | 默认排序 | 是否可排序 | 列宽 | 是否固定 | 是否可拖拽调整列宽 | 溢出处理 |
|---------|----------|------------|------|----------|--------------------|----------|
| `APP-06-PAGE-personal-info-list-FIELD-title` | 固定 | 否 | 自适应 | 否 | 否 | 换行 |

### 4.2 详情/表单字段

同列表字段，本页不提供编辑。

## 5. 操作表

### 5.1 行级操作

本页无行级操作。

### 5.2 批量操作

本页不支持批量操作。

### 5.3 页面级操作

| 操作 ID | 操作名 | 位置 | 触发条件 | 前置权限 | 二次确认 | 成功态 | 失败态 |
|---------|--------|------|----------|----------|----------|--------|--------|
| `APP-06-PAGE-personal-info-list-ACT-refresh` | 刷新内容 | 页面顶部 | 内容加载失败 | 访客/用户 | 否 | 重新加载 | `M06-ERR-content-link-missing` |

## 6. 数据联动规则

| 触发字段 | 触发事件 | 影响字段 | 联动行为 | 备注 |
|----------|----------|----------|----------|------|
| 承接方式 | h5/native | 内容区 | H5 用 webview，native 用原生正文 | `M06-RULE-compliance-content` |

## 7. 状态与异常

| 状态类型 | 触发场景 | 页面表现 | 用户可做的操作 | 引用 |
|----------|----------|----------|----------------|------|
| 加载态 | 内容加载 | loading | 无 | — |
| 空态（无数据） | 内容未配置 | 空态 + 返回 | 返回设置 | `M06-ERR-content-link-missing` |
| 空态（搜索无结果） | 本页不适用 | 本节不适用 | 无 | — |
| 错误态（网络） | H5 加载失败 | 错误页 + 重试 | 重试 | `M06-ERR-content-link-missing` |
| 无权限态 | 无 | 公开可访问 | 无 | — |
| 业务态-内容有效 | 已配置 | 正常展示 | 阅读 | `M06-RULE-compliance-content` |
| 降级态 | H5 不可用但有原生兜底 | 展示原生兜底 | 阅读 | `M06-ENUM-link-type` |

## 8. 查询与列表

本页不展示分页列表。

## 9. 验收标准

| AC ID | 场景 | 类型 | 优先级 |
|-------|------|------|--------|
| `APP-06-AC-personal-info-open` | 打开清单内容 | 正常 | P0 |
| `APP-06-AC-personal-info-missing` | 内容缺失 | 异常 | P0 |

```text
AC-ID: APP-06-AC-personal-info-open
Given 后台已配置个人信息收集清单 H5 链接
When  用户从设置页点击个人信息收集清单
Then  页面打开对应内容并展示标题和更新时间
```

## 10. 关联

| 关联类型 | 引用 ID | 说明 |
|----------|---------|------|
| 依赖的模块配置项 | `M06-CFG-compliance-links` | 合规内容 |
| 依赖的模块规则 | `M06-RULE-compliance-content` | H5 优先 |
