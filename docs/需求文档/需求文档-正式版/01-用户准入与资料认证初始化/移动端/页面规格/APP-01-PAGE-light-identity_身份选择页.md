# 页面规格 - APP-01-PAGE-light-identity 身份选择页

> 本文基于蓝湖 `登录-身份`。身份可跳过，但会影响后续资料字段分支。

| 版本 | 日期 | 修改人 | 变更摘要 |
|------|------|--------|----------|
| v0.1 | 2026-06-22 | Codex | 初稿 |

- **页面 ID**：`APP-01-PAGE-light-identity`
- **所属模块 PRD**：`../模块PRD文档/模块PRD_APP-01_用户准入与资料认证初始化.md`
- **页面路由**：待技术方案确认
- **入口来源**：出生日期页下一步；中断恢复
- **对应设计稿**：蓝湖 `登录-身份`
- **对应后台**：`ADM-01-PAGE-access-dict`

---

## 1. 页面定位

- **目标用户**：新登录用户
- **核心任务**：选择在校生/职场人，为资料字段和学历认证分流提供依据
- **页面类型**：首登轻量引导单步页

---

## 2. 布局

| 区块 | 内容 | 规则 |
|------|------|------|
| 标题区 | `请选择` + `—请选择你的身份（为你推荐匹配的异性）—` | 文案以蓝湖为准 |
| 选项区 | `在校学生`、`职场人` | 单选卡片 |
| 底部操作 | 圆形下一步按钮、跳过能力 | 未选择可跳过；已选择可保存下一步 |

---

## 3. 字段表

| 字段 ID | 显示名 | 类型 | 必填 | 取值范围 | 可编辑 | 数据来源 |
|---------|--------|------|------|----------|--------|----------|
| `identityType` | 身份 | enum | 否 | 在校生/职场人 | 是 | `M01-ENUM-identity-type` |

---

## 4. 操作表

| 操作 ID | 操作名 | 触发条件 | 成功态 | 失败态 |
|---------|--------|----------|--------|--------|
| `APP-01-PAGE-light-identity-ACT-select` | 选择身份 | 点击选项 | 选项高亮 | — |
| `APP-01-PAGE-light-identity-ACT-next` | 下一步 | 已选择身份 | 保存并进入学历选择页 | 保存失败提示重试 |
| `APP-01-PAGE-light-identity-ACT-skip` | 跳过 | 任意状态 | 不保存身份，进入学历选择页 | — |

---

## 5. 联动规则

| 触发 | 影响 | 规则 |
|------|------|------|
| 选择在校生 | 基本资料、学历认证 | 后续优先展示学校/专业、学生证上传 |
| 选择职场人 | 基本资料、学历认证 | 后续优先展示职业/年收入、毕业生学历认证方式 |
| 跳过身份 | 强引导 | 强引导中继续要求用户补充或选择默认分支前先确认身份 |

---

## 6. 验收标准

| AC ID | 场景 | 类型 | 优先级 |
|-------|------|------|--------|
| `APP-01-AC-identity-optional` | 不选择身份可跳过 | 正常 | P0 |
| `APP-01-AC-identity-branch` | 选择身份影响后续字段 | 正常 | P0 |
| `APP-01-AC-identity-recover` | 中断恢复反显已保存身份 | 正常 | P0 |

---

## 7. 关联

| 关联类型 | 引用 ID | 说明 |
|----------|---------|------|
| 枚举 | `M01-ENUM-identity-type` | 身份类型 |
| 规则 | `M01-RULE-light-guide` | 轻量引导可跳过 |
