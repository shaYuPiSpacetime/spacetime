# 项目交付缺口

本文档只登记尚未闭环、会影响开发验证或上线验收的明确缺口。任何密钥、Token、证书内容及签名 URL 均不得写入本文档。

## 当前缺口

| 编号 | 状态 | 交付缺口 | 影响 | 闭环标准 |
| --- | --- | --- | --- | --- |
| GAP-OSS-001 | 待 OSS 上传 | 本地开发环境已配置 `DEV_OSS_*`，但尚未执行本轮待交付静态素材的 OSS 上传与公网回读验证。 | 非底部 Tab 静态素材不能作为已交付的远程资源引用；相关页面不能宣称 OSS 资源闭环。 | 执行 `cd miniapp && npm run assets:upload-icons`；上传成功后确认 `miniapp/src/constants/ossIcons.ts` 已更新，逐项验证公网 URL 返回成功，并按项目规则核对上传前后文件字节一致。 |

## 配置核查记录（2026-08-21）

- 已将用户提供的本地环境文件复制为 `backend/.env.local`；该文件受 `.gitignore` 保护。
- 已从模板生成 `backend/src/main/resources/application-dev.yml`；该文件受 `backend/.gitignore` 保护。
- 数据库、Redis、固定登录账号、OSS、微信小程序及微信支付变量已提供；敏感值未写入版本库或本文档。
- `DEV_FIXED_LOGIN_ENABLED` 未显式提供，开发配置默认值为 `true`。
- 腾讯云 IM、短信与消息调度变量未显式提供；当前代码均有开发默认值（IM 关闭、短信 Mock、调度使用默认间隔），不阻塞基础开发启动，但不构成真实 IM/短信联调交付。
- 微信支付证书与私钥路径已配置，但对应文件当前在项目目录及其配置的本机路径下均不存在；支付下单与回调联调会因此阻塞，需补齐文件并确认其与商户号、证书序列号匹配。
