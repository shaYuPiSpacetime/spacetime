---
alwaysApply: false
---

# 测试用例总规范

> 合并原 001（用例分类）+ 003（手动用例/报告模板）+ 004（策略决策）+ 008（文档结构）

---

## 一、产出物输出路径与命名

所有测试产出物统一放在 `<项目根>/docs/测试文档/` 下，文件名为测试模块名称：

| 产出物      | 路径                                                   | 生产者                      |
| ----------- | ------------------------------------------------------ | --------------------------- |
| 测试用例    | `docs/测试文档/{模块名称}-testcase.md`                 | code-test skill             |
| 测试报告    | `docs/测试文档/{模块名称}-testreport.md`               | code-test skill Step 8      |
| L1 批量脚本 | `docs/测试文档/{模块名称}-test-l1.sh`                  | code-test skill Step 4 派生 |
| L2/L3 Java  | `backend/src/test/java/com/spacetime/.../XxxTest.java` | code-test skill Step 4 派生 |
| L4 E2E      | `frontend/e2e-tests/{模块名}.spec.ts`                  | code-test skill Step 4 派生 |

> L1 脚本执行方式：`API_URL=xxx TOKEN=xxx bash docs/测试文档/{模块名称}-test-l1.sh`

---

## 二、测试策略自动决策

### 后端评估（6 维度打分 → L1/L2/L3）

| #   | 维度            | 0 分          | 1 分          | 2 分             |
| --- | --------------- | ------------- | ------------- | ---------------- |
| A   | 新增/修改接口数 | 1-2 个        | 3-5 个        | 6+ 个            |
| B   | 状态流转逻辑    | 无            | ≤3 种状态     | >3 种或嵌套      |
| C   | 纯计算/规则逻辑 | 无（仅 CRUD） | 简单 if/else  | 复杂矩阵/公式    |
| D   | 数据关联复杂度  | 单表          | 2-3 表联查    | 4+ 表或跨库      |
| E   | 老代码影响范围  | 不涉及        | 修改注解/配置 | 修改核心 Service |
| F   | 安全变更        | 无            | 新增权限      | 移除安全注解     |

| 总分 | 后端组合     |
| ---- | ------------ |
| 0-2  | L1           |
| 3-5  | L1 + L2      |
| 6+   | L1 + L2 + L3 |

> **轻量模式**（无技术方案）：跳过评分，默认 L1，用户可手动追加 L2/L3。

### 前端评估（4 条件命中 → 是否追加 L4）

| #   | 触发条件                                          |
| --- | ------------------------------------------------- |
| G   | 多角色权限差异（不同 UserType 看到不同按钮/菜单） |
| H   | 复杂交互流程（弹窗嵌套/二次确认/状态联动）        |
| I   | 多页面联动（操作页面 A 需在页面 B 验证）          |
| J   | 核心业务页面（高频使用的列表/详情页）             |

无前端变更 → 不生成前端测试；有变更但 G/H/I/J 均未命中 → 仅手动用例；命中任一 → 手动 + L4。

用户可覆盖："只要 L1" / "全部都要" / "不要 Playwright"

---

## 三、用例编写规范

### 用例 ID

```
{层级}-{优先级}-{序号}
F1-P0-01    功能1 冒烟  第1条
L2-01       Controller 测试 第1条
L4-03       E2E 测试 第3条
M-02        手动测试 第2条
G-P3-01     全局权限测试 第1条
```

> **增量模式**：新增用例延续已有 ID 序号（如已有 F1-P0-01~03，新增从 F1-P0-04 开始），不修改已有用例 ID。

### 优先级

| 级别 | 名称      | 说明                              |
| ---- | --------- | --------------------------------- |
| P0   | 冒烟      | 核心路径跑通（每个功能至少 1 条） |
| P1   | 功能      | 正常/异常场景（覆盖技术方案 §10） |
| P2   | 边界      | 状态组合、空值、极端值            |
| P3   | 权限/安全 | 未登录、无权限、越权              |

### 从代码变更提取用例（完整/轻量模式共用）

| 变更类型                      | 提取为                                     |
| ----------------------------- | ------------------------------------------ |
| 新增 Controller 接口          | → P0 冒烟（正常调用）+ P2 边界（参数校验） |
| 修改 Service 方法逻辑         | → P1 功能（覆盖修改的分支）                |
| `@RequirePermission` 权限注解 | → P3 权限测试                              |
| 前端页面变更                  | → 手动用例（按需追加 L4）                  |
| 状态流转/状态机逻辑           | → L3 单元测试用例                          |

### 从审查报告补充用例

| 审查来源                           | 审查级别 | 转化为                   |
| ---------------------------------- | -------- | ------------------------ |
| superpowers:requesting-code-review | Critical | P0 冒烟（安全/数据问题） |
| superpowers:requesting-code-review | Warning  | P2 边界测试              |

---

## 四、testcase.md 文档模板

```markdown
# {模块名称} - 测试用例

> **关联文档**：
>
> - 测试报告：`docs/测试文档/{模块名称}-testreport.md`
>   **创建日期**：{YYYY-MM-DD}
>   **测试模式**：完整模式 / 轻量模式 / 增量模式
>   **目标项目**：后端 `backend/` / 前端 `frontend/`

---

## 1. 测试策略决策

### 后端评估

| 维度     | 评估结果 | 得分             |
| -------- | -------- | ---------------- |
| A~F      | {描述}   | {分}             |
| **总分** |          | **{N} → {组合}** |

> 轻量模式：无评分，默认 L1，用户追加：{L2/L3/无}

### 前端评估

| 条件 | 命中    | 说明   |
| ---- | ------- | ------ |
| G~J  | {✅/❌} | {说明} |

**最终策略：{L1+L2+L3} + {手动+L4}**

## 2. 测试数据准备

| 数据需求 | 用途   | 如何准备 | 是否幂等 |
| -------- | ------ | -------- | -------- |
| {描述}   | {用途} | {方式}   | {是/否}  |

> 标注幂等性：只读测试可重跑，写入测试需要 setup/teardown 策略。

## 3. L1 - 接口测试用例

> 本章节描述"测什么"，不含 cURL 脚本。可执行脚本由 Step 4 派生到 `docs/测试文档/{模块名称}-test-l1.sh`。

| 用例ID   | 优先级 | 场景   | 接口            | 前置条件 | 数据来源 | 期望结果 | 验证方式         |
| -------- | ------ | ------ | --------------- | -------- | -------- | -------- | ---------------- |
| F1-P0-01 | P0     | 正常流 | `POST /api/...` | {条件}   | 自动查询 | success  | 重新查询验证状态 |

> **列说明**：
>
> - **数据来源**：`自动查询`（脚本通过列表接口查找）/ `固定值`（如无效 ID 999999999）/ `无需数据`（如缺参数测试）
> - **验证方式**：`响应断言`（只检查返回值）/ `重新查询验证状态`（操作后查列表确认数据变更）/ `链式后续`（本用例的结果是下一条用例的前置条件）

## 4. L2 - Controller 测试用例（按需）

| 用例ID | 测试方法 | 验证点   | 期望   |
| ------ | -------- | -------- | ------ |
| L2-01  | test_xxx | {验证点} | {期望} |

> 派生脚本：`backend/src/test/java/com/spacetime/.../XxxControllerTest.java`
> JUnit 版本：5（Spacetime 统一 JUnit 5）

## 5. L3 - Service 单元测试用例（按需）

| 用例ID | 测试方法 | 输入   | 期望输出 |
| ------ | -------- | ------ | -------- |
| L3-01  | test_xxx | {输入} | {期望}   |

> 派生脚本：`backend/src/test/java/com/spacetime/.../XxxServiceTest.java`

## 6. L4 - E2E 浏览器测试用例（按需）

| 用例ID | 优先级 | 页面   | 操作步骤 | 期望结果 |
| ------ | ------ | ------ | -------- | -------- |
| L4-01  | P0     | {页面} | {步骤}   | {期望}   |

> 派生脚本：`frontend/e2e-tests/{模块名}.spec.ts`
> 前端项目：frontend（React 18 + Tailwind CSS + shadcn/ui）

## 7. 前端手动测试用例

| 用例ID | 优先级 | 操作步骤 | 期望结果 | 实际结果 | 状态 |
| ------ | ------ | -------- | -------- | -------- | ---- |
| M-01   | P0     | {步骤}   | {期望}   |          |      |

## 8. 补充用例（来自审查报告）

> 本章节记录从 superpowers:requesting-code-review 等审查补充的用例，如无则留空。

| 用例ID | 来源 | 审查级别 | 场景 | 期望结果 |
| ------ | ---- | -------- | ---- | -------- |
```
